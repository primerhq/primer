# Deferred from Specs

Planned-but-not-built items rolled up from the triage cards that
fed the consolidated developer docs. Entries are grouped by the
originating spec date.

This file is regenerated by `scripts/docs_verifier.py`. Manual
edits will be overwritten on the next consolidation run.

## 2026-04-26

### Gemini LLM Adapter Design

Source: `docs/superpowers/specs/2026-04-26-gemini-llm-design.md`

- Spec asserts the constructor raises ConfigError when api_key is missing; the shipped GeminiLLM treats api_key as optional and lets the upstream surface a 401 at call time instead.
- Spec proposes a single asyncio.Semaphore per adapter for concurrency; the shipped adapter routes concurrency through the shared RateLimiter coordinator (rate_limit_key llm:{provider.id}) instead, and tests/llm/test_adapters_no_local_semaphore.py enforces that drift.
- Spec lists nine google-api-core exception classes (Unauthenticated, PermissionDenied, ResourceExhausted, InvalidArgument, FailedPrecondition, InternalServerError, ServiceUnavailable, RetryError, DeadlineExceeded) as the source mapping; the shipped classifier dispatches purely on google.genai.errors.APIError.code plus httpx.TimeoutException / httpx.NetworkError. google-api-core is not imported.
- Spec's tests/test_google_errors.py was to use a _make_google_error helper that bypassed SDK constructors via cls.__new__; the shipped tests use the real SDK constructor with a minimal google-shaped error dict instead.
- Spec lists fourteen unit-test classes (~80 tests); the shipped tests/llm/test_gemini.py has sixteen (adds TestStream and TestCountTokens beyond the spec).

### HuggingFace Embedder Adapter Design

Source: `docs/superpowers/specs/2026-04-26-huggingface-embedder-design.md`

- Spec proposes self._semaphore = asyncio.Semaphore(provider.limits.max_concurrency); shipped code instead delegates to a RateLimiter abstraction (default InMemoryRateLimiter) keyed on 'embedder:{provider.id}'. The test class is still named TestConcurrency and validates serialisation behaviour, but the asyncio.Semaphore attribute the spec named does not exist.
- Spec's _encode_sync sets normalize_embeddings=False; shipped code uses normalize_embeddings=True with a docstring explaining the change is mandatory for cosine ranking in LanceDB and pgvector.
- Spec does not mention any prompt-prefix logic; the shipped adapter adds a model-family prompt registry (_prompts.py) and an additional _select_prompt argument feeding _encode_sync.
- Spec does not mention a 'task_type' field on ExtendedEmbeddingConfig; the shipped code reads config.task_type to decide between query and document prompts.

### OpenAI Embedder Adapter Design

Source: `docs/superpowers/specs/2026-04-26-openai-embedder-design.md`

- The spec calls the classifier's return type MatrixError, but primer.model.except_ uses PrimerError (the implementation returns PrimerError). This is a spec-vs-code naming mismatch worth resolving in the consolidated docs.
- The spec describes _get_client as passing config.api_key.get_secret_value() straight through 'including an empty string for unauthenticated LM Studio'. The shipped code instead substitutes the sentinel 'no-key-required' when the key is empty or None, because AsyncOpenAI rejects api_key=None outright. The spec's behavior is not implemented verbatim.
- The spec drafts concurrency as self._semaphore = asyncio.Semaphore(provider.limits.max_concurrency) and `async with self._semaphore:`. The shipped code instead uses an injected RateLimiter (with InMemoryRateLimiter fallback) and the key f'embedder:{provider.id}'. Spec wording does not mention the RateLimiter abstraction.

### OpenResponses LLM Adapter Design

Source: `docs/superpowers/specs/2026-04-26-openresponses-llm-adapter-design.md`

- Spec hardcodes the AsyncOpenAI client to receive the literal SecretStr value (provider.config.api_key.get_secret_value()). The code substitutes 'no-key-required' when api_key is None or empty because AsyncOpenAI rejects api_key=None outright. Drift from the spec, intentional.
- Spec uses tool.schema as the function 'parameters' field; the universal Tool exposes args_schema instead, so the adapter calls tool.args_schema. The field rename happened upstream of this adapter.
- Spec writes DocumentPart.data into file_data as raw base64 with a separate filename field; the implementation emits a 'data:{mime};base64,{...}' data URI because OpenAI rejects raw base64 with invalid_union. Documented inline.
- Spec assigns role='user/assistant/system' content as 'input_text'; the implementation routes assistant text through 'output_text' to support history replay without invalid_union. Spec did not anticipate this.
- Spec uses NOT_GIVEN sentinels for tool_choice and response_format; the implementation returns None and the caller conditionally adds the key to the payload. Behaviour equivalent.
- Spec lists the audio.transcript and image_generation_call partial event mappings explicitly; image_generation lifecycle events beyond partial_image (in_progress / generating / completed) are not exhaustively mapped, matching the spec's deferred list.

### Provider Adapters - Shared Architecture Design

Source: `docs/superpowers/specs/2026-04-26-provider-adapters-shared-arch-design.md`

- Root exception class named MatrixError - the actual class is PrimerError. (The hierarchy itself is present.)
- Per-adapter asyncio.Semaphore(provider.limits.max_concurrency) on _semaphore. Replaced project-wide by the RateLimiter ABC and InMemoryRateLimiter fallback; tests/llm/test_adapters_no_local_semaphore.py is an active source-level pin against the spec's design.
- Module name primer/model/except.py - the actual filename is except_.py (avoiding the 'except' Python keyword).
- Tests directory tests/llm/test_openresponses.py was called out as test_openresponses.py / test_anthropic.py / test_gemini.py / test_ollama.py in the layout, present, but the spec also said 'tests/llm/<provider>.py' (no test_ prefix) in the testing-approach section, which is inconsistent with how the tests actually landed.

### ToolsetProvider + MCP + OAuth Design

Source: `docs/superpowers/specs/2026-04-26-toolset-provider-oauth-design.md`

- The spec's tests/toolset/oauth/test_mcp_oauth.py module is not present; OAuth integration is folded into tests/toolset/test_mcp.py (TestMcpOAuthIntegration) instead.
- tests/integration/test_mcp_smoke.py is not gated on a real OAuth-protected MCP server URL - the OAuth smoke variant the spec described has not landed.
- negotiate() in primer/toolset/oauth/discovery.py never auto-selects the 2025-11-25 spec version; it returns 2025-06-18 whenever the protected-resource path resolves, and only honours 2025-11-25 when a caller pins OAuthConfig.spec_version='2025-11-25'. The spec called for trying 2025-11-25 first and applying its stricter WWW-Authenticate parsing on response 401s.
- The provider registry's default toolset factory in primer/api/registries/provider_registry.py constructs McpToolsetProvider without an oauth handler; there is no wiring that turns HttpConfig.oauth into a PrimerOAuthHandler instance.
- No HTTP route exists for the application-side OAuth callback (e.g. /toolsets/{id}/oauth/complete) that calls McpToolsetProvider.complete_oauth; the UI cannot finish a flow today even though the model layer is complete.
- PrimerOAuthHandler does not document or expose a separate AuthenticationError pathway distinct from AuthRequiredError on the spec's 'token cached but expired, refresh denied' row of the error mapping table; the code retries by raising AuthRequiredError, which is correct, but the mapped distinction in the spec's error table is implicit only.

## 2026-05-02

### Workspace + Sandbox Design

Source: `docs/superpowers/specs/2026-05-02-workspace-design.md`

- Spec calls the lifecycle ABC WorkspaceProvider; code renamed it to WorkspaceBackend so the WorkspaceProvider name could be used by a new persisted-config row. Any consolidated doc must match the code's naming.
- Spec calls out a `packages: list[PackageSpec]` and a `PackageSpec` model on WorkspaceTemplate; the field and class were removed (image-as-BOM redesign) and a backwards-compat validator silently drops legacy `packages` from stored rows.
- Spec's exec tool defines a `background=True` mode plus an exec_status companion; the local exec tool implementation raises NotImplementedError for background=true. The spec explicitly marks the companion TBD.
- Spec's ResourceLimits.cpu_cores/memory_bytes/network are SHOULD-enforce on the backend; LocalWorkspaceBackend only emits warnings rather than enforcing them - acceptable for the local-dev tier but worth calling out.
- Spec text says workspace.aclose has no concept of phase / probe; no such health surface (Workspace.status, ping, phase, last_probe_at, failure_reason) is mentioned. Code added a probe machinery.
- Spec's session.aclose says cleanup of `.tmp/<session_id>/` happens on END but does not describe Workspace.aclose tearing down the runtime container/pod. The current backends do destroy/preserve resources per backend choice; the consolidated doc should describe the current rule per backend.

## 2026-05-03

### Agent Executor Design

Source: `docs/superpowers/specs/2026-05-03-agent-executor-design.md`

- Spec describes a top-level resolve_llm_model helper. No such function exists in primer/agent; the worker pool resolves the LLMModel inline via WorkerPool._resolve_llm_model.
- Spec sets DEFAULT_TRIGGER_RATIO to 0.85; the shipped CompactionStrategy uses 0.90.
- Spec puts the pruning placeholder text as '... see git history at <sha> if you need it' / 'see ThreadMessage.id'. The shipped placeholder is just '[output of N chars omitted by compaction; check the persisted history if you need the full text]' with no per-backend hint.
- Spec describes _BaseAgentExecutor._dispatch_tool_calls as the central dispatcher. The shipped base class still defines it (defence-in-depth), but the real per-turn loop is in primer/agent/loop._dispatch_tool_calls; only the loop helper is exercised at runtime.
- Spec's _last_assistant_text_was_question / _extract_question heuristic is documented as 'strip plus check trailing ?'. Shipped _extract_question just returns the rstripped text rather than slicing to the question mark.
- Spec's CompactionStrategy.maybe_compact decision flow describes max(estimate, last_known_input_tokens) and a token estimate formula per Part. Implementation matches, but no _validate_input_messages step is shipped (spec called for one in the invoke pseudocode).
- Spec specifies 'one DB transaction for AgentExecutor; one git commit for WorkspaceAgentExecutor' for end-of-turn persistence. AgentExecutor's _persist_turn issues per-row Storage.create calls; there is no transaction primitive in v1 (spec acknowledged this for compaction; not for normal persistence).
- Spec's AgentExecutor _replace_compacted_head delete-then-create-then-update sequence is described as 'a future revision can wrap this in a transaction'. Code implements the un-transactional v1 path verbatim; no follow-up was added.

### Agent Graph Design

Source: `docs/superpowers/specs/2026-05-03-agent-graph-design.md`

- The spec defines Graph.entry_node_id as a required string field; the code drops it in favour of inferring the entry from the unique _BeginNode (which itself is not in the spec).
- The spec's _TerminalNode (kind='terminal') does not exist; the code replaces it with _EndNode (kind='end') carrying output_template + output_schema for structured graph output.
- The spec's JsonPathBranch.when (flat dict mapping dotted paths to expected values) is absent; the code carries a list of BranchCondition objects with explicit op + value + path fields - a strictly richer surface, but the spec's flat-dict shape is gone.
- The spec's _GraphNodeEvent inner type carries inner_type + inner_payload (as shipped); the spec also mentioned a parallel primer/graph/events.py re-export module - no events.py file exists under primer/graph/.
- The spec's NodeOutput defines text + parsed + history + iteration only; it does not mention the error / ended_detail fields the code adds for fan-out collect mode.
- The spec's per-node workspace layout includes a session.json (SessionInfo: graph-level), graph.json (GraphBinding snapshot), waiting.json (when status == WAITING) and per-node agent.json; the code writes state.json + nodes/<nid>/messages.jsonl and a optional graph.json via write_graph_binding, but session.json / waiting.json / nodes/<nid>/agent.json are not produced by WorkspaceGraphExecutor.
- The spec's signature for invoke is invoke(messages: list[Message]); the workspace executor's invoke widens this to Any to accept graph_input. Callers passing list[Message] still work, but the public type signature no longer matches the spec verbatim.

### Document Ingestion Design

Source: `docs/superpowers/specs/2026-05-03-document-ingestion-design.md`

- MarkItDownLoader: spec calls for primer/ingest/loaders/markitdown.py wrapping markitdown.MarkItDown; the directory only contains docling.py. No 'markitdown' or 'MarkItDown' references anywhere under primer/ or tests/.
- Optional dependency extras: spec calls for [project.optional-dependencies] entries 'docling' and 'markitdown' in pyproject.toml. Neither extra exists; docling is a core dependency and markitdown is absent.
- Lazy backend import + ConfigError: spec says DoclingLoader should import docling lazily inside __init__ and raise a friendly ConfigError when the backend is missing. The actual DoclingLoader imports docling at module top level (so an absent docling install fails at import time, not with the typed error).
- Test fixtures: spec calls for tests/ingest/fixtures/ with a sample PDF and gated tests test_docling_loader.py, test_markitdown_loader.py, test_docling_splitter.py. None of these exist; tests/ingest only contains __init__.py, test_models.py, test_recursive_splitter.py, test_ingester.py.
- Shared in-memory test doubles: spec calls for tests/ingest/_doubles.py housing the shared VectorStore and Embedder fakes. The fakes are inlined inside tests/ingest/test_ingester.py instead.
- Real-OpenAI smoke test: tests/ingest/test_real_openai_ingest_smoke.py (gated on OPENAI_API_KEY) is not present.
- Spec's IngestResult walkthrough says loaded.meta is merged onto each chunk's meta 'under the key source_doc and chunk.meta (top level)'. The code does this, but the Chunk docstring still describes the top-level merge - the wording in the spec is slightly muddled but the behaviour matches.
- Spec lists ProviderError as the error wrapping for loader transport failures (corrupt PDF, network fetching URL); the implementation raises UnsupportedContentError for any docling.convert failure regardless of cause.

## 2026-05-05

### MMR + Cross-Encoder Reranking Design

Source: `docs/superpowers/specs/2026-05-05-mmr-cross-encoder-reranking-design.md`

- Public search endpoint integration. The spec describes the pipeline as the new default for vector search, but POST /v1/collections/{id}/search in primer/api/routers/knowledge.py still calls VectorStore.search directly and ignores Collection.search. CollectionSearcher is only consumed by primer/catalog/catalog.py for the four internal SemanticCatalog collections.
- Cross-encoder wiring for the SemanticCatalog. SemanticCatalog._searcher_for builds CollectionSearcher without ever passing cross_encoder, so InternalCollectionsConfig.cross_encoder cannot actually drive reranking. The catalog also does not copy InternalCollectionsConfig.mmr or .cross_encoder onto the Collection rows it creates.
- BAAI/bge-reranker-v2-m3 as the default model. Spec section 4 names it the 2026 open-source default; primer/bootstrap/defaults.py instead seeds 'cross-encoder/ms-marco-MiniLM-L-6-v2'.
- Gated integration smoke test for the BAAI/bge-reranker-v2-m3 round-trip mirroring tests/integration/test_huggingface_smoke.py. The spec calls for it (section 7); no such test exists under tests/integration/.
- RateLimits | None typing on CrossEncoderProvider.limits. The shipped model uses the shared Limits type and makes it required; there is no RateLimits class today.
- Discriminated-union framing on CrossEncoderProvider.config. The spec describes it as 'discriminated union once a 2nd backend lands'; the shipped field is typed as a bare HuggingFaceCrossEncoderConfig, with no Annotated[Union, Field(discriminator=...)] scaffolding to extend.

## 2026-05-08

### REST API Foundation (Phase 0) Design

Source: `docs/superpowers/specs/2026-05-08-rest-api-foundation-design.md`

- The spec's VectorStoreRegistry (single-active store keyed at row id '_active_vector_store', constructed via VectorStoreBackendFactory, exposed via get()/get_provider()/invalidate()/aclose()) is not in the code. The implementation chose SemanticSearchRegistry (per-row VectorStoreProvider cache keyed by SemanticSearchProvider id) instead.
- The spec's VectorStoreConfig single-row 'active store' config model (referred to as 'ships in Phase 0, model only') is not in the code in that role. primer/model/vector.py defines VectorStoreConfig but the registry key surface is now SemanticSearchProvider rows.
- X-Primer-Principal request-header passthrough is not implemented. get_principal reads request.state.principal which is populated by the cookie+bearer AuthMiddleware; there is no Header(alias=PRINCIPAL_HEADER) dependency.
- MATRIX_ env_prefix (and the MATRIX_CONFIG_PATH TOML hook) is not in the code; the implementation uses PRIMER_ env_prefix and PRIMER_CONFIG_PATH instead.
- AppConfig.db_password (SecretStr) and the unconditional Postgres-only db_host/db_port/db_database/db_user/db_min_pool_size/db_max_pool_size fields described in the spec are not in the code. The implementation collapsed the storage knobs into a single optional db: StorageProviderConfig | None field that defaults to embedded SQLite - the zero-config boot path.
- ConfigError -> 500 '/errors/misconfigured' from the spec mapping table maps to 503 '/errors/service-unavailable' in the live error map.
- The spec's app.state.vector_store_registry attribute is absent; the lifespan stashes semantic_search_registry (and many other attributes) instead.
- The 'Toolset dispatch is deferred to Phase 1' Phase-0 stub (get_toolset raising ConfigError('toolset dispatch not configured; ships in Phase 1') until Phase 1 lands) was never written: the production dispatch is in place from day one because Phase 1 followed without a Phase-0 release.
- The aclose() ABC additions on LLM/Embedder/CrossEncoder/ToolsetProvider were the spec's planned Phase-0 contract. The methods do exist now, but they were not landed as no-op defaults - the actual adapters all implement them.

### Semantic Catalog Subsystem Design

Source: `docs/superpowers/specs/2026-05-08-semantic-catalog-design.md`

- The spec promises that 'nothing in the existing tree imports from primer.catalog'. In practice the production subsystem (primer/internal_collections.py) does not import from primer.catalog either, so no one in the live codebase actually constructs or wires SemanticCatalog. The spec'd module is exercised only by unit tests; agent-facing search runs through InternalCollectionsSubsystem.search instead.
- The spec hard-codes _catalog_agents / _catalog_tools / _catalog_graphs / _catalog_collections as the reserved collection ids. The shipping production runtime uses _internal_agents / _internal_graphs / _internal_collections / _internal_tools (and a fifth _internal_ai_docs). No code path actually creates rows under the _catalog_* ids outside the unit tests.
- Spec section 3.2 'Tool.id semantics change' calls for a docstring update on Tool.id describing the toolset_id__bare_name convention. primer/model/chat.py does carry Tool.toolset_id, but the Tool.id docstring still describes generic Identifiable id semantics; the scoping convention is documented only on ToolExecutionManager and in primer/catalog/catalog.py.

### Web Toolset Design (web-search + http-request)

Source: `docs/superpowers/specs/2026-05-08-web-toolset-design.md`

- The spec's file layout `primer/toolset/web/backends/{base.py,ddg.py,__init__.py}` no longer exists. Backend code was moved to primer/web_search/ (adapter.py, duckduckgo.py, ...) and renamed from WebSearchBackend / DuckDuckGoBackend to WebSearchAdapter (ABC) / DuckDuckGoAdapter.
- Spec test layout names tests/toolset/web/test_ddg_backend.py; that file does not exist. DDG coverage lives at tests/web_search/test_duckduckgo_adapter.py.
- Spec types the backend exception translation as `ddgs.exceptions.DDGSException -> primer.model.except_.ProviderError`. The actual exception class hierarchy uses WebSearchUnavailable / WebSearchProviderError (PrimerError subclasses) instead; no ProviderError class is referenced.
- Spec proposes a `backend` kwarg on build_web_toolset that defaults to DuckDuckGoBackend(); the current factory removes that kwarg entirely in favour of a required web_search_service kwarg routed through the registry.

## 2026-05-10

### Background Session Execution + Distributed Scheduler

Source: `docs/superpowers/specs/2026-05-10-background-execution-scheduler-design.md`

- session_leases table is absent from primer/scheduler/postgres.py (and not created anywhere). The spec's idx_session_leases_claimable index, runnable column, and next_attempt_at lease column are not produced by the scheduler; lease ownership now lives in the ClaimEngine's leases table created by primer/storage/postgres.py.
- Scheduler.claim and Scheduler.heartbeat_leases are absent from the ABC and impls - the spec listed them as the worker pool's primary entrypoints. Their responsibility moved to ClaimEngine.claim_due / heartbeat. The spec was not updated to reflect this.
- Scheduler affinity ordering (CASE WHEN s.last_worker_id = $worker_id THEN 0 ELSE 1 END) called for in spec §5.3 is not present anywhere; the ClaimEngine query in primer/claim/sql.py picks by priority_score / next_attempt_at without re-attach affinity.
- Boot-time safety sweep that re-arms session_leases.runnable for sessions in CREATED / RUNNING (spec §10) is not run; PostgresScheduler.initialize only marks workers dead past 5 minutes of missed heartbeats.
- WorkspaceSession.attempt_count and WorkspaceSession.last_error fields listed in spec §8.2 are not declared on the Pydantic model. The Postgres complete_turn SQL writes them via jsonb_set under attempt_count / last_error keys, so they exist in the JSONB row but cannot be read through the typed model accessor.
- _run_one_turn (the Scheduler-only path) tracks _in_flight via direct .add / .discard but the engine path duplicates the bookkeeping; there is no test_runtime_modes test under tests/api covering the WORKER-mode router muting documented in spec §9.2 / 11.
- There is no dedicated migration file under primer/storage/postgres/migrations/ producing session_leases / workers tables; the workers DDL ships inline in PostgresScheduler.initialize and the leases DDL ships inline in PostgresStorageProvider.
- Cross-worker live debugging hooks (out-of-scope per spec §16) remain out of scope; metrics are emitted but no Prometheus exporter is wired (matches spec deferral).

## 2026-05-11

### Workspace Backends Rearchitecture - Design

Source: `docs/superpowers/specs/2026-05-11-workspace-backends-design.md`

- Spec §8.1-§8.3 calls for three concrete Sandbox subclasses (DockerSandbox / PodmanSandbox / ContainerdSandbox) tightly coupled to each runtime's wire protocol. The code ships only WSSandbox and routes all three runtimes through it.
- Spec §8.2 PodmanRuntimeAdapter mirroring DockerRuntimeAdapter against the podman socket: primer/workspace/runtime/podman.py is a stub that raises ConfigError on construct.
- Spec §8.3 ContainerdRuntimeAdapter speaking CRI v1 over gRPC with vendored stubs under primer/workspace/runtime/containerd/stubs/: primer/workspace/runtime/containerd/adapter.py is a stub that raises ConfigError; no stubs directory, no grpcio dependency.
- Spec §6.2 ContainerWorkspaceConfig fields default_image, name_prefix, volume_driver, pull_policy and the runtime: ContainerRuntimeConfig discriminated-union shape: the implemented ContainerWorkspaceConfig only carries runtime, connection, reachability, image_pull_secrets; image / pull_policy / volume_driver / name_prefix moved or were dropped.
- Spec §6.2 KubernetesWorkspaceConfig fields kubeconfig_path, context, namespace, name_prefix, storage_class, default_pvc_size, service_account, image_pull_secrets, pull_policy, pod_security_context, container_security_context, node_selector, tolerations, annotations, labels: the implemented KubernetesWorkspaceConfig is connection + reachability + variant only; those fields moved to the template or were dropped.
- Spec §6.3 _ContainerTemplateConfig.extra_volume_size and _KubernetesTemplateConfig.container_overrides field: only container_overrides existence is mentioned in code as security-restricted, but the explicit ContainerTemplateConfig.extra_volume_size is absent.
- Spec §9.1 SandboxStateRepo.initialize uses a single shell-string ('set -e; if [ ! -d .git ]; then git init...; fi') with shlex-quoted paths; the code uses argv-form Sandbox.exec calls instead.
- Spec §17.1 tests/workspace/runtime/test_runtime_contract.py and the parametrised contract suite across Docker/Podman/containerd: no test_runtime_contract.py exists; only test_docker_backend.py is wired (gated).

## 2026-05-15

### Web Console Implementation - Top-Level Design

Source: `docs/superpowers/specs/2026-05-15-web-console-implementation-design.md`

- Spec §1.3 / §4.4 mandate deleting ui/components/predicate-builder.jsx and removing the index.html script tag. The file still exists at ui/components/predicate-builder.jsx and is still listed in ui/index.html as a text/babel script (so it ships in the /console/_app.js bundle as dead code).
- Spec §9.1 lists /toolsets/builtin as a literal route ahead of /toolsets/:id. router.js has only /toolsets, /toolsets/:id and no /toolsets/builtin entry; built-in toolsets are not addressable via the documented hash.
- Spec §3.2 / §2.2 model the three core CDN scripts as SRI-pinned unpkg.com fetches with CSP script-src enumerating https://unpkg.com. The current code self-hosts those scripts under ui/vendor/ and the CSP script-src enumerates only 'self' + 'unsafe-eval' + 'unsafe-inline' - the spec's described unpkg.com source-list entry and the script-tag SRI integrity attributes are absent.
- Spec §3.2 lists IBM Plex Sans / Mono delivered via Google Fonts CSS with font-src https://fonts.gstatic.com. Code self-hosts the Plex woff2 subsets under ui/vendor/fonts/ and constrains font-src to 'self' only.
- Spec §2.1 sketches a single app.mount('/console', StaticFiles(directory=..., html=True)) call. Code wraps StaticFiles in a _CachingStaticFiles subclass that applies path-aware Cache-Control. Not strictly a gap, but the spec is silent on caching behaviour.

## 2026-05-16

### UI Agents - Design

Source: `docs/superpowers/specs/2026-05-16-ui-agents-design.md`

- Per-row status pill cache + 30s poll while page is visible (spec 2.1). The list refetches each row's status once per list-data change, with no interval and no visibility gating.
- Top-right Test agent button on the detail header that opens NewSessionModal with defaultAgentId + bindingKind=agent (spec 3.1). The header instead shows a Chat button that calls POST /v1/chats; AG_NewSessionModal is defined in the file but never instantiated.
- ?create=1 query-param auto-open of the New Agent modal from the dashboard quick action (spec 2.3).
- top_p, max_tokens, response_format (text / json_schema) and tools choice (auto / required / none / specific) inputs in the create / edit form (spec 2.3 + 4.1). Only temperature is wired; the Agent model itself has no field for these.
- Used-in-graphs reference row that fetches GET /v1/graphs?limit=200 and client-side filters for nodes referencing this agent (spec 4.3). Not present in AG_ReferencesPanel.
- Recent sessions row in the References panel from GET /v1/sessions?agent_id={id}&limit=5 (spec 4.3). The Sessions tab covers the data but the references panel itself does not surface a top-5 preview.
- Tools tab JSON-Schema-rendered argument form per tool (spec 5). AG_ToolEntry shows the raw schema in a collapsible code block, not a rendered form.
- Per-tool canonical id assertion using tool.id rather than tool.name (spec 5, app spec section 8). The code does use tool.id, but the modal's tool catalogue uses the catalogue's tool.id (bare) plus a separate scoped_id and there is no test asserting the id-vs-name discipline.
- Metadata tab read-only render of created_at, updated_at, id, version alongside metadata (spec 7). Currently only the agent.metadata bag is shown.
- Config-tab metadata JSON textarea for editing metadata (spec 7). No metadata editor is provided; AG_NewAgentModal also omits metadata.

### UI Backend Additions - Design

Source: `docs/superpowers/specs/2026-05-16-ui-backend-additions-design.md`

- GET /v1/health/recent_errors?since=<iso>&limit=<n> with the documented items/total envelope - not implemented; the Dashboard tile surfaces a literal 0 rather than the spec's '-' placeholder.
- POST /v1/toolsets/{id}/tools/{tool_id}/invoke with {arguments, workspace_id?} -> {result, duration_ms} - not implemented (intentional v1 deferral).
- POST /v1/workspaces/{wid}/files multipart variant for drag-drop binary upload - not implemented (intentional v1 deferral).
- SSE endpoints on /v1/sessions/stream and /v1/workers/stream - not implemented (intentional v1 deferral; WebSocket session stream is the only live surface).

### UI Chrome - Design

Source: `docs/superpowers/specs/2026-05-16-ui-chrome-design.md`

- Live counts polled from inside chrome.jsx components. The spec said 'each becomes a thin wrapper over useResource'; in practice the polling is centralised in app.jsx and chrome.jsx is presentational. The behaviour is equivalent but the code split is different.
- Multi-status filter on /v1/sessions (`?status=created,running,paused`). The live sidebar count uses /sessions?limit=1 which counts ALL sessions, not just live ones. The spec's note flagged this as a potential fallback but neither the comma-list nor the three-call sum was implemented.
- Sidebar localStorage keys 'primer-console-sidebar-collapsed' and 'primer-console-sidebar-collapsed-mode'. Live keys are 'primer.sidebar.collapsed' and 'primer.sidebar.iconsOnly' - non-blocking but the spec's naming was not adopted.
- Active-state highlight via useRouter().path === route OR path.startsWith(route + '/'). Live code passes a 'page' string from app.jsx and uses === comparison; app.jsx contains the detail-to-list mapping (e.g. session-detail -> sessions). Behaviour matches but the spec's rule is in app.jsx, not chrome.jsx.
- Bell warning panel. Only the IC bootstrap-required case is implemented as a single navigate button. The spec asked for a numeric badge, a dropdown list with per-warning 'Go to fix' links, AND a draining-workers count derived from the workers poll. The drainer counter never landed.
- Worker pool colour states. Live code is err/warn/default (two-state); the spec's green/yellow/red rule including scheduler.alive checks was simplified because /v1/health doesn't expose the flag the spec referenced.
- Topbar instance label reads from tweaks.instanceLabel. tweaks.instanceLabel exists as a default but Topbar hardcodes 'primer · localhost:8765'. Wiring is one prop away and the tweaks panel could add the input.
- Command palette workspaces / agents / graphs entity sources. Spec asked for parallel /workspaces /agents /graphs fetches with per-source spinner rows; live palette only has sessions + docs + pages. The 'incremental render' UX is therefore not present.
- Command palette subsequence + run-length scoring. Live match is plain substring includes - simpler and less accurate on long queries. ~30 LoC scorer the spec specified was never written.
- Command palette quick-action entries (Open dashboard, Open OpenAPI, Open health, Toggle theme, Open tweaks panel) at the top of the empty-query result list. None present.
- ToastContainer move from app.jsx into chrome.jsx + migration onto useToast. App.jsx still owns a duplicate toast-stack JSX and a setState array; the auto-dismiss timings are 5s/12s rather than the spec's kind-specific durations; 'Copy request id' and 'action buttons' are not rendered.
- Tweaks panel addition of an instanceLabel free-text input. The tweak exists in the defaults but no UI input ships.

### UI Dashboard - Design

Source: `docs/superpowers/specs/2026-05-16-ui-dashboard-design.md`

- Per-spec table 2.1, the Workers tile should source counts from /v1/health worker_pool.{capacity,in_flight} + an active/draining count from /v1/workers. The code receives workerStats from app.jsx (which does combine those two endpoints) but the Workers tile derives its main value from workersItems.length (real worker rows) and its status from workerStats.in_flight / workerStats.capacity, not from a single canonical formula. There is no dashboard-level use of /v1/health.
- Sessions tile is supposed to read /v1/sessions?status=running,paused,created&limit=1 with the total field; the code instead fires three separate calls (/sessions?limit=1 for grand total, POST /sessions/find for running, POST /sessions/find for paused) and clicks through to /sessions without carrying the status filter.
- Errors-1h tile is supposed to render a long dash with a tooltip endpoint not implemented (planned) per backend-additions section 2.1. The code renders the number 0 and the sub text no 5xx responses with status ok when zero, which can mislead the operator into thinking the metric is real.
- Worker pool gauge updates every 2s sharing the topbar:health cache (spec section 2.2). The dashboard does not subscribe to topbar:health at all; it consumes the prop snapshot and triggers a 1Hz force-rerender for animation. Cache reuse happens transparently through app.jsx.
- Spec section 2.3 calls for an in-memory ring buffer of length 150 (5 min at 2s cadence) discarded on navigation. The implementation seeds a 30-point pseudo-random walk in useMemo and overwrites the last point with workerStats.in_flight; no real ring buffer exists.
- Spec section 2.4 says the recent sessions table polls at 5000ms with columns status pill, id (12-char + tooltip-full), agent_id or graph_id, workspace_id, last_turn_at relative. The poll cadence matches, but the id is truncated at 22 chars without a tooltip and the time column shows created_at not last_turn_at; a Turns column was added.
- Spec section 2.5 calls for ?create=1 to be appended for New Workspace and New Agent so the destination page opens its create modal on mount. The implementation navigates to plain /workspaces and /agents with no query param.
- Spec section 2.5 calls for an Open Search Bench quick action navigating to /knowledge/search (replacing the removed Open Predicate Builder). The code ships Collections (navigate /knowledge/collections) and Workers in those slots; no Search Bench entry exists.
- Spec section 2.5 expects the 2x3 grid to drop to a single row of 5 buttons once Predicate Builder is removed; the code still uses a 2x3 grid (gridTemplateColumns 1fr 1fr) and keeps six buttons.
- Spec section 3 calls for skeleton loaders (4 cards + gauge + 8 rows) on first load and a per-card long-dash with retry icon on partial failure. The code shows no skeletons; tiles either render zeros or the previous data.
- Spec section 4 schedules IC config polling at 30000ms shared with sidebar:ic-config. The dashboard does not poll IC config at all; it reads the icConfig prop sourced from app.jsx's separately-keyed hook (not sidebar:ic-config).
- Spec section 4 also calls for pause on document.hidden and idle. use-resource.js supports a pauseWhile option but the dashboard hooks do not pass one; visibility/idle pausing relies entirely on foundation defaults.
- predicate-builder.jsx still exists in ui/components and is loaded via ui/index.html, contradicting the spec assertion that the predicate builder is gone.

### UI Foundation - Design

Source: `docs/superpowers/specs/2026-05-16-ui-foundation-design.md`

- Deletion of ui/components/predicate-builder.jsx - the file still exists at ~390 lines and is still referenced by a text/babel script tag in ui/index.html (line 72). Only the chrome.jsx sidebar entry has been removed.
- scripts/ui/check-foundation.sh CI shim that would python -m http.server the foundation tests and curl-fetch the parse result. No scripts/ui/ directory exists.
- Per-app.jsx replacement of every local toast state with useToast - useMutation's error path falls back to module-level toastPush, but the App's top-level toast container has not been audited in this triage to confirm full migration.

### UI Graphs - Design

Source: `docs/superpowers/specs/2026-05-16-ui-graphs-design.md`

- The persistent yellow banner 'Graph executor is unimplemented (NotImplementedError). Sessions bound to a graph end with failed on the first turn - pinned by T0156' is nowhere in graphs.jsx or app.jsx; the only reminder is the page-sub 'Multi-agent flows . executor not yet shipped' rendered by app.jsx. The premise is also stale - primer/graph/executor.py + primer/graph/workspace_executor.py are implemented and the worker can run a graph turn.
- The 'status pill from /v1/graphs/{id}/status' on the list page (spec section 2) is done by a non-cached Promise.all batch keyed off list.data rather than a per-row useResource subscription with the documented 30s polling cadence; refreshing the list re-fires every status call.
- Spec section 3.2 calls Begin/End nodes 'terminal' and describes a single 'small filled circle' kind; the code splits them into kind='begin' (hollow accent ring) and kind='end' (filled disk) with separate Pydantic node classes (_BeginNode / _EndNode).
- Spec section 3.2 limits node kinds to agent / graph / terminal and edges to static / json_path / callable. The code ships fan_out / fan_in / tool_call node kinds and represents callable + json_path as variants of a single conditional edge whose router discriminator is json_path or callable.
- Spec section 3.4 says the side panel is read-only when a node is selected because 'edits are at the graph level, not node level'. The implementation makes the side panel a full editor for every per-kind field; PUT-replace is still the wire shape, but node-level edits are first-class in the UI.
- Spec section 4 says 'On 422: render field-level errors near the offending node (use extensions.errors[].loc[] to map to nodes/edges)'. The Save flow only routes 422 to a toast and to the in-modal fieldErrors map on the create modal; node-anchored inline 422 errors in the editor do not exist.
- Spec section 4 calls the diff count an 'n changes' pill; the toolbar instead shows 'unsaved changes' as a muted span (no count) and gates Save on diffCount > 0.
- Spec section 5 declares subgraph navigation 'v1 implements this; nested graphs render as a link, not an inline editor'. Double-click does navigate, but there is no link affordance on the node itself - the operator has to discover the double-click via the GR_GraphStatsBlock help text.

### UI Health - Design

Source: `docs/superpowers/specs/2026-05-16-ui-health-design.md`

- Spec 4 says the sparkline should render via window.matrixVendor.sparkline; the implementation uses a local BigSpark SVG component and ui/vendor/sparkline.js exposes window.primerVendor.buildSparkline, not window.matrixVendor.sparkline.
- Spec 3 requires the Degraded subtitle to list which checks failed (status, scheduler.alive, worker_pool.capacity == null); the current subtitle only shows the request line, HTTP status / error, and version, with no per-check failure breakdown.
- Spec 3 says the hero must also require worker_pool.capacity != null to be green; the code only checks status === 'ok' AND sched.alive === true, so an API-only process (capacity=null) would still show green.
- Spec 5.2 calls for in_flight and capacity to be rendered prominently above the worker-pool table with a utilization percentage; the current MetricsPanel lists them as ordinary rows and the utilization % is not shown (only an amber threshold on the in_flight cell).
- Spec 4 mandates a documented footer 'Sparkline buffer is in-memory; resets on navigation.'; no such footer is rendered.
- Spec 7 says runtime_mode=api should make the hero say 'Degraded - no worker pool'; the current copy is just 'Degraded' with no worker-pool-specific reason.

### UI Internal Collections - Design

Source: `docs/superpowers/specs/2026-05-16-ui-internal-collections-design.md`

- Spec's State 1 'Collections multi-select chip group: agents / graphs / collections / tools' is absent. The PUT body has no collections field; the backend always materialises all four reserved collections.
- Spec's localStorage flag primer-console-ic-bootstrapped is not used. The page derives state from activated_at on the config row instead - a strictly stronger signal.
- Spec's 'Bootstrap response includes per-source counts; render them once on completion' indeterminate-spinner v1 is replaced by a richer phase-aware progress panel driven by the /bootstrap/status row (not the bootstrap response).
- Spec's CDC-lag metric pulled from /v1/health metrics block is not implemented. The page never calls /v1/health; the lag is only acknowledged as the eventual-consistency banner copy.
- Spec's status panel '# documents per source' on State 3 is not rendered. ConfigKV does not include per-source counts (the spec acknowledged a backend dependency that did not land).
- Spec's pollMs argument to /bootstrap/status is fixed at 1s/5s; there is no operator knob (not required, but worth noting).
- Spec's filter-field T0174 surfacing under section 6 is not rendered as a State-3 note; the page is silent and only the search bench (in knowledge.jsx) carries any related copy.

### UI Knowledge - Design

Source: `docs/superpowers/specs/2026-05-16-ui-knowledge-design.md`

- Per-row document count column on the Collections list (spec 2.1). The code only surfaces the count inside the selected-collection detail panel.
- Collection list shows name; the schema and UI both use description instead (Collection inherits Describeable, not a Name field).
- Detail-page tabs (Documents / Config / Bind embedding provider / Destroy) called out in spec 2.2. The code renders an inline detail panel with three action buttons (List documents modal, Search modal, Edit modal) and uses the generic CRUD DELETE without a tab/consequences modal listing document count.
- Bind embedding provider tab with a warning Changing the provider invalidates existing chunks (spec 2.2). The embedder is editable on the existing Edit modal but with no consequences warning; in practice the backend leaves embedder mutable while making search_provider_id immutable, so the warning would still apply to embedder swaps.
- Documents list columns text-preview (first 200 chars) and ingested_at (spec 3.1). The table shows Meta keys instead and there is no ingested_at field on the Document model.
- Document detail page with Content, Metadata, Embedding status tabs (spec 3.2). No dedicated detail route exists; KN_NewDocumentModal in edit mode is the entire detail surface and has no status badge.
- Embedding status badge reading a status field (spec 3.2). Document has no status field, and the UI has no placeholder line either.
- Dedicated Search-bench page at #/knowledge/search with collections as a search target (spec 4). Code routes to it but excludes collections from KN_SEARCH_TARGETS; collection-scoped search lives in KN_CollectionSearchModal and the scoped SearchBench mode.
- MMR checkbox and Cross-encoder checkbox in the Search form (spec 4.2). The body Collection.search supports mmr + cer, but the form does not expose them; only a helper line about T0174 is shown.
- Inline 503 banner above the results pane when /search returns subsystem-inactive mid-page (spec 4.5). The code uses a toast instead.
- ingested_at on documents and the document detail page generally (spec 3.1, 3.2).
- Search results header rendering of cosine vs score and the View full link to entity detail (spec 4.3). KN_SearchResult shows the score and chunk_id but has no entity navigation.

### UI Providers - Design

Source: `docs/superpowers/specs/2026-05-16-ui-providers-design.md`

- Right-hand 'References used in' panel on the detail page. Spec 4.2 calls for LLM = scan /v1/agents for llm_provider_id === id; Embedding = scan /v1/collections and the IC config; Cross-Encoder = read the IC config. None of these scans are wired in ProviderDetail / ProviderDetailBody.
- Used-in column on the list page (spec 2). The list table renders ID / Provider / Models only, no computed reference count.
- List uses limit=200, spec 2 calls for limit=50. Minor but documented.
- Free-form JSON editor fallback for vendors not covered by structured config (spec 3 Config row). JsonField exists in the file but is not wired into NewProviderModal; vendor selection is restricted to PROVIDER_KINDS_FIELDS keys so an 'other' / unknown vendor cannot be created via the UI.
- Vendor selection allowing free-form 'other' per app spec section 7 (spec 3). Vendor is a constrained select, not free-form.
- Explicit SecretStr 'Replace' button (spec 4.2). The form prefills secret fields with blank in edit mode (after stripping the '**********' redaction) and treats the password input as the replace surface; there is no distinct 'Replace' affordance gating editability.
- T0380 OpenResponsesConfig.flavor coerced-to-default warning chip (spec 4.5 row 2). PROVIDER_KINDS_FIELDS constrains flavor to the documented enum via a select, so the operator cannot enter an off-enum value, but the spec-called warning chip is not rendered when an existing row carries a non-enum flavor (e.g. seeded via the API). The implementation loop notes this as deferred.
- Cross-validation warning that re-fires when 'vendor select changes but config wasn't edited' (spec 4.5 row 1). The T0379 helper text is always shown; there is no edit-tracking that turns into an inline warning conditional on a vendor change.
- 'Refresh from provider' button under the Models table on the detail page (spec 4.3). The detail page has a Refresh that re-fetches /models (which is still the static row list); there is no detail-page button that hits /_discover_models to refresh from upstream.
- 'Add model' button on the detail page that opens an inline row editor and PUT-replaces (spec 4.3). Add-model editing only exists inside the create / edit modal; the detail page's Models table is read-only.

### UI Sessions - Design

Source: `docs/superpowers/specs/2026-05-16-ui-sessions-design.md`

- Server-side filter and sort wiring on the list endpoint: the spec calls for ?status=created,running, ?workspace_id=, ?agent_id=, ?graph_id= query-string emission from the UI, but the code passes only limit=200 and filters client-side. There is no ?graph_id= filter on the backend at all.
- Cursor-mode pagination toggle: the spec mandates a 'Switch to cursor mode' link when total > 200 with ?cursor= forward-only paging; sessions-list.jsx implements only offset pagination with no cursor branch.
- Date-range filter UI rendered disabled with a v2 tooltip: not present in sessions-list.jsx.
- Skeleton 8 rows on loading: SLLoadingTable does render skeletons but it is bypassed when items.length > 0 across polls; the spec's explicit 8-row skeleton on the initial page is present (SLLoadingTable hardcodes length: 8).
- Persistent stale-cache notice info banner ('This view always reads /v1/sessions/{id} (authoritative)...') is not rendered in session-detail.jsx.
- Graph-bound session warning banner ('Graph executor is unimplemented (NotImplementedError)...') is not rendered at the top of session-detail.jsx; only a small 'executor missing' pill in the References panel signals graph binding.
- Modal-level Graph warning banner ('Graph executor is unimplemented - this session will fail on the first turn.') is not present in NewSessionModal.
- Field-level 422 error rendering keyed on extensions.errors[].loc is not present; NewSessionModal swallows errors via the shared mutation toast.
- Navigation to the new session detail (/sessions/{response.id}) on 201 lives in the caller's onCreate prop rather than the modal itself; behavior depends on call site.
- metadata: {} field is not sent on create; the modal omits it and lets the server default.

### UI Toolsets - Design

Source: `docs/superpowers/specs/2026-05-16-ui-toolsets-design.md`

- Two distinct sub-views (User toolsets list + Built-in cards). The current UI is a single unified table with kind filters, not the spec's separate built-in card grid with collapsible per-card tool lists.
- List exclusion by id prefix `_` (spec section 2.1). The reserved ids are unprefixed (system / workspaces / search / web), so the spec's prefix-based filter would never trigger. Filtering is instead driven by the catalogue's builtin flag.
- Kind=web option in the Create modal as a valid user-created variant (spec section 2.2). The Provider dropdown only offers mcp; web is described as runtime built-in only.
- Conditional rendering of the stdio allowlist warning ONLY when the command's first token is not in mcp_stdio_allowed_commands (spec section 2.2). The UI always renders the warning whenever stdio is selected (allowlist not exposed via API), which is stricter than the spec but uses different copy.
- Allowlist warning rendered as a yellow chip on the toolset detail Config tab (spec section 2.2). The Config tab is a read-only JSON dump with no per-field warning chips; the warning lives only inside the create / edit modal.
- JSON-Schema rendered argument form per tool on the Tools tab (spec section 2.3). The Tools tab shows tool id + description + approval pill only; no schema form. Per-row input_schema is available via ToolDetailModal on the separate ToolsPage instead.
- Test Call button disabled on each tool row of the Tools tab (spec section 2.3). No Test Call button is rendered at all on the toolset detail Tools tab.
- Sessions tab implemented via a single GET /v1/sessions?limit=200 with client-side filter against the bound agent's toolset reference (spec section 2.3). The code issues TWO calls (sessions and agents) and joins them; the spec describes only the sessions call.
- Documented T0711 anomaly block requires the /tools error to be 500 AND carry the /errors/internal slug (spec section 2.4). The UI inspects only status == 500 plus transport == http; the slug is not checked.
- Spec section 3 Built-in cards page with availability pill + collapsible tool list calling /v1/toolsets/{id}/tools per built-in (and expecting 503 Subsystem inactive for search when IC is off). This second sub-view does not exist; built-ins fall back into the unified list and 503 inactive copy is not rendered.

### UI Workers - Design

Source: `docs/superpowers/specs/2026-05-16-ui-workers-design.md`

- Spec 3.1 says the 'In flight' card sums /v1/health worker_pool.in_flight / capacity with a shared cache; the implementation sums per-worker w.in_flight (always 0 since the field is not in WorkerInfo) and never calls /v1/health.
- Spec 3.1 says the 'Scheduler' card reflects /v1/health scheduler.alive; the code hardcodes value='alive', sub='last claim 2s ago', accent='green'.
- Spec 3.2 mandates a status chip + free-text filter applied client-side to the visible list; the chips and search input are rendered as static markup with no onClick / onChange / useState and no filtering function is applied to the workers array.
- Spec 4 recommends option (B): derive per-row in_flight from GET /v1/sessions?worker_id={id}&status=running counts per worker, batched. The code only uses the page-wide sessions prop and never issues a worker-scoped sessions fetch; if sessions is not passed (or empty) every row falls back to in_flight=0.
- Spec 4 calls for a fallback to (A) (per-row in_flight rendered as a dash) when ?worker_id= is unsupported; the row always renders '{inFlight} / {capacity}' with no '-' fallback path.
- Spec 3.4 says polling is paused on hidden + idle; workers.jsx itself only declares pollMs: 2000 and relies on useResource for that contract, which should be called out so it isn't lost in the consolidation.

### UI Workspaces - Design

Source: `docs/superpowers/specs/2026-05-16-ui-workspaces-design.md`

- Provider kind chips on the list page (spec 2.2). The live filter is a flat <select> per provider_id, not chip toggles per kind (local/container/k8s).
- Per-workspace session count on the list page (spec 2.3, 2.4). The list table renders phase + created instead of a sessions count; no per-row /workspaces/{id}/sessions?limit=1 fan-out happens.
- ?create=1 quick-action entry point (spec 2.5). The list page has no useEffect that reads location.search/hash for create=1; only the in-page button opens the modal.
- Env / init_commands overrides editor in the create modal (spec 3.1). The modal submits only {template_id}; there is no field for env or init_commands in WS_NewWorkspaceModal.
- Separate /files/info fetch on file select (spec 5.2). The UI only calls /files/read?encoding=text and uses size_bytes from that response. is_text detection by extension never happens.
- Binary-file 'Preview not available - [Download]' fallback (spec 5.2). For non-utf8 files the backend returns 400 BadRequestError suggesting encoding=base64; the UI displays this as a 'Could not read file' error rather than swapping to a Download-only stub.
- Spec 5.4 tree controls 'New file', 'Refresh', 'Upload' (disabled), right-click 'Rename', 'Download', 'Delete'. Only Refresh is rendered. Download and Delete live in the editor header as buttons instead of a context menu. There is no 'New file' affordance and no Upload button. Rename is not supported.
- pauseWhile editor-dirty pause (spec 5.5). The tree's pollMs=10000 is not paused while editing is active; no draft-dirty -> tree.pauseWhile wiring exists.
- '+ New Session' button in the Sessions tab with defaultWorkspaceId (spec 6). Not rendered.
- Empty-state copy in the Log tab. Live copy says 'No commits yet - the workspace's .state repo is empty until a session writes to it' - close to spec, but no callout that 'workspace .state is initialised lazily on first session activity' verbatim.
- Config tab materialised-template diff view (spec 8). The live tab only renders id/template_id/provider_id/created_at + overrides JSON; no template fetch, no override-injected line bolding.
- Destroy tab 'idempotent DELETE handler tolerates 204 OR 404 as success' (spec 10 anomaly summary). The mutation only special-cases 409; 404 would fall through to the generic toast.
- Use of window.matrixVendor.highlightPython for syntax highlight (spec 5.2). The UI uses an inline regex-based WS_CodeHighlight component instead.

### UI backend additions - deferred endpoints

Source: `docs/superpowers/specs/2026-05-16-ui-backend-deferred-endpoints.md`

- GET /v1/health/recent_errors?since=<iso>&limit=<n> returning {items: [{timestamp, status, type, title, request_id, path, method}], total} with default limit 50, max 200, default since=now-1h, desc by timestamp. Not implemented in primer/api/routers/health.py; no response-log persistence layer exists.
- POST /v1/toolsets/{id}/tools/{tool_id}/invoke accepting {arguments, workspace_id?} and returning {result, duration_ms}, with 422 for schema mismatch, 502 for tool failure (echoing upstream message), and 504 for per-toolset timeout. Not implemented in primer/api/routers/tools.py or primer/api/routers/providers.py; the harness toolset invocation path is in-session only.
- POST /v1/workspaces/{wid}/files?path=<p> with multipart/form-data body for drag-drop binary upload, returning 201 with the same envelope shape as PUT. Not implemented in primer/api/routers/workspaces.py; no UploadFile parameter is wired into files_router.
- GET /v1/sessions/stream and GET /v1/workers/stream over text/event-stream, both emitting a 'snapshot' initial event then session.created/updated/deleted/turn or worker.heartbeat/drained events, with Last-Event-ID resume bounded by a configurable replay window (default 60s). Not implemented; the only live surface is the per-session WebSocket route in primer/api/routers/sessions.py.

## 2026-05-22

### Yielding Tools Design

Source: `docs/superpowers/specs/2026-05-22-yielding-tools-design.md`

- The spec's pre_cancel(meta) hook called synchronously inside the cancel-yielded-tool API before publishing YieldCancelled is not invoked by primer/api/routers/yields.py; the MCP Task adapter is expected to use this for upstream mcp_server.tasks.cancel but the path is not wired.
- The spec describes a sister chat-surface cancel endpoint POST /v1/chats/{chat_id}/yields/{tool_call_id}/cancel; no router file was found that exposes the same canonical path for chats (cancel-yielded-tool on chats may be served only through the inline approval / ask_user UI flow, not via a parallel REST endpoint that matches the spec wording).
- MCP Task push-mode bridging via mcp_task_push_url or notifications/tasks/status is not implemented; McpTaskBridge is poll-only.
- The watch_files spec text calls for an asyncio mtime poller (LocalWorkspaceWatcher polling at 500ms) before migrating to watchdog post-v1; the code instead jumped straight to watchfiles.awatch / inotify with no asyncio mtime fallback path.

## 2026-05-24

### 2026-05-24-sqlite-storage-and-optional-config-design

Source: `docs/superpowers/specs/2026-05-24-sqlite-storage-and-optional-config-design.md`

- Defensive ConfigError at lifespan time when config.db resolves to SQLite AND config.scheduler.provider == POSTGRES. PostgresScheduler simply accesses self._storage.pool, which would raise AttributeError at runtime against a SqliteStorageProvider rather than a clean ConfigError. (searched: primer/scheduler/factory.py, primer/scheduler/postgres.py, primer/api/app.py)
- Spec mentions environment variables as MATRIX_* (e.g. MATRIX_DB__PROVIDER, MATRIX_CONFIG_PATH). The implementation uses prefix PRIMER_ instead (PRIMER_DB__PROVIDER, PRIMER_CONFIG_PATH). Not a missing feature, but a naming divergence the consolidated docs should resolve. (searched: primer/api/config.py, primer/cli.py)
- The example config file config.example.yaml still ships with the pre-spec flat db_host/db_port/db_database/db_user/db_password/db_min_pool_size/db_max_pool_size keys rather than the nested db: provider/config shape the new AppConfig accepts. It would be silently ignored under the current schema (AppConfig has extra='ignore'). (searched: config.example.yaml)

### 2026-05-24-ui-semantic-search-and-yielding-tools-design

Source: `docs/superpowers/specs/2026-05-24-ui-semantic-search-and-yielding-tools-design.md`

- 
- 
- 
- 
- 
- 
- 
- 
- 
- 

### Channels - Discord Adapter Design (Spec 3.3)

Source: `docs/superpowers/specs/2026-05-24-channels-discord-design.md`

- Spec section 9 calls for a specific actionable error string ('message_content intent disabled - enable in Developer Portal') and an explicit gateway-code-4004 mapping on invalid bot tokens; the current connection.py just re-raises whatever discord.py surfaces.
- Spec section 4 / 6.1 describe a probe that surfaces the disabled message_content intent via a gateway disconnect code; nothing in connection.py or adapter.verify() inspects gateway disconnect codes.
- Spec section 6.1's modal-submit fallback (the REJECT_MODAL_CUSTOM_ID_PREFIX branch in on_interaction) is present as defensive code but the modal prefix used is 'primer_reject_modal' rather than the spec's 'matrix_reject_modal'; the spec's exact prefix is not reachable.
- Spec section 7 mentions documenting that abandoned ask_user threads cannot be answered via Discord after a bot restart; no user-facing documentation or log warning surfaces this fact at restart time.

### Channels - Slack Adapter Design (Spec 3.1)

Source: `docs/superpowers/specs/2026-05-24-channels-slack-design.md`

- Acknowledgement post in §6.3 is not implemented: there is no chat.postEphemeral '✓ recorded' reply and no chat.update on the original message after a button click to replace the buttons with a 'Approved by @user at HH:MM:SS' status line. grep for chat.update / chat_postEphemeral inside primer/channel/slack/ returns no matches.
- Probe enrichment in §7 is missing: there is no probe()/'verify' wrapper that returns the bot's name and channel name for a 'Channel ready' UI panel; verify() only raises on failure and returns None.
- Connection-state surfacing in §9 (probe endpoint reporting {ok, connected}) is not exposed; the adapter has no API to inspect socket-mode liveness or auto-reconnect state.
- Rate-limit handling in §9 is not implemented: post_prompt awaits a single chat_postMessage call with no Retry-After honouring retry, no second-429 warning log.
- Cross-tenant silent-drop in §9 is implicit (adapters_by_channel_id lookup returns None and handlers exit) but is not documented or covered by a regression test.
- Acceptance test (4) - inspect SLACK_CONNECTIONS._connections to prove sharing - the underlying attribute exists as ._entries (not _connections) and the live test_slack_smoke.py currently pytest.skip()s, so end-to-end sharing under real traffic is not exercised.
- Acceptance test (6) 'removing the last channel closes the socket' is exercised in test_connection_sharing.py via refcount only; no test asserts no leaked WS background tasks on shutdown.
- Spec calls the event_type literals 'matrix_ask' and 'matrix_approval'; code uses 'primer_ask' and 'primer_approval' (mirrored by REJECT_MODAL_CALLBACK_ID='primer_reject_modal' and block_id='primer_approval'). This is a deliberate rename to match the primer brand.

### Channels - Telegram Adapter Design (Spec 3.2)

Source: `docs/superpowers/specs/2026-05-24-channels-telegram-design.md`

- Cold-start storage-scan fallback: adapter._resolve_tag returns None on cache miss with only a 'TODO: cold-lookup fallback' comment; there is no _cold_lookup helper, no session_storage.find call, and no _extract_tool_call_id helper. The spec section 7 explicitly requires recovery via session_storage.find filtered by parked_status with a tag-rehash loop.
- Process-local tag cache is unbounded: TelegramChannelAdapter._tag_cache is a plain dict, not the bounded LRU (1024 entries) the spec calls for.
- Bounded retry on 429 rate-limit responses is not implemented; send_message has no try/except around retry_after handling, and there is no dispatcher-side warning log on the second failure as the spec section 9 outlines.
- is_polling flag on the connection registry is not exposed; _Entry has refcount/poll_timeout but no liveness flag for the probe endpoint to read.
- probe()-shaped helper returning {ok, bot_username, chat_title} is not present; verify() only raises on failure and the bot username + chat title are not surfaced through any return value.
- Rejection-reason flow is partially wired: factory.py sends a force_reply message and _on_message parses REJECT_TOKEN_RE on the reply, but the spec describes a single dispatcher path; the adapter side has no dedicated 'rejection pending' state and no test exercises the reject-then-reason round trip end to end.
- verify() does not log or attach the bot_username/chat_title for use by an upstream probe endpoint; the values are fetched and discarded other than for the existence checks.

### Channels Core - Design (Spec 3.0)

Source: `docs/superpowers/specs/2026-05-24-channels-core-design.md`

- No /v1/channel_providers/{id}/probe or /v1/channels/{id}/probe endpoints are mounted by primer/api/routers/channels.py; the spec's adapter.verify() hook exists but is not exposed via REST, and the UI consequently has no green/red probe-state checkmark.
- ChannelRegistry has no warm_up() method; adapters are built lazy on first get_adapter call rather than eagerly at lifespan start as the spec's '_make_lifespan' sketch shows. There is also no per-adapter restart-with-exponential-backoff loop (the spec called for 1s/2s/4s/.../60s with the probe endpoint reporting 'healthy'/'restarting'/'down') - nothing in primer/channel/ matches 'backoff' or 'restart'.
- No process-local digest8 -> (workspace_id, session_id, tool_call_id) cache as described in spec section 7 (search for 'digest8' or 'process-local' returns nothing under primer/channel/); the inbound path relies on per-adapter platform-specific decoding plus the existing event_bus key shape rather than a shared digest index.
- No explicit post-resolve hook for channels (the spec called this out as a follow-up; primer/ai_docs/channels.md confirms it is intentionally absent: 'Once a yield resolves, the channel button isn't updated.').

### Semantic Search Subsystem Design

Source: `docs/superpowers/specs/2026-05-24-semantic-search-subsystem-design.md`

- The spec calls for VectorStoreProviderConfig and VectorStoreProviderType to be removed (or repurposed) once the rename lands. Both still live in primer/model/provider.py as internal adapter shapes consumed by SemanticSearchRegistry._default_factory and by VectorStoreProviderFactory. The migration the spec scheduled is incomplete.
- The spec says SemanticSearchRegistry takes the StorageProvider at construction. The shipped constructor takes Storage[SemanticSearchProvider] directly (single-storage handle, not the provider). Same outcome at runtime but a different surface from the spec's prose.
- Phase D in the spec defers the Chat surface UI (M6). No chat-surface SSP picker is in ui/, consistent with the deferral; calling it out so the consolidation doc does not promise it.

### Tool-Call Approval System

Source: `docs/superpowers/specs/2026-05-24-tool-approval-system-design.md`

- The spec defines server-to-client WS frames tool_approval_pending and tool_approval_resolved (one emitted at park time, one on resume, written into chat message history so cursor-based reconnect can replay them). No grep in primer/ finds either kind being emitted; the chat UI polls REST instead, and the live message-history rows do not carry these envelopes.
- The spec lists a 409 (duplicate respond after mark_resumable won the race) row in the error mapping table; the respond handler in primer/api/routers/tool_approval.py only guards on "not parked on _approval" (404) and mismatched tool_call_id (404), and does not call mark_resumable() to atomically claim the response. Double-respond races are not currently detected as 409.

## 2026-05-25

### 2026-05-25-designer-handoff-ui-spec

Source: `docs/superpowers/specs/2026-05-25-designer-handoff-ui-spec.md`

- Shared <DiscriminatedConfigForm fieldsByVariant onVariantChange value onChange /> helper in ui/components/shared.jsx
- Sidebar grouping per spec (single CHANNELS section between PROVIDERS and SUBSYSTEMS holding Providers/Channels/Associations; Approvals under SUBSYSTEMS)
- Workspace-detail mobile tab for Channels

### LanceDB embedded SemanticSearchProvider - design

Source: `docs/superpowers/specs/2026-05-25-lance-ssp-design.md`

- Spec §4.2 specifies a distance: Literal['cosine','l2','dot'] field on LanceConfig that applies to every collection on the SSP. The implemented LanceConfig has no distance field; instead, distance is a per-call argument on create_collection() and is stored on the catalogue row.
- Spec §5.1 names the catalogue table _matrix_collections. The implementation uses _primer_collections.
- Spec §5.2 describes create_table as ANN-index-type 'IVF_HNSW_SQ' with a fallback to plain HNSW via try/except. The implementation instead iterates three lancedb.index configs (HnswSq, HnswPq, IvfPq) and breaks on the first success.
- Spec §5.2 describes search_by_meta as building a SQL filter against the JSON-serialised meta column with json_extract. The implementation falls back to a Python-side linear scan because LanceDB 0.30.2 rejects json_extract on utf8 columns. _meta_predicate is retained but always returns None; _walk_meta is a leftover helper.
- Spec §5.2 calls for create_table to materialise the dataset on create_collection with mode='overwrite_unless_exists' on the catalogue. The implementation uses plain create_table on both the per-collection table and the catalogue (the catalogue is created only when list_tables shows it missing).
- Spec §6.1 keeps a distance knob on the modal and submits it in the lance body. The implementation does not render or submit distance for lance (consistent with LanceConfig dropping the field).

### UI Reconciliation - Design Spec

Source: `docs/superpowers/specs/2026-05-25-ui-reconciliation-design.md`

- Spec 2.1 mentioned a useResourceFiltered helper as the spec-4.4 mitigation for unreliable filter query params. No such helper exists under ui/foundation/. Pages call useResource(path) directly with the query-string approach and would rely on the page's own error handling to recover.
- Spec 2.3's index.html sketch loads babel.min.js from unpkg. The actual index.html drops babel from the browser entirely - the JSX is precompiled server-side by primer.api._jsx_bundle. This is a stronger choice than the spec described but the spec text does not anticipate the new bundle pipeline.
- Spec 2.6 promises 'all 20 component files load' as Phase 1 acceptance. The production tree ships 30+ component files plus a docs/ subtree, so the test_console_loads.py parametrisation is over the real NAV inventory rather than the spec's 20-component list.

## 2026-05-26

### Workspace UI plumbing - design

Source: `docs/superpowers/specs/2026-05-26-workspace-ui-plumbing-design.md`

- Spec's container provider form lists default_image, name_prefix, volume_driver, and pull_policy fields (LocalWorkspaceConfig also has 'path' rather than the implemented 'root_path'). The live model is structured around runtime / connection / reachability / image_pull_secrets and never had default_image or pull_policy; the spec's field list was written against an earlier model snapshot.
- Spec's kubernetes provider form lists in_cluster toggle, kubeconfig_path, storage_class, default_pvc_size, service_account, an Advanced collapse with annotations / labels / node_selector pair-list editors, pod_security_context / container_security_context / tolerations JSON textareas. The live model uses a connection discriminator (in_cluster | kubeconfig | service_account_token) instead of a toggle, has no storage_class / default_pvc_size / service_account / annotations / labels / node_selector / pod_security_context / container_security_context / tolerations on the WorkspaceProvider (most of those live on the template side or do not exist), and the providers modal has no Advanced section.
- Spec's container template form lists extra_volume_size (advisory). The live ContainerTemplateConfig has no such field.
- Spec's kubernetes template form lists an Advanced section with extra_volume_mounts / extra_volumes / container_overrides / pod_overrides as JSON textareas. The live modal exposes extra_volume_mounts, extra_volumes, pod_overrides, and container_security_context_overrides as JSON-shaped inputs but never wraps them in a collapsible Advanced block, and renames container_overrides to container_security_context_overrides.
- Spec's shared template-fields block lists a 'packages' list-row editor and a 'resources' group (cpu_limit, memory_limit, disk_limit, network_egress per ResourceLimits). The live template modal has no packages editor and no ResourceLimits group; CPU and memory limits live per-backend (cpu_cores, memory_bytes on container; cpu_request/limit, memory_request/limit on kubernetes) instead.
- Spec requires client-side state_path / tmp_path validation that rejects absolute paths and '..' segments. The live form passes the values straight through; the Pydantic validator catches it server-side but the inline error path is not pre-empted.
- Spec calls for the test 'test_workspace_provider_delete_with_template_ref_returns_409' as a dedicated negative case. The live test file covers the cascade-block path via reserved-id rejection tests rather than this exact name.

## 2026-05-27

### 2026-05-27-workspace-session-streaming-design

Source: `docs/superpowers/specs/2026-05-27-workspace-session-streaming-design.md`

- ClaimKind.SESSION still has value 'session' and the adapter class is still SessionClaimAdapter; the spec called for ClaimKind.SESSION value to update to 'workspace_session' and the adapter to be renamed WorkspaceSessionClaimAdapter.
- WorkspaceMessageWriter.append has a comment '# Future: publish to SessionTickRouter here.' but does NOT publish per-record ticks itself. The dispatch layer publishes ticks instead, so writer callers from non-dispatch paths (e.g. the SessionClaimAdapter terminal-error write) skip the tick.
- No resumed-record write path was found in primer/session/dispatch.py; the file's translate_stream_event docstring lists RESUMED as worker-synthesised but only YIELDED, CANCELLED, and ERROR are actually appended by dispatch. The TurnLogResumed turn-log entry IS written, but no SessionMessageKind.RESUMED record is appended to messages.jsonl on resume.
- The spec lists a tests/ui_e2e/test_session_live_journey.py file; the actual UI E2E lives at tests/ui_e2e/test_session_lifecycle_journey.py instead (plus test_session_panel_signals.py and test_session_files_toast_lasterr.py).

### Backend Architecture Audit

Source: `docs/superpowers/specs/2026-05-27-backend-architecture-audit.md`

- No `--workers N` flag has been added to the CLI; the audit's recommendation under finding 1.1 to be explicit about SQLite write serialisation is still latent (only single-process api+worker or split api/worker exist).
- ChatTickRouter and SessionTickRouter are still process-local (primer/chat/tick_router.py, primer/session/tick_router.py); the audit's specific concern in 1.4 (live ticks delivered to the old process between reconnects are dropped) is mitigated by cursor replay on the next connect but not eliminated by a distributed tick router.
- The audit's worker-capability model recommendation from `Critique of the Vision` item 8 has no implementation; ClaimAdapter and WorkerPool have no `capabilities: [...]` tag or claim filter.
- No token-bucket rate limiter (Critique-of-Vision item 2). PostgresRateLimiter still uses row-locked leases; the audit suggested `UPDATE ... RETURNING` against a budget row for embedding-burst scale.
- The audit's recommendation to elevate `--workers N + SQLite single-writer` to an explicit note is unaddressed; no documentation or guard rejects multi-writer SQLite.
- InMemoryScheduler with a worker runtime still only warns (primer/api/app.py:459-467); finding 1.2's CRITICAL recommendation to hard-reject silent-corruption configurations is unimplemented.
- Predicate translator duplication (finding 6.2) persists: primer/storage/_predicate.py and primer/storage/_sqlite_predicate.py are independent visitors with ~60% overlap.
- No SQLite index parity with the Postgres GIN index (finding 6.8). Backend-divergent query cost is still possible.
- Pagination defaults have not been unified across routers (finding 6.11).
- Channel push-event protocol still only emits park-time Yielded events (finding 4.7); intermediate stream events are not forwarded to Slack/Telegram/Discord.

### CRUD Factory Level-Up - Design

Source: `docs/superpowers/specs/2026-05-27-crud-factory-levelup-design.md`

- 
- 
- 
- 

### Chat-Turn Detachment Design

Source: `docs/superpowers/specs/2026-05-27-chat-turn-detachment-design.md`

- There is no Scheduler.claim_chats method, no ChatLease dataclass, no _claim_chat_loop / _sweep_chats path on WorkerPool, and no separate chat-claimable bus topic flowing into a chat-specific claim loop - the spec's preamble pre-empted these substitutions by pointing to ClaimEngine. WorkerPool still has _engine_claim_loop + _run_engine_chat instead; for synthesis purposes the spec's standalone APIs simply do not exist.
- Chat does not carry claimed_by / claimed_at / last_heartbeat_at columns. Lease ownership lives on the ClaimEngine leases row, not on Chat. The spec's data-model section called this out explicitly as substituted away; code matches that substitution.
- The 'startup guard that refuses multi-process + SQLite' (spec §Constraints + §Out of scope) was not implemented under primer.api.app.create_app; the constraint is documentation-only today.
- The spec's chat-claimable bus event is still being published by the WS recv loop (primer/api/routers/chats.py line 882) but it is now redundant: the canonical wakeup is claim_engine.upsert(ClaimKind.CHAT, chat_id, priority=10) which publishes the engine's claim_ready NOTIFY. Nothing in the engine path consumes 'chat-claimable' anymore.
- Synthetic 'error' row on sweeper-driven reclaim (spec §Sweeper) is not produced - sweep_chats is a no-op. If a worker dies mid-turn the engine's heartbeat-based lease expiry takes over, but a terminal row marking the interrupted turn is not written, so _find_next_user_message can stall until a different terminal lands. Worth a follow-up note in the chats doc.
- There is no tests/storage/test_chat_claim.py exercising the eligibility SQL across SQLite and Postgres backends; the only adapter-level test is tests/claim/test_chat_adapter.py which asserts that eligibility_sql() contains the right substrings. The deeper Postgres race coverage lives in the ClaimEngine suite under tests/claim/.
- There is no tests/chat/test_drain_loop.py file. Drain-loop coverage moved into tests/chat/test_dispatch.py + test_dispatch_queue_while_compacting.py; WS-resilience coverage moved into tests/api/test_chat_resilience.py.
- The spec's promise that GET /v1/chats/{id} returns turn_status alongside the other fields is satisfied implicitly because the endpoint returns the whole Chat Pydantic model; there is no test in tests/api/test_chats.py pinning the field's presence on the wire.

### Coordinator Subsystem - Distributed-Mode Primitives

Source: `docs/superpowers/specs/2026-05-27-coordinator-design.md`

- The spec's PostgresRateLimiter acquire path waits on LISTEN rate_limit_released:<key> with a 5-second poll fallback. The implementation only polls (asyncio.sleep(0.1) between INSERT attempts) - there is no LISTEN/NOTIFY wait. A code comment in primer/coordinator/postgres.py acknowledges this: 'Future: LISTEN on rate_limit_released and select on event vs sleep'. Release still issues pg_notify but no subscriber is listening for it on the limiter side.
- The spec calls for a RateLimitBackendUnavailable error and a fail-closed try_acquire policy when Postgres is unreachable. No such exception class exists in the repo (grep -r 'RateLimitBackendUnavailable' returns nothing) and try_acquire / _try_insert let asyncpg errors propagate as-is. No fail-open/fail-closed config knob (limits.fail_open) is plumbed either.
- CoordinatorFactory.create() does not take the spec's runtime_mode: RuntimeMode parameter. Selection relies on isinstance(event_bus, InMemoryEventBus) alone. The signature is create(*, storage_provider, event_bus, owner_id).
- The spec describes worker processes generating owner_id = f'wrk-{uuid4().hex[:12]}'. primer/worker/pool.py does construct a self._worker_id with that prefix but does NOT construct or own a Coordinator - the worker pool runs in the same process as the API today (RuntimeMode.API_PLUS_WORKER) and reuses the API's coordinator. A standalone worker-process lifespan that builds its own Coordinator with a wrk- owner_id is not present.
- Four InvalidationTopic members are declared but never published or subscribed: SEMANTIC_SEARCH_PROVIDER, CHANNEL_PROVIDER, WORKSPACE_PROVIDER, HARNESS. The corresponding routers (semantic_search.py, channels, workspaces, harness) invalidate via their own local mechanisms and do not yet route through the InvalidationBus.
- The spec's PR 7 distributed-mode smoke test ('two uv run primer api processes against one Postgres') is still listed in tests/distributed/ as scenarios run by a synthetic cluster fixture. There is no documented operator-facing runbook for the two-process smoke; the spec's manual checklist (only one runs each background role, PUT invalidates cache on the other, global LLM semaphore honoured) lives only inside the spec file.

### Distributed-Mode Test Harness - Design

Source: `docs/superpowers/specs/2026-05-27-distributed-test-harness-design.md`

- The spec specified the env-var prefix as MATRIX_ (MATRIX_DB_URL, MATRIX_RUNTIME_MODE, MATRIX_API_PORT, MATRIX_OWNER_ID_PREFIX, MATRIX_DB_SCHEMA, MATRIX_ENABLE_TEST_ENDPOINTS). The implementation uses PRIMER_ consistently (PRIMER_RUNTIME_MODE, PRIMER_PORT, PRIMER_DB_SCHEMA, PRIMER_ENABLE_TEST_ENDPOINTS, PRIMER_AUTO_BOOTSTRAP). Any consolidated doc reproducing the spec's env-var names verbatim will be wrong.
- The spec also did not anticipate the pydantic-settings nested delimiter ('__'); the cluster does not pass a single MATRIX_DB_URL string but instead expands the URL into PRIMER_DB__CONFIG__HOSTNAME/PORT/USERNAME/PASSWORD/DATABASE plus a parallel PRIMER_SCHEDULER__CONFIG__* block. There is no MATRIX_DB_URL-style single-string env override in AppConfig.
- PRIMER_OWNER_ID_PREFIX is set by the cluster (api-<schema>-<i> and worker-<schema>-<i>) but is NOT actually consulted by primer code. primer/api/app.py derives the API owner_id as f'api-{uuid.uuid4().hex[:12]}' without reading the env var. The spec's claim that 'each process picks a distinct owner-id for coordinator primitives' is satisfied by the random UUID, not by the prefix, so the env var is effectively dead code today.
- The spec mentions 'a finalizer that dumps stdout/stderr from each ProcessHandle' on test failure. The implementation only surfaces logs on non-clean exit (rc not in {0, -SIGTERM, -SIGKILL}); a passing scenario whose subprocess exited cleanly leaves no captured-log artefact in the pytest report. There is no '--show-cluster-logs' flag or per-test attach-on-failure hook.

### First-Run Auto-Bootstrap - Design

Source: `docs/superpowers/specs/2026-05-27-auto-bootstrap-design.md`

- The spec describes a _reserved_factories: dict[str, ReservedFactory] map on each ProviderRegistry with reserved-id lookup short-circuiting before the storage query. The implementation explicitly rejects this design: the registry header comment in provider_registry.py says 'factories are NOT consulted at lookup time. After bootstrap the row exists in storage like any other.' Only the per-kind RESERVED_*_IDS frozensets exist; there is no _reserved_factories dispatch.
- The spec proposed BootstrapRunner takes registries (llm_registry, embedder_registry, ssp_registry, cross_encoder_registry, workspace_provider_registry). The implementation takes per-kind Storage handles directly (embedder_storage, ssp_storage, cross_encoder_storage, workspace_provider_storage, workspace_template_storage) plus the top-level StorageProvider for the marker. There is no llm_registry argument since LLMs are intentionally not bootstrapped.
- The spec calls for GET /v1/{kind}/reserved endpoints for UI hint surfacing. No such endpoint exists in primer/api/routers/. The UI does not consume any /reserved discovery endpoint.
- The spec calls for PATCH on reserved ids to be allowed for limited fields (e.g. resource limits) but rejected for identity fields. The actual workspace and workspace-template routers reject PUT outright with 403 reserved_id_protected; the LLM/embedder/cross-encoder routers wire only create+delete guards (no update guard at all). There is no field-level mutable allowlist anywhere.
- The spec lists separate router files embedders.py, semantic_search.py, cross_encoders.py, workspace_providers.py to modify. In the code, llm_providers, embedders, and cross_encoders are all served from a single shared primer/api/routers/providers.py, and workspace providers + templates from primer/api/routers/workspaces.py. The reserved-id protections are scattered across providers.py, semantic_search.py, and workspaces.py rather than per-kind files.

### Generic Claim Machine - Design

Source: `docs/superpowers/specs/2026-05-27-claim-machine-design.md`

- ChatClaimAdapter.on_release does not clear cancel_requested_at, even though the spec called it out explicitly. The chat model still carries that field but the adapter never touches it.
- Lease.next_attempt_at is not returned from PostgresClaimEngine.claim_due; the spec's claim-latency metric is observed as 0.0 for Postgres-claimed leases (the in-memory path computes the real value). Acknowledged inline as 'we observe 0 as the latency placeholder' but it leaves the metric blind for the production backend.

### Harness - Distributable Bundles for Primer

Source: `docs/superpowers/specs/2026-05-27-harness-design.md`

- Spec 12.1 describes a new 'Distributions' nav group above 'Subsystems' in the sidebar. The actual ui/components/chrome.jsx NAV puts a single 'harnesses' item directly into one of the existing groups; there is no 'Distributions' grouping.
- Spec 8.5 describes 'harness:{id}:tick' events published during long operations. primer/harness/dispatch.py only publishes 'harness:{id}:done' on completion; there are no 'tick' status-change events. (The spec already notes the UI polls in v1 instead of subscribing.)
- Spec 8.1 describes a HarnessLease BaseModel and claim_harnesses / heartbeat_harness / release_harness methods on the Scheduler ABC. None of these exist as concrete code: the spec's own preamble redirects these to the unified ClaimEngine (cluster-2). HarnessClaimAdapter is the concrete substitute. The consolidated doc should describe the engine path directly, not the obsolete Scheduler protocol the spec body still walks through.
- Spec 7 lists WS /v1/harnesses/{id}/ws as 'optional in v1, implemented as polling'. No WebSocket route exists in primer/api/routers/harness.py - confirmed by grep. UI polls GET /v1/harnesses/{id} per spec.
- Spec 8.4 describes HarnessSweeper as an active background task that walks rows with claimed_by != NULL and clears stale claims. The actual sweep_harnesses in primer/harness/dispatch.py is a documented no-op (cluster-2 substitution) - lease TTL + heartbeating are handled by the ClaimEngine. HarnessSweeper still starts under leader election but does nothing.
- Spec 13 'harness__update' tool table row says it accepts {id, name?, description?, ref?, subpath?, git_token?}. The toolset is present (primer/toolset/harness.py) but should be cross-checked against the live tool argument schemas; the spec table is the v1 contract.
- Spec 14 lists error codes including git_auth_failed, git_ref_not_found, git_clone_failed, worker_died. The codes actually emitted by the worker are auth_failed / ref_not_found / clone_failed (no git_ prefix on the HarnessGitError code; the spec body uses the prefixed form). 'worker_died' is not emitted because lease-loss handling moved to the engine.

### Observability - Design

Source: `docs/superpowers/specs/2026-05-27-observability-design.md`

- llm_retry_total counter declared in metrics.py but never incremented anywhere. No adapter (anthropic, gemini, ollama, openresponses, openrouter, openchat) calls llm_retry_total.labels(...).inc(); grep across primer/ returns zero hits outside the registry/reset_for_test definitions.
- claim_active_count gauge declared but never written. Grep finds no claim_active_count.labels(...) call site anywhere in primer/, only the metric definition and the __all__ export.
- ws_replay_backlog_seconds histogram declared but never observed. Neither chats.py nor sessions.py measures the now() - cursor row age at connect time as the spec calls for.
- Postgres claim_enqueue_latency_seconds is always observed as 0.0 because the returned Lease shape lacks next_attempt_at / created_at. The spec specifies wait = (claimed_at - lease.created_at). Marked with an inline comment in postgres.py.
- session WS handler always sets ws.frames_sent=0 on the span; the inline comment notes 'session loop tracks own frames' but the per-frame counter wired in chats.py is not mirrored back to the span attribute.

### Workspace Runtime Protocol - Design

Source: `docs/superpowers/specs/2026-05-27-workspace-runtime-protocol-design.md`

- Server-side health op handler. The spec lists health (returning {version, uptime_s, watches_active, execs_running}) in the op surface and the WSSandbox.inspect / RuntimeClient.ping flow both call it, but runtime/primer_runtime/ops.py::HANDLERS does not register a health handler. The op currently returns EUNSUPPORTED, so inspect falls back to an 'unknown' SandboxInspectInfo and ping fails open.
- Server-side archive op handler. The spec defines archive(paths) -> streamed binary tar frames + archive_done event, and the worker side implements RuntimeClient.archive accordingly, but runtime/primer_runtime/ops.py has no archive handler and runtime/primer_runtime/server.py has no streaming archive branch. Archive calls receive EUNSUPPORTED.
- Binary follow-up frames for bulk payloads. The spec says ops MAY use a binary frame tagged (req_id, sequence, total) for >64 KB payloads. Read_file and write_file always go base64-in-text today; no binary chunking path exists.
- MATRIX_RUNTIME_TOKEN env var name. The spec specifies MATRIX_RUNTIME_TOKEN; the code (runtime, docker adapter, runtime_client docstrings) uses PRIMER_RUNTIME_TOKEN. The Authorization: Bearer flow is otherwise identical.
- Token rotation policy as documented. Spec says tokens are valid for the sandbox lifetime and recreating the workspace is the only rotation path; there is no explicit assertion or doc note enforcing this in code beyond the SecretStr persistence.
- Worker re-establishing watch_start on reconnect. The spec failure-recovery table calls for the worker to re-issue watch_start after a mid-watch reconnect; RuntimeClient._reconnect_loop reconnects and fails pending futures, but does not record active watch subscriptions to resume after reconnect. Streams just close.
- WS code 4400 on protocol minor mismatch with warn-and-degrade. The runtime closes 4400 only on major mismatch; there is no minor-version warn path with feature-set negotiation. PROTOCOL_VERSION is hard-coded to 1.0 on both sides so the gap is latent.
- Sandbox contract test suite reused for WSSandbox + LocalSandbox parity. tests/workspace/test_ws_sandbox.py covers WSSandbox in isolation; there is no parametrised Sandbox-contract test that runs both impls through the same assertions.
- Performance/perf gate test. The spec acceptance criteria call for a yielding-tool wake-latency assertion (<=100 ms p95 vs ~500-1000 ms today); there is no perf test in tests/ that asserts this.
- Failure-injection tests for runtime crash + container restart + auto reconnect within 2 s, and mid-exec disconnect surfacing ETIMEDOUT/EPROTOCOL cleanly. These specific scenarios are not exercised in tests/runtime/ or tests/workspace/.

## 2026-05-29

### Workspace Stack Redesign - Design Spec

Source: `docs/superpowers/specs/2026-05-29-workspace-stack-redesign-design.md`

- 
- 

## 2026-05-30

### 2026-05-30-mobile-console-design

Source: `docs/superpowers/specs/2026-05-30-mobile-console-design.md`

- interactive-widget=resizes-content viewport meta
- JSON textarea full-screen expand affordance
- Use mobile layout link in desktop topbar
- Drawer + sheet focus trap
- Filter bottom-sheet on list pages

### Auto-Compaction at 90% + Per-Provider Token Counting

Source: `docs/superpowers/specs/2026-05-30-auto-compaction-token-counting-design.md`

- (searched: tests/e2e/test_chat_compact_journey.py, tests/ui_e2e/)
- (searched: primer/llm/_tokenizer/anthropic.py, primer/llm/_tokenizer/gemini.py)
- (searched: primer/agent/compaction.py, primer/agent/compaction_mixin.py)

### OpenAI-Compatible Chat Completions LLM Provider Design

Source: `docs/superpowers/specs/2026-05-30-openchat-llm-provider-design.md`

- Spec sketched the request shaping as an ~800-line openchat.py with inlined helpers. The implementation factored the helpers out into primer/llm/_openai_compat.py so OpenRouterLLM can reuse them; openchat.py is ~280 lines and contains only the adapter class plus the policy table. Drift from the spec, intentional.
- Spec promised a parametrised _build_sampling_params lift into primer/llm/_openai_common.py shared with openresponses; this is in place (build_sampling_params with SamplingTarget Literal) and openresponses calls it with target='responses'. _extract_extended_kwargs was NOT factored into _openai_common - it stayed in _openai_compat as a Chat-Completions-only helper since the extended whitelist differs between the two wire formats.
- Spec referenced tests/api/test_providers.py for the provider-row round-trip. The actual home for these tests is tests/model/test_provider_openchat.py (the schema layer rather than the API router layer).

## 2026-05-31

### Agent Graphs - Spec A: Builder Completeness + Structured I/O Contract

Source: `docs/superpowers/specs/2026-05-31-agent-graphs-spec-a-builder-completeness-design.md`

- Spec §2.2 says 'multiple End nodes ready in the same superstep: the lexicographically-smallest End node id wins; the others' outputs are discarded'. The code does the opposite: Spec B §2.4 reversed this so every End fires independently when reached. tests/graph/test_multi_end_independent.py pins the new behaviour. The consolidated docs should document the live (Spec B) rule, not Spec A's.
- Spec §5.5 calls for client-side balance checks on Jinja templates ({{/}} and {%/%} count match) blocking save on mismatch. graphs.jsx does not appear to implement template balance checking - the JSON-textarea parse-on-save rule is wired, but the Jinja balance check is not visible in the search hits.
- Spec §5.4 calls for the executor to emit a single error SessionMessageRecord with payload {code, message, node_id, path?} before terminating. The executor does emit error records for routing_failed (base.py line ~1146), but the spec's 'path' field (jsonschema instance path) on end_output_invalid records was not verified to be populated; flag for synthesizer.
- Spec §6.7 calls for a 'fix me' affordance on each topology-violation banner item that selects/highlights the offending entity on the canvas. The banner separates hard from soft violations and gates Save, but I did not see jump-to-entity wiring in graphs.jsx; treat as a UI gap to confirm.
- Spec §1.4 keeps _CallableRouter unchanged and explicitly out of UI editing scope; graphs.jsx renders callable-router conditional edges only as a default-curve (no UI affordance to edit callable_id). Aligned with the spec - no gap, listed for completeness.

### Agent Graphs - Spec B: Map-reduce + Tool-call Nodes

Source: `docs/superpowers/specs/2026-05-31-agent-graphs-spec-b-mapreduce-toolcall-design.md`

- Spec §3.4 names the endpoint GET /v1/tools; the code ships it at /v1/tools/catalogue to avoid colliding with the pre-existing providers router. The spec's bare path is not mounted. Operators / UI consumers who write to the spec verbatim will 404.

## 2026-06-01

### 2026-06-01-triggers-and-subscriptions-design

Source: `docs/superpowers/specs/2026-06-01-triggers-and-subscriptions-design.md`

- No POST /v1/triggers/preview endpoint that echoes the next N cron occurrences (Spec §13.3 recommended it for correctness). The UI computes the cron preview client-side instead.
- No GET /v1/tools/catalogue surface that auto-lists trigger__* tools as the spec calls out in §10 and §11.3 - no /v1/tools/catalogue router or matching test was found.
- No explicit per-trigger catchup_limit field on ScheduledTriggerConfig; the 64-fire cap from spec §8 is hard-coded as the iter_missed_fires default and in WorkerPool._run_engine_trigger. The spec called for 'configurable per-trigger via catchup_limit - default 64'.
- No lifespan-time recovery of trigger leases per spec §12.7 (re-upsert claim leases for every Trigger row with enabled=true and next_fire_at IS NOT NULL at startup). No 'recover_trigger' / 'trigger.*recover' code path is wired into primer/api/app.py lifespan.
- No fire_id idempotency check in subscriber dispatchers per spec §12.6 ('each sub-dispatcher checks for an existing artefact with the same fire_id before writing'). chat_message, agent_fresh_session, and graph_fresh_session do not query for an existing artefact by fire_id before creating one.
- No primer/trigger/model.py re-export module described in spec §12.1; the Trigger / Subscription models are imported directly from primer.model.trigger throughout.

### Outbound Harnesses

Source: `docs/superpowers/specs/2026-06-01-harness-outbound-design.md`

- 
- 
- 
- 
- 

### Spec A - Subharness Dependencies

Source: `docs/superpowers/specs/2026-06-01-harness-subharness-deps-design.md`

- Spec §7 step 4 describes a sparse fetch of harness.yaml + overrides.schema.json only ('NOT a full clone... shallow clone to a tmp, copy only those two files, discard the clone'). primer.harness.git.fetch_harness_metadata in fact does a full clone_at_ref into a tempdir and reads everything from it before discarding. Functionally equivalent for correctness but bandwidth-heavier than the spec called for.
- Spec §12 describes extending render_bundle with an `overrides_namespace_path: tuple[str, ...]` kwarg so the recursion logic slices parent_overrides per level. The actual primer.harness.template.render_bundle signature (checkout_dir, subpath, overrides, harness_ctx) was NOT extended. Instead the dispatch layer slices overrides via _slice_overrides_along_path BEFORE calling render_bundle. Same net behaviour; different decomposition.
- Spec §12 describes a top-level helper walk_dependencies(parent_yaml, fetcher) -> (list, dict). The actual primer.harness.dependencies.walk_dependencies signature is (*, parent_deps: list[dict], fetcher: Fetcher) - it takes the deps list directly, not the whole yaml document. Caller (dispatch._do_fetch) extracts harness_meta['dependencies'] first.
- Spec §11 lists error code `dependency_fetch_failed` body as `{name, git_url, ref, code: <inner git error code>}`. The actual payload from dispatch._do_fetch is `{code: 'dependency_fetch_failed', message, git_url, ref, inner_code}` - the `name` field is NOT included in the surfaced JSON (the walker has the name but the wrapper logs only git_url/ref/inner_code).
- Spec §8 step 1 says install re-walks the transitive dep tree from stored dependencies_resolved (re-resolves via DFS in case anything moved). The actual _do_install reads harness.dependencies_resolved directly and iterates it without re-resolving via fetch_harness_metadata; the defensive 'no cycle/conflict' re-check the spec called out as defensive is therefore not performed at install time.

## 2026-06-02

### 2026-06-02-mcp-server-endpoint-design

Source: `docs/superpowers/specs/2026-06-02-mcp-server-endpoint-design.md`

- Spec §3 wires build_mcp_server with Server('primer', version=PRIMER_VERSION); the code constructs Server('primer') with no version argument.
- Spec §7 lists HARD_DENY as a frozenset containing 'system__call_tool' and 'web__http-request' that operators cannot override; the implementation has reduced HARD_DENY to an empty frozenset and explicitly removed those entries as paternalistic. The 'hard_denied' reason from is_exposable is also no longer reachable.
- Spec §7 lists 'approval_required' as one of the is_exposable denial reasons and references has_approval_policy(tool) + ApprovalResolver; the implementation's is_exposable predicate does not consult any approval policy and only returns 'yielding_unsupported' / 'needs_session'. The docstring notes the approval gate is deferred to a dispatcher layer that is not present in invoke_exposed today.
- Spec §13 enumerates an externally-visible error code 'tool_not_exposable'; the dispatcher's NotExposed carries a structured reason that surfaces only as METHOD_NOT_FOUND on the wire (the granular reason lives in the audit log).
- Spec §14 calls for tests/mcp/test_translation.py covering Tool -> mcp_types.Tool and ToolCallResult -> CallToolResult mapping; that module does not exist (translation is exercised indirectly by test_server_e2e.py and test_server_handlers.py).
- Spec §11 places the MCP sidebar entry under the existing Integrations or Settings group; the chrome.jsx entry is a top-level sidebar item with id='mcp' and label 'MCP Server' (the page itself is routed at /settings/mcp).

## 2026-06-04

### 2026-06-04-user-documentation-system-design

Source: `docs/superpowers/specs/2026-06-04-user-documentation-system-design.md`

- Dedicated /docs/_meta/authoring-guide and /docs/_lint developer panel routes. The spec calls for a hidden authoring guide and a structured lint output view in dev mode, but no special case for the _meta or _lint section prefixes exists in WSP_DocsArticle beyond the generic section index fallthrough. The lint findings are exposed only via server logs.
- The /v1/user_docs/embeds/manifest endpoint reads from app.state.user_docs_embeds (a hand-maintained list duplicated in primer/api/app.py and ui/components/docs/embeds.jsx). The spec implied the embeds manifest would be derived from the React registry; instead the two lists drift independently.
- Spec calls out a UserDocsService.search(query) method returning ranked (slug, snippet) tuples. The current service exposes list_sections, get_doc, get_ai_doc, all_entries, lint_issues, and set_embeds_manifest, but no search() method. Search is entirely client-side over the manifest payload.
- Auth scoping is not asserted in tests. Spec section 5.2 says docs are not anonymous-accessible; tests/api/test_user_docs_router.py does not exercise an unauthenticated 401 case.

## 2026-06-05

### Developer Documentation Consolidation

Source: `docs/superpowers/specs/2026-06-05-dev-docs-consolidation-design.md`

- Spec section 3 enumerates 24 tracked files under docs/dev/; today only the docs/dev/_work/ scratch dir exists. None of the 23 synthesis-produced docs (README, CONTRIBUTING, 7 architecture docs, 14 subsystem docs) are present, and docs/dev/deferred-from-specs.md is not yet produced. The synthesis phase has not run.
- Spec section 3 mandates a .gitignore exception '!docs/dev/' so the consolidated tree ships in git; current .gitignore carries only 'docs/dev/_work/' and '!AGENTS.md'. The '!docs/dev/' exception is not necessary today because docs/dev/ is not under a gitignored parent, but the spec calls for it explicitly.
- Spec section 5.1 specifies 74 parallel triage agents but the live spec set under docs/superpowers/specs/ has 75 entries (this design spec is the 75th); the triage directory contains 63 cards, so 12 specs (including this one before this run) are still without a card.
- Spec section 9 calls for AGENTS.md to restate the repo-wide standing rules verbatim (conventional commits without Co-Authored-By, no force-push to main, narrowed sweep stays green, no em dashes, restart `uv run primer api` and verify /v1/health after code-changing tasks) plus the bug-reporter workflow (bugs under ~/.primer/bugs/, meta.json.status tracking, fix updates status/fixed_at/commit_sha). The live AGENTS.md focuses on the MCP orientation contract and the _internal_ai_docs collection; it does not carry the standing-rules verbatim block or the bug-reporter workflow described in section 9.
- Spec section 11 requires verifier checks to ship as a tests/docs/ pytest suite enforcing 'no empty template sections' and 'exact heading sequence per template'. tests/docs/test_docs_hygiene.py exists but cannot enforce template-heading conformance until the 23 synthesis-produced docs are present to check against.
- Spec section 5.4 lists six inline-fix verifier checks (cross-references, naming consistency, coverage, placeholders, mermaid syntax, em-dash leakage) plus one constructive output (deferred roll-up). scripts/docs_verifier.py implements the deferred roll-up and delegates the six checks to tests/docs/, which can only run meaningfully once the 23 docs exist.

### Per-Session Turn Log Design

Source: `docs/superpowers/specs/2026-06-05-per-session-turn-log-design.md`

- Spec §6.1 calls for the resumed event to be emitted in the claim adapter at primer/claim/adapters/sessions.py. In code, primer/claim/adapters/sessions.py contains no TurnLog imports; the resumed event is emitted from inside run_one_session_turn in primer/session/dispatch.py when session.parked_at is non-None. Functionally equivalent but the architectural seam differs.
- Spec §9 lists the file at primer/session/turn_log_writer.py. The actual file lives at primer/observability/turn_log_writer.py and tests live under tests/observability/test_turn_log_writer.py (spec called for tests/session/test_turn_log_writer.py).
- Spec §9 calls for a 'New Turn log tab on ui/components/graphs.jsx' for the graph-run detail page with All-nodes / By-node sub-tabs. The code uses a single TurnLogTab inside ui/components/session-detail.jsx (graph runs share the session-detail page); ui/components/graphs.jsx has no turn-log integration. The 'By node' sub-tab is implemented as a scope dropdown inside TurnLogTab.
- Spec §5.1 declares the WorkspaceTurnLogWriter constructor as WorkspaceTurnLogWriter(*, workspace_io: WorkspaceIO, relative_path: str). The shipped constructor is WorkspaceTurnLogWriter(*, append_line: AppendLine, read_existing: ReadExisting | None = None) - the writer no longer references WorkspaceIO directly; the caller (worker pool / workspace_executor) supplies path-bound closures.
- Spec §5.3 names the no-op test writer's directory-of-record as a primer/session/ symbol. NoopTurnLogWriter is exported from primer.observability.turn_log_writer; no shim re-export lives under primer.session.
- Spec §12 notes that 'the storage provider's standard create_storage_if_missing semantics handle the schema bootstrap on next startup' for TurnLogRecord. No explicit registration call or migration is visible in the searched code paths; the table is materialised on first get_storage(TurnLogRecord) call but a dedicated bootstrap site / migration file was not found.

