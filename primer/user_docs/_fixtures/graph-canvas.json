{
  "GET /graphs?limit=200": {
    "kind": "offset",
    "offset": 0,
    "length": 1,
    "total": 1,
    "items": [
      {
        "id": "docs-demo-graph",
        "description": "Demo weekly digest graph",
        "nodes": [
          {
            "kind": "begin",
            "id": "start",
            "description": null,
            "input_schema": null
          },
          {
            "kind": "agent",
            "id": "run",
            "agent_id": "weekly-digest",
            "input_template": "{{ instructions }}",
            "response_format": null,
            "description": null,
            "input_schema": null
          },
          {
            "kind": "end",
            "id": "done",
            "description": null,
            "output_template": "{{ nodes.run.text }}",
            "output_schema": null
          }
        ],
        "edges": [
          {
            "kind": "static",
            "from_node": "start",
            "to_node": "run"
          },
          {
            "kind": "static",
            "from_node": "run",
            "to_node": "done"
          }
        ],
        "max_iterations": null,
        "harness_id": null
      }
    ]
  },
  "GET /graphs/{id}": {
    "id": "docs-demo-graph",
    "description": "Demo weekly digest graph",
    "nodes": [
      {
        "kind": "begin",
        "id": "start",
        "description": null,
        "input_schema": null
      },
      {
        "kind": "agent",
        "id": "run",
        "agent_id": "weekly-digest",
        "input_template": "{{ instructions }}",
        "response_format": null,
        "description": null,
        "input_schema": null
      },
      {
        "kind": "end",
        "id": "done",
        "description": null,
        "output_template": "{{ nodes.run.text }}",
        "output_schema": null
      }
    ],
    "edges": [
      {
        "kind": "static",
        "from_node": "start",
        "to_node": "run"
      },
      {
        "kind": "static",
        "from_node": "run",
        "to_node": "done"
      }
    ],
    "max_iterations": null,
    "harness_id": null
  },
  "GET /agents?limit=200": {
    "kind": "offset",
    "offset": 0,
    "length": 1,
    "total": 1,
    "items": [
      {
        "id": "weekly-digest",
        "description": "Summarises the week",
        "model": {
          "provider_id": "demo-openai",
          "model_name": "gpt-4o"
        },
        "temperature": null,
        "tools": [],
        "system_prompt": [
          "You write concise weekly digests."
        ],
        "compaction_prompt": [],
        "harness_id": null
      }
    ]
  },
  "GET /tools/catalogue": {
    "items": [
      {
        "id": "system__list_llm_providers",
        "description": "List LLMProvider entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_llm_provider",
        "description": "Fetch one LLMProvider by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_llm_provider",
        "description": "Create a new LLMProvider. Body shape is the full LLMProvider schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "AnthropicConfig": {
                  "description": "Connection settings for the Anthropic LLM provider.\n\nTargets the Anthropic API \u2014 single api_key auth. AWS Bedrock and\nGCP Vertex variants warrant their own provider types if needed.\n\n``api_key`` is optional at the schema level so operators can wire a\nproxy that injects auth elsewhere; without one, calls to the real\nAnthropic API surface 401 at call time.",
                  "properties": {
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Anthropic API key. Optional \u2014 leave unset only when the endpoint is fronted by something that supplies auth.",
                      "title": "Api Key"
                    }
                  },
                  "title": "AnthropicConfig",
                  "type": "object"
                },
                "GoogleConfig": {
                  "description": "Connection settings for the Gemini LLM provider.\n\nTargets the Gemini API (Google AI Studio) \u2014 single api_key auth.\nVertex AI uses a different auth model (GCP application default\ncredentials + project/location) and warrants its own provider type\nif needed.\n\n``api_key`` is optional at the schema level so operators can wire a\nproxy that injects auth elsewhere; without one, calls to the real\nGemini API surface 401 at call time.",
                  "properties": {
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Gemini API key from Google AI Studio. Optional \u2014 leave unset only when the endpoint is fronted by something that supplies auth.",
                      "title": "Api Key"
                    }
                  },
                  "title": "GoogleConfig",
                  "type": "object"
                },
                "LLMModel": {
                  "description": "A single LLM model exposed by a provider.",
                  "properties": {
                    "name": {
                      "description": "Provider-side model identifier (e.g. 'gpt-4o-mini').",
                      "minLength": 1,
                      "title": "Name",
                      "type": "string"
                    },
                    "context_length": {
                      "description": "Maximum number of tokens the model accepts in a request.",
                      "exclusiveMinimum": 0,
                      "title": "Context Length",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "name",
                    "context_length"
                  ],
                  "title": "LLMModel",
                  "type": "object"
                },
                "LLMProviderType": {
                  "description": "Supported LLM provider backends.\n\nThe string value is what gets serialized in configuration files, so it\nmust remain stable across releases.",
                  "enum": [
                    "openresponses",
                    "openchat",
                    "gemini",
                    "anthropic",
                    "ollama",
                    "openrouter"
                  ],
                  "title": "LLMProviderType",
                  "type": "string"
                },
                "Limits": {
                  "description": "Rate-limit settings the client must respect for a provider.",
                  "properties": {
                    "max_concurrency": {
                      "description": "Maximum number of in-flight requests allowed at once.",
                      "exclusiveMinimum": 0,
                      "title": "Max Concurrency",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "max_concurrency"
                  ],
                  "title": "Limits",
                  "type": "object"
                },
                "OllamaConfig": {
                  "description": "Connection settings for the Ollama LLM provider.\n\nTargets a local or remote Ollama HTTP server. ``api_key`` is\noptional \u2014 Ollama has no authentication by default but users may\ndeploy it behind a reverse proxy that enforces its own auth.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the Ollama HTTP endpoint (e.g. http://localhost:11434).",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional bearer token forwarded as the Authorization header. Useful when running Ollama behind a reverse proxy that enforces auth.",
                      "title": "Api Key"
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OllamaConfig",
                  "type": "object"
                },
                "OpenChatConfig": {
                  "description": "Connection settings for the ``openchat`` LLM provider.\n\nTargets the legacy OpenAI Chat Completions wire format\n(``/v1/chat/completions``) \u2014 the surface every OpenAI-compatible\nthird party (LM Studio, Ollama-OpenAI shim, vLLM, OpenRouter, \u2026)\nsupports.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the provider's HTTP endpoint.",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional API key forwarded as the Authorization bearer. Leave unset for unauthenticated endpoints (LM Studio, self-hosted vLLM, etc.); the upstream provider will return 401 at call time if it actually requires authentication.",
                      "title": "Api Key"
                    },
                    "flavor": {
                      "$ref": "#/$defs/OpenChatFlavor",
                      "default": "other",
                      "description": "Identifies which OpenAI-compatible Chat Completions server is on the other end so the adapter can apply flavor-specific behavior. Defaults to OTHER (conservative; api_key required)."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OpenChatConfig",
                  "type": "object"
                },
                "OpenChatFlavor": {
                  "description": "Which OpenAI-compatible Chat Completions server is on the wire.\n\nThe legacy ``/v1/chat/completions`` surface is the dominant one for\nnon-OpenAI servers (LM Studio, Ollama's OpenAI shim, vLLM,\nOpenRouter, Together, Fireworks). Distinguishing the flavor lets the\nadapter apply server-specific defaults \u2014 chiefly whether an\n``api_key`` is required at construction time.\n\nUse :attr:`OTHER` for any OpenAI-compatible Chat Completions\nendpoint not explicitly modelled.",
                  "enum": [
                    "openai",
                    "lmstudio",
                    "ollama",
                    "vllm",
                    "other"
                  ],
                  "title": "OpenChatFlavor",
                  "type": "string"
                },
                "OpenResponsesConfig": {
                  "description": "Connection settings for the ``openresponses`` LLM provider.\n\nCarries a :attr:`flavor` discriminator so the adapter can apply\nserver-specific quirk handling without proliferating provider\nenum values for every OpenAI-compatible endpoint variant.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the provider's HTTP endpoint.",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional API key forwarded as the Authorization bearer. Leave unset for unauthenticated endpoints (LM Studio, self-hosted vLLM, etc.); the upstream provider will return 401 at call time if it actually requires authentication.",
                      "title": "Api Key"
                    },
                    "flavor": {
                      "$ref": "#/$defs/OpenResponsesFlavor",
                      "default": "other",
                      "description": "Identifies which OpenAI-compatible server is on the other end so the adapter can apply flavor-specific behavior. Defaults to OTHER (conservative; no quirk handling applied)."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OpenResponsesConfig",
                  "type": "object"
                },
                "OpenResponsesFlavor": {
                  "description": "Which OpenAI-compatible server is on the other end of the wire.\n\nThe wire protocol is the same across these flavors (the official\n``openai`` SDK speaks to all of them) but server-side quirks differ\nenough that the adapter benefits from knowing which one it is talking\nto. Examples of flavor-specific behavior observed in the project's\nresearch probes:\n\n* LM Studio suppresses reasoning events when ``store=True``.\n* LM Studio leaves ``encrypted_content`` absent on reasoning items.\n* Real OpenAI requires a non-empty ``api_key``; LM Studio tolerates one.\n\nUse :attr:`OTHER` for any OpenAI-compatible endpoint that is not\nexplicitly modelled (Ollama, vLLM, llama.cpp, Together, OpenRouter,\netc.) \u2014 the adapter will treat it conservatively and apply no\nflavor-specific optimisations.",
                  "enum": [
                    "openai",
                    "lmstudio",
                    "other"
                  ],
                  "title": "OpenResponsesFlavor",
                  "type": "string"
                },
                "OpenRouterConfig": {
                  "additionalProperties": false,
                  "description": "OpenRouter provider configuration.\n\nOpenRouter is a unified gateway in front of many upstream LLM\nproviders (Anthropic, OpenAI, Google, Mistral, ...) exposing a\ndrop-in OpenAI-compatible /chat/completions endpoint. The base\nURL is fixed (https://openrouter.ai/api/v1); only the API key is\nrequired. The two attribution fields are optional; when set, the\nadapter sends them as HTTP-Referer and X-Title on every request\nso the deploy shows up on OpenRouter's app leaderboard.\n\nDoes NOT inherit from ``_HttpApiKeyConfig`` because OpenRouter's\nshape is different: no ``url`` field (hard-coded), and ``api_key``\nis required (the upstream is always remote and always\nauthenticated).\n\nUses ``extra='forbid'`` so a config dict shaped for a different\nprovider (e.g. one carrying a ``url`` field) is rejected by the\nLLMProvider validator instead of silently coercing into an\nOpenRouter config with the extra field dropped. Sibling LLM\nconfigs do not need this because their ``url``/``flavor`` fields\nalready distinguish them; ``OpenRouterConfig``'s only field that\noverlaps with another arm is ``api_key``, which is present on\nevery config.",
                  "properties": {
                    "api_key": {
                      "description": "OpenRouter API key. Required (the OpenRouter base URL is always remote and always authenticated; a null key is meaningless).",
                      "format": "password",
                      "title": "Api Key",
                      "type": "string",
                      "writeOnly": true
                    },
                    "app_name": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Sent as the `X-Title` HTTP header on every request for OpenRouter app attribution. Optional. When unset the header is omitted; calls still succeed but do not show up on OpenRouter's leaderboard surface.",
                      "title": "App Name"
                    },
                    "app_url": {
                      "anyOf": [
                        {
                          "format": "uri",
                          "maxLength": 2083,
                          "minLength": 1,
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Sent as the `HTTP-Referer` HTTP header for OpenRouter app attribution. Optional. Symmetric to ``app_name``.",
                      "title": "App Url"
                    }
                  },
                  "required": [
                    "api_key"
                  ],
                  "title": "OpenRouterConfig",
                  "type": "object"
                }
              },
              "description": "A configured LLM provider entry.\n\nOne ``LLMProvider`` describes a single chat/completion backend the\napplication can route requests to. The ``id`` (inherited from\n:class:`Identifiable`) is a user-chosen handle; ``provider`` selects the\nbackend implementation; ``config`` carries backend-specific connection\ndetails; ``models`` and ``limits`` constrain what the application is\nallowed to send.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/LLMProviderType",
                  "description": "Which LLM provider backend this entry targets."
                },
                "models": {
                  "description": "Models permitted on this provider; must contain at least one.",
                  "items": {
                    "$ref": "#/$defs/LLMModel"
                  },
                  "minItems": 1,
                  "title": "Models",
                  "type": "array"
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/OpenResponsesConfig"
                    },
                    {
                      "$ref": "#/$defs/OpenChatConfig"
                    },
                    {
                      "$ref": "#/$defs/GoogleConfig"
                    },
                    {
                      "$ref": "#/$defs/AnthropicConfig"
                    },
                    {
                      "$ref": "#/$defs/OllamaConfig"
                    },
                    {
                      "$ref": "#/$defs/OpenRouterConfig"
                    }
                  ],
                  "description": "Backend-specific connection configuration; must match ``provider``.",
                  "title": "Config"
                },
                "limits": {
                  "$ref": "#/$defs/Limits",
                  "description": "Rate-limit settings enforced when calling this provider."
                }
              },
              "required": [
                "id",
                "provider",
                "models",
                "config",
                "limits"
              ],
              "title": "LLMProvider",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_llm_provider",
        "description": "Replace an existing LLMProvider. Pass ``id`` (the row to update) and ``entity`` (the full new LLMProvider body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "AnthropicConfig": {
                  "description": "Connection settings for the Anthropic LLM provider.\n\nTargets the Anthropic API \u2014 single api_key auth. AWS Bedrock and\nGCP Vertex variants warrant their own provider types if needed.\n\n``api_key`` is optional at the schema level so operators can wire a\nproxy that injects auth elsewhere; without one, calls to the real\nAnthropic API surface 401 at call time.",
                  "properties": {
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Anthropic API key. Optional \u2014 leave unset only when the endpoint is fronted by something that supplies auth.",
                      "title": "Api Key"
                    }
                  },
                  "title": "AnthropicConfig",
                  "type": "object"
                },
                "GoogleConfig": {
                  "description": "Connection settings for the Gemini LLM provider.\n\nTargets the Gemini API (Google AI Studio) \u2014 single api_key auth.\nVertex AI uses a different auth model (GCP application default\ncredentials + project/location) and warrants its own provider type\nif needed.\n\n``api_key`` is optional at the schema level so operators can wire a\nproxy that injects auth elsewhere; without one, calls to the real\nGemini API surface 401 at call time.",
                  "properties": {
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Gemini API key from Google AI Studio. Optional \u2014 leave unset only when the endpoint is fronted by something that supplies auth.",
                      "title": "Api Key"
                    }
                  },
                  "title": "GoogleConfig",
                  "type": "object"
                },
                "LLMModel": {
                  "description": "A single LLM model exposed by a provider.",
                  "properties": {
                    "name": {
                      "description": "Provider-side model identifier (e.g. 'gpt-4o-mini').",
                      "minLength": 1,
                      "title": "Name",
                      "type": "string"
                    },
                    "context_length": {
                      "description": "Maximum number of tokens the model accepts in a request.",
                      "exclusiveMinimum": 0,
                      "title": "Context Length",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "name",
                    "context_length"
                  ],
                  "title": "LLMModel",
                  "type": "object"
                },
                "LLMProviderType": {
                  "description": "Supported LLM provider backends.\n\nThe string value is what gets serialized in configuration files, so it\nmust remain stable across releases.",
                  "enum": [
                    "openresponses",
                    "openchat",
                    "gemini",
                    "anthropic",
                    "ollama",
                    "openrouter"
                  ],
                  "title": "LLMProviderType",
                  "type": "string"
                },
                "Limits": {
                  "description": "Rate-limit settings the client must respect for a provider.",
                  "properties": {
                    "max_concurrency": {
                      "description": "Maximum number of in-flight requests allowed at once.",
                      "exclusiveMinimum": 0,
                      "title": "Max Concurrency",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "max_concurrency"
                  ],
                  "title": "Limits",
                  "type": "object"
                },
                "OllamaConfig": {
                  "description": "Connection settings for the Ollama LLM provider.\n\nTargets a local or remote Ollama HTTP server. ``api_key`` is\noptional \u2014 Ollama has no authentication by default but users may\ndeploy it behind a reverse proxy that enforces its own auth.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the Ollama HTTP endpoint (e.g. http://localhost:11434).",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional bearer token forwarded as the Authorization header. Useful when running Ollama behind a reverse proxy that enforces auth.",
                      "title": "Api Key"
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OllamaConfig",
                  "type": "object"
                },
                "OpenChatConfig": {
                  "description": "Connection settings for the ``openchat`` LLM provider.\n\nTargets the legacy OpenAI Chat Completions wire format\n(``/v1/chat/completions``) \u2014 the surface every OpenAI-compatible\nthird party (LM Studio, Ollama-OpenAI shim, vLLM, OpenRouter, \u2026)\nsupports.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the provider's HTTP endpoint.",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional API key forwarded as the Authorization bearer. Leave unset for unauthenticated endpoints (LM Studio, self-hosted vLLM, etc.); the upstream provider will return 401 at call time if it actually requires authentication.",
                      "title": "Api Key"
                    },
                    "flavor": {
                      "$ref": "#/$defs/OpenChatFlavor",
                      "default": "other",
                      "description": "Identifies which OpenAI-compatible Chat Completions server is on the other end so the adapter can apply flavor-specific behavior. Defaults to OTHER (conservative; api_key required)."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OpenChatConfig",
                  "type": "object"
                },
                "OpenChatFlavor": {
                  "description": "Which OpenAI-compatible Chat Completions server is on the wire.\n\nThe legacy ``/v1/chat/completions`` surface is the dominant one for\nnon-OpenAI servers (LM Studio, Ollama's OpenAI shim, vLLM,\nOpenRouter, Together, Fireworks). Distinguishing the flavor lets the\nadapter apply server-specific defaults \u2014 chiefly whether an\n``api_key`` is required at construction time.\n\nUse :attr:`OTHER` for any OpenAI-compatible Chat Completions\nendpoint not explicitly modelled.",
                  "enum": [
                    "openai",
                    "lmstudio",
                    "ollama",
                    "vllm",
                    "other"
                  ],
                  "title": "OpenChatFlavor",
                  "type": "string"
                },
                "OpenResponsesConfig": {
                  "description": "Connection settings for the ``openresponses`` LLM provider.\n\nCarries a :attr:`flavor` discriminator so the adapter can apply\nserver-specific quirk handling without proliferating provider\nenum values for every OpenAI-compatible endpoint variant.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the provider's HTTP endpoint.",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional API key forwarded as the Authorization bearer. Leave unset for unauthenticated endpoints (LM Studio, self-hosted vLLM, etc.); the upstream provider will return 401 at call time if it actually requires authentication.",
                      "title": "Api Key"
                    },
                    "flavor": {
                      "$ref": "#/$defs/OpenResponsesFlavor",
                      "default": "other",
                      "description": "Identifies which OpenAI-compatible server is on the other end so the adapter can apply flavor-specific behavior. Defaults to OTHER (conservative; no quirk handling applied)."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OpenResponsesConfig",
                  "type": "object"
                },
                "OpenResponsesFlavor": {
                  "description": "Which OpenAI-compatible server is on the other end of the wire.\n\nThe wire protocol is the same across these flavors (the official\n``openai`` SDK speaks to all of them) but server-side quirks differ\nenough that the adapter benefits from knowing which one it is talking\nto. Examples of flavor-specific behavior observed in the project's\nresearch probes:\n\n* LM Studio suppresses reasoning events when ``store=True``.\n* LM Studio leaves ``encrypted_content`` absent on reasoning items.\n* Real OpenAI requires a non-empty ``api_key``; LM Studio tolerates one.\n\nUse :attr:`OTHER` for any OpenAI-compatible endpoint that is not\nexplicitly modelled (Ollama, vLLM, llama.cpp, Together, OpenRouter,\netc.) \u2014 the adapter will treat it conservatively and apply no\nflavor-specific optimisations.",
                  "enum": [
                    "openai",
                    "lmstudio",
                    "other"
                  ],
                  "title": "OpenResponsesFlavor",
                  "type": "string"
                },
                "OpenRouterConfig": {
                  "additionalProperties": false,
                  "description": "OpenRouter provider configuration.\n\nOpenRouter is a unified gateway in front of many upstream LLM\nproviders (Anthropic, OpenAI, Google, Mistral, ...) exposing a\ndrop-in OpenAI-compatible /chat/completions endpoint. The base\nURL is fixed (https://openrouter.ai/api/v1); only the API key is\nrequired. The two attribution fields are optional; when set, the\nadapter sends them as HTTP-Referer and X-Title on every request\nso the deploy shows up on OpenRouter's app leaderboard.\n\nDoes NOT inherit from ``_HttpApiKeyConfig`` because OpenRouter's\nshape is different: no ``url`` field (hard-coded), and ``api_key``\nis required (the upstream is always remote and always\nauthenticated).\n\nUses ``extra='forbid'`` so a config dict shaped for a different\nprovider (e.g. one carrying a ``url`` field) is rejected by the\nLLMProvider validator instead of silently coercing into an\nOpenRouter config with the extra field dropped. Sibling LLM\nconfigs do not need this because their ``url``/``flavor`` fields\nalready distinguish them; ``OpenRouterConfig``'s only field that\noverlaps with another arm is ``api_key``, which is present on\nevery config.",
                  "properties": {
                    "api_key": {
                      "description": "OpenRouter API key. Required (the OpenRouter base URL is always remote and always authenticated; a null key is meaningless).",
                      "format": "password",
                      "title": "Api Key",
                      "type": "string",
                      "writeOnly": true
                    },
                    "app_name": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Sent as the `X-Title` HTTP header on every request for OpenRouter app attribution. Optional. When unset the header is omitted; calls still succeed but do not show up on OpenRouter's leaderboard surface.",
                      "title": "App Name"
                    },
                    "app_url": {
                      "anyOf": [
                        {
                          "format": "uri",
                          "maxLength": 2083,
                          "minLength": 1,
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Sent as the `HTTP-Referer` HTTP header for OpenRouter app attribution. Optional. Symmetric to ``app_name``.",
                      "title": "App Url"
                    }
                  },
                  "required": [
                    "api_key"
                  ],
                  "title": "OpenRouterConfig",
                  "type": "object"
                }
              },
              "description": "A configured LLM provider entry.\n\nOne ``LLMProvider`` describes a single chat/completion backend the\napplication can route requests to. The ``id`` (inherited from\n:class:`Identifiable`) is a user-chosen handle; ``provider`` selects the\nbackend implementation; ``config`` carries backend-specific connection\ndetails; ``models`` and ``limits`` constrain what the application is\nallowed to send.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/LLMProviderType",
                  "description": "Which LLM provider backend this entry targets."
                },
                "models": {
                  "description": "Models permitted on this provider; must contain at least one.",
                  "items": {
                    "$ref": "#/$defs/LLMModel"
                  },
                  "minItems": 1,
                  "title": "Models",
                  "type": "array"
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/OpenResponsesConfig"
                    },
                    {
                      "$ref": "#/$defs/OpenChatConfig"
                    },
                    {
                      "$ref": "#/$defs/GoogleConfig"
                    },
                    {
                      "$ref": "#/$defs/AnthropicConfig"
                    },
                    {
                      "$ref": "#/$defs/OllamaConfig"
                    },
                    {
                      "$ref": "#/$defs/OpenRouterConfig"
                    }
                  ],
                  "description": "Backend-specific connection configuration; must match ``provider``.",
                  "title": "Config"
                },
                "limits": {
                  "$ref": "#/$defs/Limits",
                  "description": "Rate-limit settings enforced when calling this provider."
                }
              },
              "required": [
                "id",
                "provider",
                "models",
                "config",
                "limits"
              ],
              "title": "LLMProvider",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_llm_provider",
        "description": "Delete a LLMProvider by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_llm_providers",
        "description": "Find LLMProvider entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_embedding_providers",
        "description": "List EmbeddingProvider entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_embedding_provider",
        "description": "Fetch one EmbeddingProvider by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_embedding_provider",
        "description": "Create a new EmbeddingProvider. Body shape is the full EmbeddingProvider schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "EmbeddingModel": {
                  "description": "A single embedding model exposed by a provider.\n\nCarries only the provider-side model identifier. Vector\ndimensionality is intentionally NOT recorded here -- it is learned\nby :class:`primer.ingest.DocumentIngester` at run time from the\nactual length of the first chunk's embedding vector and propagated\nto :meth:`primer.int.VectorStore.create_collection`. Recording the\ndimension twice (registry + actual) was a redundancy that could\nsilently drift; deferring the answer to ingestion time keeps the\nregistry schema-free.",
                  "properties": {
                    "name": {
                      "description": "Provider-side model identifier (e.g. 'text-embedding-3-small').",
                      "minLength": 1,
                      "title": "Name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "name"
                  ],
                  "title": "EmbeddingModel",
                  "type": "object"
                },
                "EmbeddingProviderType": {
                  "description": "Supported embedding provider backends.\n\nThe string value is what gets serialized in configuration files, so it\nmust remain stable across releases.",
                  "enum": [
                    "huggingface",
                    "openai",
                    "gemini"
                  ],
                  "title": "EmbeddingProviderType",
                  "type": "string"
                },
                "GoogleConfig": {
                  "description": "Connection settings for the Gemini LLM provider.\n\nTargets the Gemini API (Google AI Studio) \u2014 single api_key auth.\nVertex AI uses a different auth model (GCP application default\ncredentials + project/location) and warrants its own provider type\nif needed.\n\n``api_key`` is optional at the schema level so operators can wire a\nproxy that injects auth elsewhere; without one, calls to the real\nGemini API surface 401 at call time.",
                  "properties": {
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Gemini API key from Google AI Studio. Optional \u2014 leave unset only when the endpoint is fronted by something that supplies auth.",
                      "title": "Api Key"
                    }
                  },
                  "title": "GoogleConfig",
                  "type": "object"
                },
                "HuggingFaceConfig": {
                  "description": "Connection settings for the HuggingFace embedding provider.",
                  "properties": {
                    "token": {
                      "description": "HuggingFace token used to pull the transformer model.",
                      "format": "password",
                      "title": "Token",
                      "type": "string",
                      "writeOnly": true
                    }
                  },
                  "required": [
                    "token"
                  ],
                  "title": "HuggingFaceConfig",
                  "type": "object"
                },
                "Limits": {
                  "description": "Rate-limit settings the client must respect for a provider.",
                  "properties": {
                    "max_concurrency": {
                      "description": "Maximum number of in-flight requests allowed at once.",
                      "exclusiveMinimum": 0,
                      "title": "Max Concurrency",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "max_concurrency"
                  ],
                  "title": "Limits",
                  "type": "object"
                },
                "OpenAIConfig": {
                  "description": "Connection settings for the OpenAI embedding provider.\n\nCarries a :attr:`flavor` discriminator so the adapter can apply\nserver-specific quirk handling (e.g. tolerate empty ``api_key``\nfor LM Studio) without proliferating enum variants on\n:class:`EmbeddingProviderType` for every OpenAI-compatible endpoint.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the provider's HTTP endpoint.",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional API key forwarded as the Authorization bearer. Leave unset for unauthenticated endpoints (LM Studio, self-hosted vLLM, etc.); the upstream provider will return 401 at call time if it actually requires authentication.",
                      "title": "Api Key"
                    },
                    "flavor": {
                      "$ref": "#/$defs/OpenAIEmbeddingFlavor",
                      "default": "other",
                      "description": "Identifies which OpenAI-compatible server is on the other end so the adapter can apply flavor-specific behavior. Defaults to OTHER (conservative; api_key required)."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OpenAIConfig",
                  "type": "object"
                },
                "OpenAIEmbeddingFlavor": {
                  "description": "Which OpenAI-compatible embedding server is on the other end of the wire.\n\nThe wire protocol is the same across these flavors (the official\n``openai`` SDK speaks to all of them) but server-side expectations\ndiffer enough that the adapter benefits from knowing which one it\nis talking to. Examples of flavor-specific behavior:\n\n* Real OpenAI rejects empty ``api_key`` with 401.\n* LM Studio tolerates empty ``api_key`` (unauthenticated by default)\n  but accepts non-empty keys (e.g. for a reverse proxy that enforces\n  its own auth).\n\nUse :attr:`OTHER` for any OpenAI-compatible embedding endpoint that\nis not explicitly modelled \u2014 the adapter will treat it conservatively\nand require an api_key.",
                  "enum": [
                    "openai",
                    "lmstudio",
                    "other"
                  ],
                  "title": "OpenAIEmbeddingFlavor",
                  "type": "string"
                }
              },
              "description": "A configured embedding provider entry.\n\nOne ``EmbeddingProvider`` describes a single vector-embedding backend the\napplication can route requests to. The ``id`` (inherited from\n:class:`Identifiable`) is a user-chosen handle; the ``config`` field is\na discriminated set of provider-specific configurations; the matching\nvariant must agree with ``provider``.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/EmbeddingProviderType",
                  "description": "Which embedding provider backend this entry targets."
                },
                "models": {
                  "description": "Models permitted on this provider; must contain at least one.",
                  "items": {
                    "$ref": "#/$defs/EmbeddingModel"
                  },
                  "minItems": 1,
                  "title": "Models",
                  "type": "array"
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/OpenAIConfig"
                    },
                    {
                      "$ref": "#/$defs/HuggingFaceConfig"
                    },
                    {
                      "$ref": "#/$defs/GoogleConfig"
                    }
                  ],
                  "description": "Backend-specific connection configuration; must match ``provider``.",
                  "title": "Config"
                },
                "limits": {
                  "$ref": "#/$defs/Limits",
                  "description": "Rate-limit settings enforced when calling this provider."
                }
              },
              "required": [
                "id",
                "provider",
                "models",
                "config",
                "limits"
              ],
              "title": "EmbeddingProvider",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_embedding_provider",
        "description": "Replace an existing EmbeddingProvider. Pass ``id`` (the row to update) and ``entity`` (the full new EmbeddingProvider body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "EmbeddingModel": {
                  "description": "A single embedding model exposed by a provider.\n\nCarries only the provider-side model identifier. Vector\ndimensionality is intentionally NOT recorded here -- it is learned\nby :class:`primer.ingest.DocumentIngester` at run time from the\nactual length of the first chunk's embedding vector and propagated\nto :meth:`primer.int.VectorStore.create_collection`. Recording the\ndimension twice (registry + actual) was a redundancy that could\nsilently drift; deferring the answer to ingestion time keeps the\nregistry schema-free.",
                  "properties": {
                    "name": {
                      "description": "Provider-side model identifier (e.g. 'text-embedding-3-small').",
                      "minLength": 1,
                      "title": "Name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "name"
                  ],
                  "title": "EmbeddingModel",
                  "type": "object"
                },
                "EmbeddingProviderType": {
                  "description": "Supported embedding provider backends.\n\nThe string value is what gets serialized in configuration files, so it\nmust remain stable across releases.",
                  "enum": [
                    "huggingface",
                    "openai",
                    "gemini"
                  ],
                  "title": "EmbeddingProviderType",
                  "type": "string"
                },
                "GoogleConfig": {
                  "description": "Connection settings for the Gemini LLM provider.\n\nTargets the Gemini API (Google AI Studio) \u2014 single api_key auth.\nVertex AI uses a different auth model (GCP application default\ncredentials + project/location) and warrants its own provider type\nif needed.\n\n``api_key`` is optional at the schema level so operators can wire a\nproxy that injects auth elsewhere; without one, calls to the real\nGemini API surface 401 at call time.",
                  "properties": {
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Gemini API key from Google AI Studio. Optional \u2014 leave unset only when the endpoint is fronted by something that supplies auth.",
                      "title": "Api Key"
                    }
                  },
                  "title": "GoogleConfig",
                  "type": "object"
                },
                "HuggingFaceConfig": {
                  "description": "Connection settings for the HuggingFace embedding provider.",
                  "properties": {
                    "token": {
                      "description": "HuggingFace token used to pull the transformer model.",
                      "format": "password",
                      "title": "Token",
                      "type": "string",
                      "writeOnly": true
                    }
                  },
                  "required": [
                    "token"
                  ],
                  "title": "HuggingFaceConfig",
                  "type": "object"
                },
                "Limits": {
                  "description": "Rate-limit settings the client must respect for a provider.",
                  "properties": {
                    "max_concurrency": {
                      "description": "Maximum number of in-flight requests allowed at once.",
                      "exclusiveMinimum": 0,
                      "title": "Max Concurrency",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "max_concurrency"
                  ],
                  "title": "Limits",
                  "type": "object"
                },
                "OpenAIConfig": {
                  "description": "Connection settings for the OpenAI embedding provider.\n\nCarries a :attr:`flavor` discriminator so the adapter can apply\nserver-specific quirk handling (e.g. tolerate empty ``api_key``\nfor LM Studio) without proliferating enum variants on\n:class:`EmbeddingProviderType` for every OpenAI-compatible endpoint.",
                  "properties": {
                    "url": {
                      "description": "Base URL of the provider's HTTP endpoint.",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "api_key": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional API key forwarded as the Authorization bearer. Leave unset for unauthenticated endpoints (LM Studio, self-hosted vLLM, etc.); the upstream provider will return 401 at call time if it actually requires authentication.",
                      "title": "Api Key"
                    },
                    "flavor": {
                      "$ref": "#/$defs/OpenAIEmbeddingFlavor",
                      "default": "other",
                      "description": "Identifies which OpenAI-compatible server is on the other end so the adapter can apply flavor-specific behavior. Defaults to OTHER (conservative; api_key required)."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "OpenAIConfig",
                  "type": "object"
                },
                "OpenAIEmbeddingFlavor": {
                  "description": "Which OpenAI-compatible embedding server is on the other end of the wire.\n\nThe wire protocol is the same across these flavors (the official\n``openai`` SDK speaks to all of them) but server-side expectations\ndiffer enough that the adapter benefits from knowing which one it\nis talking to. Examples of flavor-specific behavior:\n\n* Real OpenAI rejects empty ``api_key`` with 401.\n* LM Studio tolerates empty ``api_key`` (unauthenticated by default)\n  but accepts non-empty keys (e.g. for a reverse proxy that enforces\n  its own auth).\n\nUse :attr:`OTHER` for any OpenAI-compatible embedding endpoint that\nis not explicitly modelled \u2014 the adapter will treat it conservatively\nand require an api_key.",
                  "enum": [
                    "openai",
                    "lmstudio",
                    "other"
                  ],
                  "title": "OpenAIEmbeddingFlavor",
                  "type": "string"
                }
              },
              "description": "A configured embedding provider entry.\n\nOne ``EmbeddingProvider`` describes a single vector-embedding backend the\napplication can route requests to. The ``id`` (inherited from\n:class:`Identifiable`) is a user-chosen handle; the ``config`` field is\na discriminated set of provider-specific configurations; the matching\nvariant must agree with ``provider``.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/EmbeddingProviderType",
                  "description": "Which embedding provider backend this entry targets."
                },
                "models": {
                  "description": "Models permitted on this provider; must contain at least one.",
                  "items": {
                    "$ref": "#/$defs/EmbeddingModel"
                  },
                  "minItems": 1,
                  "title": "Models",
                  "type": "array"
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/OpenAIConfig"
                    },
                    {
                      "$ref": "#/$defs/HuggingFaceConfig"
                    },
                    {
                      "$ref": "#/$defs/GoogleConfig"
                    }
                  ],
                  "description": "Backend-specific connection configuration; must match ``provider``.",
                  "title": "Config"
                },
                "limits": {
                  "$ref": "#/$defs/Limits",
                  "description": "Rate-limit settings enforced when calling this provider."
                }
              },
              "required": [
                "id",
                "provider",
                "models",
                "config",
                "limits"
              ],
              "title": "EmbeddingProvider",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_embedding_provider",
        "description": "Delete a EmbeddingProvider by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_embedding_providers",
        "description": "Find EmbeddingProvider entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_cross_encoder_providers",
        "description": "List CrossEncoderProvider entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_cross_encoder_provider",
        "description": "Fetch one CrossEncoderProvider by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_cross_encoder_provider",
        "description": "Create a new CrossEncoderProvider. Body shape is the full CrossEncoderProvider schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "CrossEncoderModel": {
                  "description": "A single cross-encoder model exposed by a provider.",
                  "properties": {
                    "name": {
                      "description": "Provider-side model identifier (e.g. 'BAAI/bge-reranker-v2-m3', 'cross-encoder/ms-marco-MiniLM-L-6-v2').",
                      "minLength": 1,
                      "title": "Name",
                      "type": "string"
                    },
                    "max_pair_length": {
                      "anyOf": [
                        {
                          "exclusiveMinimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional cap on the (query + document) token length the model will consume per pair. ``None`` defers to the model's own default; set this when truncation matters.",
                      "title": "Max Pair Length"
                    }
                  },
                  "required": [
                    "name"
                  ],
                  "title": "CrossEncoderModel",
                  "type": "object"
                },
                "CrossEncoderProviderType": {
                  "description": "Supported cross-encoder reranker backends.\n\nThe string value is what gets serialized in configuration files, so\nit must remain stable across releases.",
                  "enum": [
                    "huggingface"
                  ],
                  "title": "CrossEncoderProviderType",
                  "type": "string"
                },
                "HuggingFaceCrossEncoderConfig": {
                  "description": "Connection settings for the HuggingFace cross-encoder backend.\n\nLocal sentence-transformers models. Most cross-encoders (BAAI,\ncross-encoder/*, MS-MARCO MiniLM) are public, so the token is\noptional and only needed for gated repos.",
                  "properties": {
                    "token": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional HuggingFace token. Required only if the model lives in a gated repository; public reranker models like ``BAAI/bge-reranker-v2-m3`` and ``cross-encoder/*`` do not need it.",
                      "title": "Token"
                    }
                  },
                  "title": "HuggingFaceCrossEncoderConfig",
                  "type": "object"
                },
                "Limits": {
                  "description": "Rate-limit settings the client must respect for a provider.",
                  "properties": {
                    "max_concurrency": {
                      "description": "Maximum number of in-flight requests allowed at once.",
                      "exclusiveMinimum": 0,
                      "title": "Max Concurrency",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "max_concurrency"
                  ],
                  "title": "Limits",
                  "type": "object"
                }
              },
              "description": "A configured cross-encoder reranker provider entry.\n\nOne ``CrossEncoderProvider`` describes a single reranker backend\nthe application can route requests to. The ``id`` (inherited from\n:class:`Identifiable`) is a user-chosen handle; ``provider``\nselects the backend implementation; ``config`` carries\nbackend-specific connection details; ``models`` and ``limits``\nconstrain what the application is allowed to send.\n\nMirrors :class:`LLMProvider` and :class:`EmbeddingProvider` so the\nprovider registry stays uniform across the three model families.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/CrossEncoderProviderType",
                  "description": "Which cross-encoder backend this entry targets."
                },
                "models": {
                  "description": "Models permitted on this provider; must contain at least one.",
                  "items": {
                    "$ref": "#/$defs/CrossEncoderModel"
                  },
                  "minItems": 1,
                  "title": "Models",
                  "type": "array"
                },
                "config": {
                  "$ref": "#/$defs/HuggingFaceCrossEncoderConfig",
                  "description": "Backend-specific connection configuration; must match ``provider``."
                },
                "limits": {
                  "$ref": "#/$defs/Limits",
                  "description": "Rate-limit settings enforced when calling this provider."
                }
              },
              "required": [
                "id",
                "provider",
                "models",
                "config",
                "limits"
              ],
              "title": "CrossEncoderProvider",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_cross_encoder_provider",
        "description": "Replace an existing CrossEncoderProvider. Pass ``id`` (the row to update) and ``entity`` (the full new CrossEncoderProvider body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "CrossEncoderModel": {
                  "description": "A single cross-encoder model exposed by a provider.",
                  "properties": {
                    "name": {
                      "description": "Provider-side model identifier (e.g. 'BAAI/bge-reranker-v2-m3', 'cross-encoder/ms-marco-MiniLM-L-6-v2').",
                      "minLength": 1,
                      "title": "Name",
                      "type": "string"
                    },
                    "max_pair_length": {
                      "anyOf": [
                        {
                          "exclusiveMinimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional cap on the (query + document) token length the model will consume per pair. ``None`` defers to the model's own default; set this when truncation matters.",
                      "title": "Max Pair Length"
                    }
                  },
                  "required": [
                    "name"
                  ],
                  "title": "CrossEncoderModel",
                  "type": "object"
                },
                "CrossEncoderProviderType": {
                  "description": "Supported cross-encoder reranker backends.\n\nThe string value is what gets serialized in configuration files, so\nit must remain stable across releases.",
                  "enum": [
                    "huggingface"
                  ],
                  "title": "CrossEncoderProviderType",
                  "type": "string"
                },
                "HuggingFaceCrossEncoderConfig": {
                  "description": "Connection settings for the HuggingFace cross-encoder backend.\n\nLocal sentence-transformers models. Most cross-encoders (BAAI,\ncross-encoder/*, MS-MARCO MiniLM) are public, so the token is\noptional and only needed for gated repos.",
                  "properties": {
                    "token": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional HuggingFace token. Required only if the model lives in a gated repository; public reranker models like ``BAAI/bge-reranker-v2-m3`` and ``cross-encoder/*`` do not need it.",
                      "title": "Token"
                    }
                  },
                  "title": "HuggingFaceCrossEncoderConfig",
                  "type": "object"
                },
                "Limits": {
                  "description": "Rate-limit settings the client must respect for a provider.",
                  "properties": {
                    "max_concurrency": {
                      "description": "Maximum number of in-flight requests allowed at once.",
                      "exclusiveMinimum": 0,
                      "title": "Max Concurrency",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "max_concurrency"
                  ],
                  "title": "Limits",
                  "type": "object"
                }
              },
              "description": "A configured cross-encoder reranker provider entry.\n\nOne ``CrossEncoderProvider`` describes a single reranker backend\nthe application can route requests to. The ``id`` (inherited from\n:class:`Identifiable`) is a user-chosen handle; ``provider``\nselects the backend implementation; ``config`` carries\nbackend-specific connection details; ``models`` and ``limits``\nconstrain what the application is allowed to send.\n\nMirrors :class:`LLMProvider` and :class:`EmbeddingProvider` so the\nprovider registry stays uniform across the three model families.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/CrossEncoderProviderType",
                  "description": "Which cross-encoder backend this entry targets."
                },
                "models": {
                  "description": "Models permitted on this provider; must contain at least one.",
                  "items": {
                    "$ref": "#/$defs/CrossEncoderModel"
                  },
                  "minItems": 1,
                  "title": "Models",
                  "type": "array"
                },
                "config": {
                  "$ref": "#/$defs/HuggingFaceCrossEncoderConfig",
                  "description": "Backend-specific connection configuration; must match ``provider``."
                },
                "limits": {
                  "$ref": "#/$defs/Limits",
                  "description": "Rate-limit settings enforced when calling this provider."
                }
              },
              "required": [
                "id",
                "provider",
                "models",
                "config",
                "limits"
              ],
              "title": "CrossEncoderProvider",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_cross_encoder_provider",
        "description": "Delete a CrossEncoderProvider by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_cross_encoder_providers",
        "description": "Find CrossEncoderProvider entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_toolsets",
        "description": "List Toolset entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_toolset",
        "description": "Fetch one Toolset by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_toolset",
        "description": "Create a new Toolset. Body shape is the full Toolset schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "HttpConfig": {
                  "description": "HTTP transport for an MCP server \u2014 connects to a remote endpoint\nspeaking MCP over HTTP (e.g. SSE / streamable HTTP).",
                  "properties": {
                    "url": {
                      "description": "Base URL of the remote MCP server endpoint.",
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "headers": {
                      "additionalProperties": {
                        "type": "string"
                      },
                      "description": "HTTP headers to include on every request to the MCP server (e.g. Authorization).",
                      "title": "Headers",
                      "type": "object"
                    },
                    "oauth": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/OAuthConfig"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "If set, the adapter performs OAuth 2.1 (PKCE) on 401 responses. Any Authorization header in `headers` is overridden by the OAuth flow's bearer token once obtained."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "HttpConfig",
                  "type": "object"
                },
                "McpConfig": {
                  "description": "Connection settings for an MCP toolset provider.\n\nCarries a :attr:`transport` discriminator selecting how the client\ntalks to the MCP server, and a :attr:`config` field holding the\ntransport-specific details. The two MUST agree \u2014 a model validator\nenforces consistency.",
                  "properties": {
                    "transport": {
                      "$ref": "#/$defs/TransportType",
                      "description": "Which transport mechanism the MCP client uses to talk to the server."
                    },
                    "config": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/StdioConfig"
                        },
                        {
                          "$ref": "#/$defs/HttpConfig"
                        }
                      ],
                      "description": "Transport-specific connection details. Must match ``transport``.",
                      "title": "Config"
                    }
                  },
                  "required": [
                    "transport",
                    "config"
                  ],
                  "title": "McpConfig",
                  "type": "object"
                },
                "OAuthClientCredentials": {
                  "description": "Static OAuth client credentials, for servers that don't support DCR.\n\nIf ``client_secret`` is None the client is treated as a public client\n(PKCE only, no Authorization header on the token endpoint). If\n``client_secret`` is set the token endpoint is called with HTTP Basic.",
                  "properties": {
                    "client_id": {
                      "description": "OAuth client identifier.",
                      "minLength": 1,
                      "title": "Client Id",
                      "type": "string"
                    },
                    "client_secret": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "OAuth client secret. None for public PKCE-only clients.",
                      "title": "Client Secret"
                    }
                  },
                  "required": [
                    "client_id"
                  ],
                  "title": "OAuthClientCredentials",
                  "type": "object"
                },
                "OAuthConfig": {
                  "description": "OAuth settings for an HTTP MCP server.\n\nAttached to :class:`HttpConfig` via the optional ``oauth`` field.\nWhen present, :class:`primer.toolset.mcp.McpToolsetProvider`\nperforms Authorization Code + PKCE on a 401 from the MCP endpoint\ninstead of immediately failing.",
                  "properties": {
                    "redirect_uri": {
                      "description": "The OAuth callback URL the application exposes. The auth server will redirect the user here with ?code=...&state=...; the application's handler then calls McpToolsetProvider.complete_oauth(code=..., state=...).",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Redirect Uri",
                      "type": "string"
                    },
                    "scopes": {
                      "description": "OAuth scopes to request. Empty list = whatever the server defaults to.",
                      "items": {
                        "type": "string"
                      },
                      "title": "Scopes",
                      "type": "array"
                    },
                    "resource_uri": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "RFC 8707 resource indicator. Set on token requests when the negotiated spec version is 2025-06-18 or 2025-11-25. If None, defaults to the MCP server's URL (the spec's recommended canonical resource identifier).",
                      "title": "Resource Uri"
                    },
                    "static_client": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/OAuthClientCredentials"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "If set, skip Dynamic Client Registration and use these credentials. Required when the auth server does not support DCR; optional otherwise."
                    },
                    "spec_version": {
                      "anyOf": [
                        {
                          "enum": [
                            "2025-03-26",
                            "2025-06-18",
                            "2025-11-25"
                          ],
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Force a specific MCP authorization spec version. If None, the adapter probes (newest -> oldest) and picks the first version the server supports.",
                      "title": "Spec Version"
                    },
                    "client_name": {
                      "default": "primer",
                      "description": "client_name passed during DCR; appears in consent UIs.",
                      "title": "Client Name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "redirect_uri"
                  ],
                  "title": "OAuthConfig",
                  "type": "object"
                },
                "StdioConfig": {
                  "description": "Stdio transport for an MCP server \u2014 launches a subprocess and\nspeaks the MCP protocol over its stdin/stdout.",
                  "properties": {
                    "command": {
                      "description": "Command (argv) to launch the MCP server subprocess.",
                      "items": {
                        "type": "string"
                      },
                      "minItems": 1,
                      "title": "Command",
                      "type": "array"
                    },
                    "env": {
                      "additionalProperties": {
                        "type": "string"
                      },
                      "description": "Environment variables to set when launching the subprocess.",
                      "title": "Env",
                      "type": "object"
                    }
                  },
                  "required": [
                    "command"
                  ],
                  "title": "StdioConfig",
                  "type": "object"
                },
                "ToolsetProviderType": {
                  "description": "Supported toolset provider backends.\n\nThe string value is what gets serialized in configuration files, so it\nmust remain stable across releases.",
                  "enum": [
                    "internal",
                    "mcp"
                  ],
                  "title": "ToolsetProviderType",
                  "type": "string"
                },
                "TransportType": {
                  "description": "Transport mechanisms supported by the MCP toolset provider.",
                  "enum": [
                    "stdio",
                    "http"
                  ],
                  "title": "TransportType",
                  "type": "string"
                }
              },
              "description": "A configured tool source.\n\nConceptually a *set of tools* the application can offer to LLMs.\nTools themselves are not enumerated here \u2014 they are resolved at\nruntime from the provider:\n\n* ``internal`` \u2014 tools registered in the application's internal\n  registry (looked up by toolset ``id``). No additional config is\n  needed; ``config`` MUST be ``None``.\n* ``mcp`` \u2014 tools queried from an MCP server. ``config`` MUST be an\n  :class:`McpConfig` describing the transport and its connection\n  details.\n\nThe ``id`` (inherited from :class:`Identifiable`) is a user-chosen\nhandle the application uses to refer to this toolset.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/ToolsetProviderType",
                  "description": "Which toolset provider backend this entry targets."
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/McpConfig"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Provider-specific config. Required for 'mcp', must be omitted for 'internal'."
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "provider"
              ],
              "title": "Toolset",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_toolset",
        "description": "Replace an existing Toolset. Pass ``id`` (the row to update) and ``entity`` (the full new Toolset body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "HttpConfig": {
                  "description": "HTTP transport for an MCP server \u2014 connects to a remote endpoint\nspeaking MCP over HTTP (e.g. SSE / streamable HTTP).",
                  "properties": {
                    "url": {
                      "description": "Base URL of the remote MCP server endpoint.",
                      "minLength": 1,
                      "title": "Url",
                      "type": "string"
                    },
                    "headers": {
                      "additionalProperties": {
                        "type": "string"
                      },
                      "description": "HTTP headers to include on every request to the MCP server (e.g. Authorization).",
                      "title": "Headers",
                      "type": "object"
                    },
                    "oauth": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/OAuthConfig"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "If set, the adapter performs OAuth 2.1 (PKCE) on 401 responses. Any Authorization header in `headers` is overridden by the OAuth flow's bearer token once obtained."
                    }
                  },
                  "required": [
                    "url"
                  ],
                  "title": "HttpConfig",
                  "type": "object"
                },
                "McpConfig": {
                  "description": "Connection settings for an MCP toolset provider.\n\nCarries a :attr:`transport` discriminator selecting how the client\ntalks to the MCP server, and a :attr:`config` field holding the\ntransport-specific details. The two MUST agree \u2014 a model validator\nenforces consistency.",
                  "properties": {
                    "transport": {
                      "$ref": "#/$defs/TransportType",
                      "description": "Which transport mechanism the MCP client uses to talk to the server."
                    },
                    "config": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/StdioConfig"
                        },
                        {
                          "$ref": "#/$defs/HttpConfig"
                        }
                      ],
                      "description": "Transport-specific connection details. Must match ``transport``.",
                      "title": "Config"
                    }
                  },
                  "required": [
                    "transport",
                    "config"
                  ],
                  "title": "McpConfig",
                  "type": "object"
                },
                "OAuthClientCredentials": {
                  "description": "Static OAuth client credentials, for servers that don't support DCR.\n\nIf ``client_secret`` is None the client is treated as a public client\n(PKCE only, no Authorization header on the token endpoint). If\n``client_secret`` is set the token endpoint is called with HTTP Basic.",
                  "properties": {
                    "client_id": {
                      "description": "OAuth client identifier.",
                      "minLength": 1,
                      "title": "Client Id",
                      "type": "string"
                    },
                    "client_secret": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "OAuth client secret. None for public PKCE-only clients.",
                      "title": "Client Secret"
                    }
                  },
                  "required": [
                    "client_id"
                  ],
                  "title": "OAuthClientCredentials",
                  "type": "object"
                },
                "OAuthConfig": {
                  "description": "OAuth settings for an HTTP MCP server.\n\nAttached to :class:`HttpConfig` via the optional ``oauth`` field.\nWhen present, :class:`primer.toolset.mcp.McpToolsetProvider`\nperforms Authorization Code + PKCE on a 401 from the MCP endpoint\ninstead of immediately failing.",
                  "properties": {
                    "redirect_uri": {
                      "description": "The OAuth callback URL the application exposes. The auth server will redirect the user here with ?code=...&state=...; the application's handler then calls McpToolsetProvider.complete_oauth(code=..., state=...).",
                      "format": "uri",
                      "maxLength": 2083,
                      "minLength": 1,
                      "title": "Redirect Uri",
                      "type": "string"
                    },
                    "scopes": {
                      "description": "OAuth scopes to request. Empty list = whatever the server defaults to.",
                      "items": {
                        "type": "string"
                      },
                      "title": "Scopes",
                      "type": "array"
                    },
                    "resource_uri": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "RFC 8707 resource indicator. Set on token requests when the negotiated spec version is 2025-06-18 or 2025-11-25. If None, defaults to the MCP server's URL (the spec's recommended canonical resource identifier).",
                      "title": "Resource Uri"
                    },
                    "static_client": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/OAuthClientCredentials"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "If set, skip Dynamic Client Registration and use these credentials. Required when the auth server does not support DCR; optional otherwise."
                    },
                    "spec_version": {
                      "anyOf": [
                        {
                          "enum": [
                            "2025-03-26",
                            "2025-06-18",
                            "2025-11-25"
                          ],
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Force a specific MCP authorization spec version. If None, the adapter probes (newest -> oldest) and picks the first version the server supports.",
                      "title": "Spec Version"
                    },
                    "client_name": {
                      "default": "primer",
                      "description": "client_name passed during DCR; appears in consent UIs.",
                      "title": "Client Name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "redirect_uri"
                  ],
                  "title": "OAuthConfig",
                  "type": "object"
                },
                "StdioConfig": {
                  "description": "Stdio transport for an MCP server \u2014 launches a subprocess and\nspeaks the MCP protocol over its stdin/stdout.",
                  "properties": {
                    "command": {
                      "description": "Command (argv) to launch the MCP server subprocess.",
                      "items": {
                        "type": "string"
                      },
                      "minItems": 1,
                      "title": "Command",
                      "type": "array"
                    },
                    "env": {
                      "additionalProperties": {
                        "type": "string"
                      },
                      "description": "Environment variables to set when launching the subprocess.",
                      "title": "Env",
                      "type": "object"
                    }
                  },
                  "required": [
                    "command"
                  ],
                  "title": "StdioConfig",
                  "type": "object"
                },
                "ToolsetProviderType": {
                  "description": "Supported toolset provider backends.\n\nThe string value is what gets serialized in configuration files, so it\nmust remain stable across releases.",
                  "enum": [
                    "internal",
                    "mcp"
                  ],
                  "title": "ToolsetProviderType",
                  "type": "string"
                },
                "TransportType": {
                  "description": "Transport mechanisms supported by the MCP toolset provider.",
                  "enum": [
                    "stdio",
                    "http"
                  ],
                  "title": "TransportType",
                  "type": "string"
                }
              },
              "description": "A configured tool source.\n\nConceptually a *set of tools* the application can offer to LLMs.\nTools themselves are not enumerated here \u2014 they are resolved at\nruntime from the provider:\n\n* ``internal`` \u2014 tools registered in the application's internal\n  registry (looked up by toolset ``id``). No additional config is\n  needed; ``config`` MUST be ``None``.\n* ``mcp`` \u2014 tools queried from an MCP server. ``config`` MUST be an\n  :class:`McpConfig` describing the transport and its connection\n  details.\n\nThe ``id`` (inherited from :class:`Identifiable`) is a user-chosen\nhandle the application uses to refer to this toolset.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/ToolsetProviderType",
                  "description": "Which toolset provider backend this entry targets."
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/McpConfig"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Provider-specific config. Required for 'mcp', must be omitted for 'internal'."
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "provider"
              ],
              "title": "Toolset",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_toolset",
        "description": "Delete a Toolset by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_toolsets",
        "description": "Find Toolset entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_agents",
        "description": "List Agent entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_agent",
        "description": "Fetch one Agent by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_agent",
        "description": "Create a new Agent. Body shape is the full Agent schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "AgentModel": {
                  "description": "Reference to the LLM the agent talks to.\n\nField name is ``model`` on :class:`Agent` so the type name carries\na tiny collision with the Pydantic-internal sense of \"model\" --\nthe docstring + module context disambiguate.",
                  "properties": {
                    "provider_id": {
                      "description": "Identifier of the configured :class:`primer.model.provider.LLMProvider` this agent uses. Resolved against the application's provider registry at session-start time; not validated at construction.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model_name": {
                      "description": "Provider-side model name (e.g. 'gpt-4o-mini', 'claude-sonnet-4-6', 'gemini-2.5-flash'). Must be one of the models the referenced provider permits, but that constraint is checked by the runtime, not here.",
                      "minLength": 1,
                      "title": "Model Name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model_name"
                  ],
                  "title": "AgentModel",
                  "type": "object"
                }
              },
              "description": "A configured agent definition.\n\nInherits ``id`` and ``description`` from :class:`Describeable`.\nThe ``description`` is intended for human display AND for vector\nindexing once an agents :class:`primer.model.collection.Collection`\nis introduced.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "model": {
                  "$ref": "#/$defs/AgentModel",
                  "description": "Which LLM provider + model the agent talks to."
                },
                "temperature": {
                  "anyOf": [
                    {
                      "minimum": 0.0,
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Sampling temperature. ``None`` defers to the LLM adapter's default. Lower bound is 0.0; no upper bound is enforced because providers disagree (OpenAI / Ollama accept up to ~2.0, Anthropic / Google cap at 1.0). The adapter is responsible for clamping or rejecting values outside its own range.",
                  "title": "Temperature"
                },
                "tools": {
                  "description": "Scoped tool ids the agent has access to, each of the form ``<toolset_id>__<tool_name>`` (e.g. ``system__list_files``). Empty list means the agent has NO tools registered. The runtime derives the set of toolset providers to resolve from the unique prefixes of this list, and exposes exactly the listed tools to the LLM \u2014 never a whole toolset. Workspace tools are NOT listed here; they are composed onto the agent automatically when it attaches to a workspace (see :class:`primer.workspace.session.AgentSession`).",
                  "items": {
                    "type": "string"
                  },
                  "title": "Tools",
                  "type": "array"
                },
                "system_prompt": {
                  "description": "Multi-part system prompt. Segments are joined by the runtime when building the LLM call -- preserving the list shape lets adapters that natively support a multi-segment system parameter (Anthropic) emit it directly, while adapters that take a single string (OpenAI / Ollama) join with ``\\n\\n``. An empty list means the agent has no system prompt.",
                  "items": {
                    "type": "string"
                  },
                  "title": "System Prompt",
                  "type": "array"
                },
                "compaction_prompt": {
                  "description": "Instructions the runtime uses to compact the agent's conversation when it exceeds the configured LLM's context window. Multi-part for the same reason as :attr:`system_prompt` (segments joined by the runtime). Compaction strategy is agent-specific because what to keep and what to drop depends on the agent's purpose -- a researcher might preserve cited sources, a coder might preserve the current file under edit. An empty list means the runtime falls back to its default compaction prompt.",
                  "items": {
                    "type": "string"
                  },
                  "title": "Compaction Prompt",
                  "type": "array"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "description",
                "model"
              ],
              "title": "Agent",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_agent",
        "description": "Replace an existing Agent. Pass ``id`` (the row to update) and ``entity`` (the full new Agent body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "AgentModel": {
                  "description": "Reference to the LLM the agent talks to.\n\nField name is ``model`` on :class:`Agent` so the type name carries\na tiny collision with the Pydantic-internal sense of \"model\" --\nthe docstring + module context disambiguate.",
                  "properties": {
                    "provider_id": {
                      "description": "Identifier of the configured :class:`primer.model.provider.LLMProvider` this agent uses. Resolved against the application's provider registry at session-start time; not validated at construction.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model_name": {
                      "description": "Provider-side model name (e.g. 'gpt-4o-mini', 'claude-sonnet-4-6', 'gemini-2.5-flash'). Must be one of the models the referenced provider permits, but that constraint is checked by the runtime, not here.",
                      "minLength": 1,
                      "title": "Model Name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model_name"
                  ],
                  "title": "AgentModel",
                  "type": "object"
                }
              },
              "description": "A configured agent definition.\n\nInherits ``id`` and ``description`` from :class:`Describeable`.\nThe ``description`` is intended for human display AND for vector\nindexing once an agents :class:`primer.model.collection.Collection`\nis introduced.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "model": {
                  "$ref": "#/$defs/AgentModel",
                  "description": "Which LLM provider + model the agent talks to."
                },
                "temperature": {
                  "anyOf": [
                    {
                      "minimum": 0.0,
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Sampling temperature. ``None`` defers to the LLM adapter's default. Lower bound is 0.0; no upper bound is enforced because providers disagree (OpenAI / Ollama accept up to ~2.0, Anthropic / Google cap at 1.0). The adapter is responsible for clamping or rejecting values outside its own range.",
                  "title": "Temperature"
                },
                "tools": {
                  "description": "Scoped tool ids the agent has access to, each of the form ``<toolset_id>__<tool_name>`` (e.g. ``system__list_files``). Empty list means the agent has NO tools registered. The runtime derives the set of toolset providers to resolve from the unique prefixes of this list, and exposes exactly the listed tools to the LLM \u2014 never a whole toolset. Workspace tools are NOT listed here; they are composed onto the agent automatically when it attaches to a workspace (see :class:`primer.workspace.session.AgentSession`).",
                  "items": {
                    "type": "string"
                  },
                  "title": "Tools",
                  "type": "array"
                },
                "system_prompt": {
                  "description": "Multi-part system prompt. Segments are joined by the runtime when building the LLM call -- preserving the list shape lets adapters that natively support a multi-segment system parameter (Anthropic) emit it directly, while adapters that take a single string (OpenAI / Ollama) join with ``\\n\\n``. An empty list means the agent has no system prompt.",
                  "items": {
                    "type": "string"
                  },
                  "title": "System Prompt",
                  "type": "array"
                },
                "compaction_prompt": {
                  "description": "Instructions the runtime uses to compact the agent's conversation when it exceeds the configured LLM's context window. Multi-part for the same reason as :attr:`system_prompt` (segments joined by the runtime). Compaction strategy is agent-specific because what to keep and what to drop depends on the agent's purpose -- a researcher might preserve cited sources, a coder might preserve the current file under edit. An empty list means the runtime falls back to its default compaction prompt.",
                  "items": {
                    "type": "string"
                  },
                  "title": "Compaction Prompt",
                  "type": "array"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "description",
                "model"
              ],
              "title": "Agent",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_agent",
        "description": "Delete a Agent by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_agents",
        "description": "Find Agent entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_graphs",
        "description": "List Graph entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_graph",
        "description": "Fetch one Graph by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_graph",
        "description": "Create a new Graph. Body shape is the full Graph schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "BranchCondition": {
                  "description": "One predicate inside a JsonPathRouter branch.\n\nResolves ``path`` against the source node's ``NodeOutput.parsed``\nvia dotted-segment + bracket-index walking, then applies ``op``.\n\nMissing-path rule: when the path doesn't resolve, EVERY operator\nreturns False \u2014 including ``ne`` and ``not_in``. Use ``exists`` to\ntest presence.",
                  "properties": {
                    "path": {
                      "description": "Dotted path with bracket indexing (`a.b[2].c`) into NodeOutput.parsed of the conditional edge's source node.",
                      "title": "Path",
                      "type": "string"
                    },
                    "op": {
                      "enum": [
                        "eq",
                        "ne",
                        "gt",
                        "gte",
                        "lt",
                        "lte",
                        "in",
                        "not_in",
                        "exists"
                      ],
                      "title": "Op",
                      "type": "string"
                    },
                    "value": {
                      "anyOf": [
                        {},
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Operand on the right of the comparison. Unused for `exists`. For `in`/`not_in` MUST be a list.",
                      "title": "Value"
                    }
                  },
                  "required": [
                    "path",
                    "op"
                  ],
                  "title": "BranchCondition",
                  "type": "object"
                },
                "FanOutSpec": {
                  "description": "One downstream-target configuration on a `_FanOutNode`.\n\nSpec B \u00a71.1. Three kinds:\n- ``broadcast`` produces N synthesized instances of one target.\n- ``tee`` runs each named target once with the FanOut's input.\n- ``map`` parses a source list and runs one instance per item.",
                  "properties": {
                    "kind": {
                      "enum": [
                        "broadcast",
                        "tee",
                        "map"
                      ],
                      "title": "Kind",
                      "type": "string"
                    },
                    "target_node_id": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "broadcast + map",
                      "title": "Target Node Id"
                    },
                    "target_node_ids": {
                      "anyOf": [
                        {
                          "items": {
                            "type": "string"
                          },
                          "type": "array"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "tee",
                      "title": "Target Node Ids"
                    },
                    "count": {
                      "anyOf": [
                        {
                          "minimum": 1,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "broadcast",
                      "title": "Count"
                    },
                    "source_node_id": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "map",
                      "title": "Source Node Id"
                    },
                    "source_path": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "map (dotted path + bracket indices, like BranchCondition.path)",
                      "title": "Source Path"
                    },
                    "on_failure": {
                      "default": "fail_fast",
                      "enum": [
                        "fail_fast",
                        "drain_then_fail",
                        "collect"
                      ],
                      "title": "On Failure",
                      "type": "string"
                    }
                  },
                  "required": [
                    "kind"
                  ],
                  "title": "FanOutSpec",
                  "type": "object"
                },
                "JsonPathBranch": {
                  "description": "One branch of a JsonPathRouter.\n\nAll ``conditions`` must hold (AND). An empty list matches\neverything \u2014 useful as a catch-all branch placed at the end of the\nbranches list.",
                  "properties": {
                    "conditions": {
                      "items": {
                        "$ref": "#/$defs/BranchCondition"
                      },
                      "title": "Conditions",
                      "type": "array"
                    },
                    "to_node": {
                      "description": "Target node id when this branch matches.",
                      "minLength": 1,
                      "title": "To Node",
                      "type": "string"
                    }
                  },
                  "required": [
                    "to_node"
                  ],
                  "title": "JsonPathBranch",
                  "type": "object"
                },
                "_AgentNodeRef": {
                  "description": "Node that runs a single :class:`primer.model.agent.Agent`.",
                  "properties": {
                    "kind": {
                      "const": "agent",
                      "default": "agent",
                      "description": "Discriminator tag identifying this node as an agent reference.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "description": "Within-graph unique node id (e.g. 'researcher_1').",
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "agent_id": {
                      "description": "Identifier of the stored Agent this node executes.",
                      "minLength": 1,
                      "title": "Agent Id",
                      "type": "string"
                    },
                    "input_template": {
                      "default": "{% for m in initial_input %}{{ m.parts[0].text }}\n{% endfor %}",
                      "description": "Jinja2 template rendered against :class:`GraphContext` to produce the user-role text appended to this node's history before invocation. Default concatenates the graph's initial input verbatim.",
                      "title": "Input Template",
                      "type": "string"
                    },
                    "response_format": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema forwarded to the agent's ``invoke(response_format=...)``. When set, the agent produces structured output and ``NodeOutput.parsed`` is populated. When ``None`` (default), the agent runs unconstrained and downstream nodes receive only the raw text + history.",
                      "title": "Response Format"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    },
                    "input_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Designer metadata. After Jinja renders ``input_template``, if the result parses as JSON, the executor soft-validates against this schema and logs a WARNING on mismatch; never fails the node. Intent is UI assistance for template-building, not a runtime gate.",
                      "title": "Input Schema"
                    }
                  },
                  "required": [
                    "id",
                    "agent_id"
                  ],
                  "title": "_AgentNodeRef",
                  "type": "object"
                },
                "_BeginNode": {
                  "description": "Entry-point node \u2014 pure data-shaping, no LLM call.\n\nCarries the graph's input contract. When ``input_schema`` is set,\nthe session-create handler validates ``graph_input`` against it\nbefore the worker dispatches the graph.",
                  "properties": {
                    "kind": {
                      "const": "begin",
                      "default": "begin",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    },
                    "input_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12 describing the graph's input. When set, the session-create handler validates ``graph_input`` against this schema; failure returns 422. When unset, the graph accepts any input shape (string, list[Message], dict).",
                      "title": "Input Schema"
                    }
                  },
                  "required": [
                    "id"
                  ],
                  "title": "_BeginNode",
                  "type": "object"
                },
                "_CallableRouter": {
                  "description": "Routes via a registered Python callable.\n\n``callable_id`` is resolved at run time against the executor's\n:class:`primer.graph.RouterRegistry`. The callable signature is\n``(context: GraphContext, source: NodeOutput) -> str`` (sync or\nasync); the returned string MUST be the id of an existing node.",
                  "properties": {
                    "kind": {
                      "const": "callable",
                      "default": "callable",
                      "description": "Discriminator tag identifying this router as callable-backed.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "callable_id": {
                      "description": "Lookup key in the executor's RouterRegistry.",
                      "minLength": 1,
                      "title": "Callable Id",
                      "type": "string"
                    }
                  },
                  "required": [
                    "callable_id"
                  ],
                  "title": "_CallableRouter",
                  "type": "object"
                },
                "_ConditionalEdge": {
                  "description": "Router-driven edge: the router decides the destination.",
                  "properties": {
                    "kind": {
                      "const": "conditional",
                      "default": "conditional",
                      "description": "Discriminator tag identifying this edge as conditional.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "from_node": {
                      "minLength": 1,
                      "title": "From Node",
                      "type": "string"
                    },
                    "router": {
                      "description": "Router resolving the next node from GraphContext.",
                      "discriminator": {
                        "mapping": {
                          "callable": "#/$defs/_CallableRouter",
                          "json_path": "#/$defs/_JsonPathRouter"
                        },
                        "propertyName": "kind"
                      },
                      "oneOf": [
                        {
                          "$ref": "#/$defs/_JsonPathRouter"
                        },
                        {
                          "$ref": "#/$defs/_CallableRouter"
                        }
                      ],
                      "title": "Router"
                    }
                  },
                  "required": [
                    "from_node",
                    "router"
                  ],
                  "title": "_ConditionalEdge",
                  "type": "object"
                },
                "_EndNode": {
                  "description": "Sink node carrying the graph's output contract.\n\nPure data-shaping \u2014 when reached, renders ``output_template`` over\nthe current ``GraphContext`` to produce the graph's final output.",
                  "properties": {
                    "kind": {
                      "const": "end",
                      "default": "end",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "title": "Description"
                    },
                    "output_template": {
                      "default": "",
                      "description": "Jinja2 template rendered over the GraphContext when End fires. The rendered string becomes the graph's final output. An empty template terminates the graph without an output payload.",
                      "title": "Output Template",
                      "type": "string"
                    },
                    "output_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12. When set, the rendered output_template MUST parse as JSON conforming to this schema; failure ends the graph with ended_detail='end_output_invalid'.",
                      "title": "Output Schema"
                    }
                  },
                  "required": [
                    "id"
                  ],
                  "title": "_EndNode",
                  "type": "object"
                },
                "_FanInNode": {
                  "description": "Pure data-shaping aggregator that waits for all incoming branches.\n\nSpec B \u00a71.1, \u00a72.2. Unlike every other node kind, FanIn's ready-set\nlogic is wait-for-all: it fires only when every incoming edge's\nsource has produced a NodeOutput. For incoming edges whose source is\na fan-out target, \"all sources produced output\" expands to \"all\nsynthesized instances produced output\".",
                  "properties": {
                    "kind": {
                      "const": "fan_in",
                      "default": "fan_in",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "title": "Description"
                    },
                    "aggregate_template": {
                      "default": "",
                      "description": "Jinja2 template rendered over GraphContext when the FanIn fires. Result populates NodeOutput.text. Has access to the aggregator list at nodes[target] and individual instances at nodes['target[i]'].",
                      "title": "Aggregate Template",
                      "type": "string"
                    },
                    "output_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12. When set, the rendered aggregate_template MUST parse as JSON conforming to this schema; failure ends the graph with ended_detail='end_output_invalid'.",
                      "title": "Output Schema"
                    }
                  },
                  "required": [
                    "id"
                  ],
                  "title": "_FanInNode",
                  "type": "object"
                },
                "_FanOutNode": {
                  "description": "Pure dispatching node \u2014 spawns parallel downstream executions.\n\nSpec B \u00a71.1. Carries a ``specs`` list; each spec produces zero or\nmore synthesized instance ids in ``GraphContext.nodes`` of the form\n``f\"{target}[{i}]\"``. The aggregator entry ``nodes[target]`` is a\n``list[NodeOutput]`` accumulated in index order.",
                  "properties": {
                    "kind": {
                      "const": "fan_out",
                      "default": "fan_out",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    },
                    "specs": {
                      "description": "At least one fan-out spec required. Multiple specs run concurrently \u2014 broadcast/tee/map can be mixed on one FanOut node.",
                      "items": {
                        "$ref": "#/$defs/FanOutSpec"
                      },
                      "minItems": 1,
                      "title": "Specs",
                      "type": "array"
                    }
                  },
                  "required": [
                    "id",
                    "specs"
                  ],
                  "title": "_FanOutNode",
                  "type": "object"
                },
                "_GraphNodeRef": {
                  "description": "Node that delegates to a sub-graph (recursive composition).",
                  "properties": {
                    "kind": {
                      "const": "graph",
                      "default": "graph",
                      "description": "Discriminator tag identifying this node as a sub-graph reference.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "graph_id": {
                      "description": "Identifier of the stored Graph this node delegates to.",
                      "minLength": 1,
                      "title": "Graph Id",
                      "type": "string"
                    },
                    "input_template": {
                      "default": "{% for m in initial_input %}{{ m.parts[0].text }}\n{% endfor %}",
                      "description": "Jinja2 template rendered to produce the sub-graph's ``initial_input`` (a single user-role :class:`Message`).",
                      "title": "Input Template",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    }
                  },
                  "required": [
                    "id",
                    "graph_id"
                  ],
                  "title": "_GraphNodeRef",
                  "type": "object"
                },
                "_JsonPathRouter": {
                  "description": "Routes by JSON-path matching against the source's parsed output.\n\nRequires the source node to have ``response_format`` set so\n:attr:`NodeOutput.parsed` is populated. Branches are evaluated\nin order; first match wins. ``default_to`` fires when no\nbranch matches; if ``None`` and no branch matches the graph\nterminates with ``ended_reason='failed'``.",
                  "properties": {
                    "kind": {
                      "const": "json_path",
                      "default": "json_path",
                      "description": "Discriminator tag identifying this router as JSON-path-based.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "branches": {
                      "description": "Ordered branches; first match wins.",
                      "items": {
                        "$ref": "#/$defs/JsonPathBranch"
                      },
                      "minItems": 1,
                      "title": "Branches",
                      "type": "array"
                    },
                    "default_to": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Fallback target when no branch matches. ``None`` means the graph terminates with ``ended_reason='failed'``.",
                      "title": "Default To"
                    }
                  },
                  "required": [
                    "branches"
                  ],
                  "title": "_JsonPathRouter",
                  "type": "object"
                },
                "_StaticEdge": {
                  "description": "Unconditional edge: always fires from ``from_node`` to ``to_node``.",
                  "properties": {
                    "kind": {
                      "const": "static",
                      "default": "static",
                      "description": "Discriminator tag identifying this edge as static.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "from_node": {
                      "minLength": 1,
                      "title": "From Node",
                      "type": "string"
                    },
                    "to_node": {
                      "minLength": 1,
                      "title": "To Node",
                      "type": "string"
                    }
                  },
                  "required": [
                    "from_node",
                    "to_node"
                  ],
                  "title": "_StaticEdge",
                  "type": "object"
                },
                "_ToolCallNode": {
                  "description": "Direct tool invocation as a graph node.\n\nSpec B \u00a71.1, \u00a72.3. Resolves ``tool_id`` via the workspace session's\n``ToolExecutionManager`` surface at runtime. Honors approval-yielding\ntools by checkpointing the graph executor and yielding the session\nto WAITING (Spec B \u00a74.8) \u2014 implemented in Phase 6 of the plan.",
                  "properties": {
                    "kind": {
                      "const": "tool_call",
                      "default": "tool_call",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "title": "Description"
                    },
                    "tool_id": {
                      "description": "Scoped tool id ('toolset_id__bare_name'). Save-time syntax check only \u2014 existence is checked when the tool manager dispatches at runtime.",
                      "minLength": 1,
                      "title": "Tool Id",
                      "type": "string"
                    },
                    "arguments": {
                      "additionalProperties": true,
                      "description": "Argument object. Any string leaf is rendered as a Jinja template against GraphContext at runtime; non-string leaves pass through. Ignored when arguments_template is set.",
                      "title": "Arguments",
                      "type": "object"
                    },
                    "arguments_template": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional escape hatch: full-JSON Jinja template that shadows ``arguments``. Used when callers need to produce dynamic argument structure (variable list length, etc).",
                      "title": "Arguments Template"
                    },
                    "output_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12. When set, the tool's result.output MUST parse as JSON conforming to this schema; failure ends the graph with ended_detail='tool_output_invalid'.",
                      "title": "Output Schema"
                    }
                  },
                  "required": [
                    "id",
                    "tool_id"
                  ],
                  "title": "_ToolCallNode",
                  "type": "object"
                }
              },
              "description": "A directed graph of agent nodes (and optionally sub-graph nodes).\n\nInherits ``id`` and ``description`` from :class:`Describeable`.\nPersisted via :class:`primer.int.Storage` with model class\n``Graph``.\n\nCyclic graphs MUST set ``max_iterations`` to bound execution;\notherwise a stuck cycle runs unbounded. Acyclic graphs may leave\nit ``None`` (the executor still terminates on terminal nodes /\ndead ends).",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "nodes": {
                  "description": "Discriminated union of agent / subgraph / terminal nodes.",
                  "items": {
                    "discriminator": {
                      "mapping": {
                        "agent": "#/$defs/_AgentNodeRef",
                        "begin": "#/$defs/_BeginNode",
                        "end": "#/$defs/_EndNode",
                        "fan_in": "#/$defs/_FanInNode",
                        "fan_out": "#/$defs/_FanOutNode",
                        "graph": "#/$defs/_GraphNodeRef",
                        "tool_call": "#/$defs/_ToolCallNode"
                      },
                      "propertyName": "kind"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/_AgentNodeRef"
                      },
                      {
                        "$ref": "#/$defs/_GraphNodeRef"
                      },
                      {
                        "$ref": "#/$defs/_BeginNode"
                      },
                      {
                        "$ref": "#/$defs/_EndNode"
                      },
                      {
                        "$ref": "#/$defs/_FanOutNode"
                      },
                      {
                        "$ref": "#/$defs/_FanInNode"
                      },
                      {
                        "$ref": "#/$defs/_ToolCallNode"
                      }
                    ]
                  },
                  "minItems": 1,
                  "title": "Nodes",
                  "type": "array"
                },
                "edges": {
                  "description": "Static or conditional edges connecting nodes.",
                  "items": {
                    "discriminator": {
                      "mapping": {
                        "conditional": "#/$defs/_ConditionalEdge",
                        "static": "#/$defs/_StaticEdge"
                      },
                      "propertyName": "kind"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/_StaticEdge"
                      },
                      {
                        "$ref": "#/$defs/_ConditionalEdge"
                      }
                    ]
                  },
                  "title": "Edges",
                  "type": "array"
                },
                "max_iterations": {
                  "anyOf": [
                    {
                      "exclusiveMinimum": 0,
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Hard cap on supersteps. ``None`` means unbounded. Recommended for any graph that contains a cycle.",
                  "title": "Max Iterations"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "description",
                "nodes"
              ],
              "title": "Graph",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_graph",
        "description": "Replace an existing Graph. Pass ``id`` (the row to update) and ``entity`` (the full new Graph body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "BranchCondition": {
                  "description": "One predicate inside a JsonPathRouter branch.\n\nResolves ``path`` against the source node's ``NodeOutput.parsed``\nvia dotted-segment + bracket-index walking, then applies ``op``.\n\nMissing-path rule: when the path doesn't resolve, EVERY operator\nreturns False \u2014 including ``ne`` and ``not_in``. Use ``exists`` to\ntest presence.",
                  "properties": {
                    "path": {
                      "description": "Dotted path with bracket indexing (`a.b[2].c`) into NodeOutput.parsed of the conditional edge's source node.",
                      "title": "Path",
                      "type": "string"
                    },
                    "op": {
                      "enum": [
                        "eq",
                        "ne",
                        "gt",
                        "gte",
                        "lt",
                        "lte",
                        "in",
                        "not_in",
                        "exists"
                      ],
                      "title": "Op",
                      "type": "string"
                    },
                    "value": {
                      "anyOf": [
                        {},
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Operand on the right of the comparison. Unused for `exists`. For `in`/`not_in` MUST be a list.",
                      "title": "Value"
                    }
                  },
                  "required": [
                    "path",
                    "op"
                  ],
                  "title": "BranchCondition",
                  "type": "object"
                },
                "FanOutSpec": {
                  "description": "One downstream-target configuration on a `_FanOutNode`.\n\nSpec B \u00a71.1. Three kinds:\n- ``broadcast`` produces N synthesized instances of one target.\n- ``tee`` runs each named target once with the FanOut's input.\n- ``map`` parses a source list and runs one instance per item.",
                  "properties": {
                    "kind": {
                      "enum": [
                        "broadcast",
                        "tee",
                        "map"
                      ],
                      "title": "Kind",
                      "type": "string"
                    },
                    "target_node_id": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "broadcast + map",
                      "title": "Target Node Id"
                    },
                    "target_node_ids": {
                      "anyOf": [
                        {
                          "items": {
                            "type": "string"
                          },
                          "type": "array"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "tee",
                      "title": "Target Node Ids"
                    },
                    "count": {
                      "anyOf": [
                        {
                          "minimum": 1,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "broadcast",
                      "title": "Count"
                    },
                    "source_node_id": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "map",
                      "title": "Source Node Id"
                    },
                    "source_path": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "map (dotted path + bracket indices, like BranchCondition.path)",
                      "title": "Source Path"
                    },
                    "on_failure": {
                      "default": "fail_fast",
                      "enum": [
                        "fail_fast",
                        "drain_then_fail",
                        "collect"
                      ],
                      "title": "On Failure",
                      "type": "string"
                    }
                  },
                  "required": [
                    "kind"
                  ],
                  "title": "FanOutSpec",
                  "type": "object"
                },
                "JsonPathBranch": {
                  "description": "One branch of a JsonPathRouter.\n\nAll ``conditions`` must hold (AND). An empty list matches\neverything \u2014 useful as a catch-all branch placed at the end of the\nbranches list.",
                  "properties": {
                    "conditions": {
                      "items": {
                        "$ref": "#/$defs/BranchCondition"
                      },
                      "title": "Conditions",
                      "type": "array"
                    },
                    "to_node": {
                      "description": "Target node id when this branch matches.",
                      "minLength": 1,
                      "title": "To Node",
                      "type": "string"
                    }
                  },
                  "required": [
                    "to_node"
                  ],
                  "title": "JsonPathBranch",
                  "type": "object"
                },
                "_AgentNodeRef": {
                  "description": "Node that runs a single :class:`primer.model.agent.Agent`.",
                  "properties": {
                    "kind": {
                      "const": "agent",
                      "default": "agent",
                      "description": "Discriminator tag identifying this node as an agent reference.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "description": "Within-graph unique node id (e.g. 'researcher_1').",
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "agent_id": {
                      "description": "Identifier of the stored Agent this node executes.",
                      "minLength": 1,
                      "title": "Agent Id",
                      "type": "string"
                    },
                    "input_template": {
                      "default": "{% for m in initial_input %}{{ m.parts[0].text }}\n{% endfor %}",
                      "description": "Jinja2 template rendered against :class:`GraphContext` to produce the user-role text appended to this node's history before invocation. Default concatenates the graph's initial input verbatim.",
                      "title": "Input Template",
                      "type": "string"
                    },
                    "response_format": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema forwarded to the agent's ``invoke(response_format=...)``. When set, the agent produces structured output and ``NodeOutput.parsed`` is populated. When ``None`` (default), the agent runs unconstrained and downstream nodes receive only the raw text + history.",
                      "title": "Response Format"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    },
                    "input_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Designer metadata. After Jinja renders ``input_template``, if the result parses as JSON, the executor soft-validates against this schema and logs a WARNING on mismatch; never fails the node. Intent is UI assistance for template-building, not a runtime gate.",
                      "title": "Input Schema"
                    }
                  },
                  "required": [
                    "id",
                    "agent_id"
                  ],
                  "title": "_AgentNodeRef",
                  "type": "object"
                },
                "_BeginNode": {
                  "description": "Entry-point node \u2014 pure data-shaping, no LLM call.\n\nCarries the graph's input contract. When ``input_schema`` is set,\nthe session-create handler validates ``graph_input`` against it\nbefore the worker dispatches the graph.",
                  "properties": {
                    "kind": {
                      "const": "begin",
                      "default": "begin",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    },
                    "input_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12 describing the graph's input. When set, the session-create handler validates ``graph_input`` against this schema; failure returns 422. When unset, the graph accepts any input shape (string, list[Message], dict).",
                      "title": "Input Schema"
                    }
                  },
                  "required": [
                    "id"
                  ],
                  "title": "_BeginNode",
                  "type": "object"
                },
                "_CallableRouter": {
                  "description": "Routes via a registered Python callable.\n\n``callable_id`` is resolved at run time against the executor's\n:class:`primer.graph.RouterRegistry`. The callable signature is\n``(context: GraphContext, source: NodeOutput) -> str`` (sync or\nasync); the returned string MUST be the id of an existing node.",
                  "properties": {
                    "kind": {
                      "const": "callable",
                      "default": "callable",
                      "description": "Discriminator tag identifying this router as callable-backed.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "callable_id": {
                      "description": "Lookup key in the executor's RouterRegistry.",
                      "minLength": 1,
                      "title": "Callable Id",
                      "type": "string"
                    }
                  },
                  "required": [
                    "callable_id"
                  ],
                  "title": "_CallableRouter",
                  "type": "object"
                },
                "_ConditionalEdge": {
                  "description": "Router-driven edge: the router decides the destination.",
                  "properties": {
                    "kind": {
                      "const": "conditional",
                      "default": "conditional",
                      "description": "Discriminator tag identifying this edge as conditional.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "from_node": {
                      "minLength": 1,
                      "title": "From Node",
                      "type": "string"
                    },
                    "router": {
                      "description": "Router resolving the next node from GraphContext.",
                      "discriminator": {
                        "mapping": {
                          "callable": "#/$defs/_CallableRouter",
                          "json_path": "#/$defs/_JsonPathRouter"
                        },
                        "propertyName": "kind"
                      },
                      "oneOf": [
                        {
                          "$ref": "#/$defs/_JsonPathRouter"
                        },
                        {
                          "$ref": "#/$defs/_CallableRouter"
                        }
                      ],
                      "title": "Router"
                    }
                  },
                  "required": [
                    "from_node",
                    "router"
                  ],
                  "title": "_ConditionalEdge",
                  "type": "object"
                },
                "_EndNode": {
                  "description": "Sink node carrying the graph's output contract.\n\nPure data-shaping \u2014 when reached, renders ``output_template`` over\nthe current ``GraphContext`` to produce the graph's final output.",
                  "properties": {
                    "kind": {
                      "const": "end",
                      "default": "end",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "title": "Description"
                    },
                    "output_template": {
                      "default": "",
                      "description": "Jinja2 template rendered over the GraphContext when End fires. The rendered string becomes the graph's final output. An empty template terminates the graph without an output payload.",
                      "title": "Output Template",
                      "type": "string"
                    },
                    "output_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12. When set, the rendered output_template MUST parse as JSON conforming to this schema; failure ends the graph with ended_detail='end_output_invalid'.",
                      "title": "Output Schema"
                    }
                  },
                  "required": [
                    "id"
                  ],
                  "title": "_EndNode",
                  "type": "object"
                },
                "_FanInNode": {
                  "description": "Pure data-shaping aggregator that waits for all incoming branches.\n\nSpec B \u00a71.1, \u00a72.2. Unlike every other node kind, FanIn's ready-set\nlogic is wait-for-all: it fires only when every incoming edge's\nsource has produced a NodeOutput. For incoming edges whose source is\na fan-out target, \"all sources produced output\" expands to \"all\nsynthesized instances produced output\".",
                  "properties": {
                    "kind": {
                      "const": "fan_in",
                      "default": "fan_in",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "title": "Description"
                    },
                    "aggregate_template": {
                      "default": "",
                      "description": "Jinja2 template rendered over GraphContext when the FanIn fires. Result populates NodeOutput.text. Has access to the aggregator list at nodes[target] and individual instances at nodes['target[i]'].",
                      "title": "Aggregate Template",
                      "type": "string"
                    },
                    "output_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12. When set, the rendered aggregate_template MUST parse as JSON conforming to this schema; failure ends the graph with ended_detail='end_output_invalid'.",
                      "title": "Output Schema"
                    }
                  },
                  "required": [
                    "id"
                  ],
                  "title": "_FanInNode",
                  "type": "object"
                },
                "_FanOutNode": {
                  "description": "Pure dispatching node \u2014 spawns parallel downstream executions.\n\nSpec B \u00a71.1. Carries a ``specs`` list; each spec produces zero or\nmore synthesized instance ids in ``GraphContext.nodes`` of the form\n``f\"{target}[{i}]\"``. The aggregator entry ``nodes[target]`` is a\n``list[NodeOutput]`` accumulated in index order.",
                  "properties": {
                    "kind": {
                      "const": "fan_out",
                      "default": "fan_out",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    },
                    "specs": {
                      "description": "At least one fan-out spec required. Multiple specs run concurrently \u2014 broadcast/tee/map can be mixed on one FanOut node.",
                      "items": {
                        "$ref": "#/$defs/FanOutSpec"
                      },
                      "minItems": 1,
                      "title": "Specs",
                      "type": "array"
                    }
                  },
                  "required": [
                    "id",
                    "specs"
                  ],
                  "title": "_FanOutNode",
                  "type": "object"
                },
                "_GraphNodeRef": {
                  "description": "Node that delegates to a sub-graph (recursive composition).",
                  "properties": {
                    "kind": {
                      "const": "graph",
                      "default": "graph",
                      "description": "Discriminator tag identifying this node as a sub-graph reference.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "graph_id": {
                      "description": "Identifier of the stored Graph this node delegates to.",
                      "minLength": 1,
                      "title": "Graph Id",
                      "type": "string"
                    },
                    "input_template": {
                      "default": "{% for m in initial_input %}{{ m.parts[0].text }}\n{% endfor %}",
                      "description": "Jinja2 template rendered to produce the sub-graph's ``initial_input`` (a single user-role :class:`Message`).",
                      "title": "Input Template",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Free-form human-readable label for the UI.",
                      "title": "Description"
                    }
                  },
                  "required": [
                    "id",
                    "graph_id"
                  ],
                  "title": "_GraphNodeRef",
                  "type": "object"
                },
                "_JsonPathRouter": {
                  "description": "Routes by JSON-path matching against the source's parsed output.\n\nRequires the source node to have ``response_format`` set so\n:attr:`NodeOutput.parsed` is populated. Branches are evaluated\nin order; first match wins. ``default_to`` fires when no\nbranch matches; if ``None`` and no branch matches the graph\nterminates with ``ended_reason='failed'``.",
                  "properties": {
                    "kind": {
                      "const": "json_path",
                      "default": "json_path",
                      "description": "Discriminator tag identifying this router as JSON-path-based.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "branches": {
                      "description": "Ordered branches; first match wins.",
                      "items": {
                        "$ref": "#/$defs/JsonPathBranch"
                      },
                      "minItems": 1,
                      "title": "Branches",
                      "type": "array"
                    },
                    "default_to": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Fallback target when no branch matches. ``None`` means the graph terminates with ``ended_reason='failed'``.",
                      "title": "Default To"
                    }
                  },
                  "required": [
                    "branches"
                  ],
                  "title": "_JsonPathRouter",
                  "type": "object"
                },
                "_StaticEdge": {
                  "description": "Unconditional edge: always fires from ``from_node`` to ``to_node``.",
                  "properties": {
                    "kind": {
                      "const": "static",
                      "default": "static",
                      "description": "Discriminator tag identifying this edge as static.",
                      "title": "Kind",
                      "type": "string"
                    },
                    "from_node": {
                      "minLength": 1,
                      "title": "From Node",
                      "type": "string"
                    },
                    "to_node": {
                      "minLength": 1,
                      "title": "To Node",
                      "type": "string"
                    }
                  },
                  "required": [
                    "from_node",
                    "to_node"
                  ],
                  "title": "_StaticEdge",
                  "type": "object"
                },
                "_ToolCallNode": {
                  "description": "Direct tool invocation as a graph node.\n\nSpec B \u00a71.1, \u00a72.3. Resolves ``tool_id`` via the workspace session's\n``ToolExecutionManager`` surface at runtime. Honors approval-yielding\ntools by checkpointing the graph executor and yielding the session\nto WAITING (Spec B \u00a74.8) \u2014 implemented in Phase 6 of the plan.",
                  "properties": {
                    "kind": {
                      "const": "tool_call",
                      "default": "tool_call",
                      "title": "Kind",
                      "type": "string"
                    },
                    "id": {
                      "minLength": 1,
                      "title": "Id",
                      "type": "string"
                    },
                    "description": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "title": "Description"
                    },
                    "tool_id": {
                      "description": "Scoped tool id ('toolset_id__bare_name'). Save-time syntax check only \u2014 existence is checked when the tool manager dispatches at runtime.",
                      "minLength": 1,
                      "title": "Tool Id",
                      "type": "string"
                    },
                    "arguments": {
                      "additionalProperties": true,
                      "description": "Argument object. Any string leaf is rendered as a Jinja template against GraphContext at runtime; non-string leaves pass through. Ignored when arguments_template is set.",
                      "title": "Arguments",
                      "type": "object"
                    },
                    "arguments_template": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional escape hatch: full-JSON Jinja template that shadows ``arguments``. Used when callers need to produce dynamic argument structure (variable list length, etc).",
                      "title": "Arguments Template"
                    },
                    "output_schema": {
                      "anyOf": [
                        {
                          "additionalProperties": true,
                          "type": "object"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional JSON Schema 2020-12. When set, the tool's result.output MUST parse as JSON conforming to this schema; failure ends the graph with ended_detail='tool_output_invalid'.",
                      "title": "Output Schema"
                    }
                  },
                  "required": [
                    "id",
                    "tool_id"
                  ],
                  "title": "_ToolCallNode",
                  "type": "object"
                }
              },
              "description": "A directed graph of agent nodes (and optionally sub-graph nodes).\n\nInherits ``id`` and ``description`` from :class:`Describeable`.\nPersisted via :class:`primer.int.Storage` with model class\n``Graph``.\n\nCyclic graphs MUST set ``max_iterations`` to bound execution;\notherwise a stuck cycle runs unbounded. Acyclic graphs may leave\nit ``None`` (the executor still terminates on terminal nodes /\ndead ends).",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "nodes": {
                  "description": "Discriminated union of agent / subgraph / terminal nodes.",
                  "items": {
                    "discriminator": {
                      "mapping": {
                        "agent": "#/$defs/_AgentNodeRef",
                        "begin": "#/$defs/_BeginNode",
                        "end": "#/$defs/_EndNode",
                        "fan_in": "#/$defs/_FanInNode",
                        "fan_out": "#/$defs/_FanOutNode",
                        "graph": "#/$defs/_GraphNodeRef",
                        "tool_call": "#/$defs/_ToolCallNode"
                      },
                      "propertyName": "kind"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/_AgentNodeRef"
                      },
                      {
                        "$ref": "#/$defs/_GraphNodeRef"
                      },
                      {
                        "$ref": "#/$defs/_BeginNode"
                      },
                      {
                        "$ref": "#/$defs/_EndNode"
                      },
                      {
                        "$ref": "#/$defs/_FanOutNode"
                      },
                      {
                        "$ref": "#/$defs/_FanInNode"
                      },
                      {
                        "$ref": "#/$defs/_ToolCallNode"
                      }
                    ]
                  },
                  "minItems": 1,
                  "title": "Nodes",
                  "type": "array"
                },
                "edges": {
                  "description": "Static or conditional edges connecting nodes.",
                  "items": {
                    "discriminator": {
                      "mapping": {
                        "conditional": "#/$defs/_ConditionalEdge",
                        "static": "#/$defs/_StaticEdge"
                      },
                      "propertyName": "kind"
                    },
                    "oneOf": [
                      {
                        "$ref": "#/$defs/_StaticEdge"
                      },
                      {
                        "$ref": "#/$defs/_ConditionalEdge"
                      }
                    ]
                  },
                  "title": "Edges",
                  "type": "array"
                },
                "max_iterations": {
                  "anyOf": [
                    {
                      "exclusiveMinimum": 0,
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Hard cap on supersteps. ``None`` means unbounded. Recommended for any graph that contains a cycle.",
                  "title": "Max Iterations"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "description",
                "nodes"
              ],
              "title": "Graph",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_graph",
        "description": "Delete a Graph by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_graphs",
        "description": "Find Graph entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_collections",
        "description": "List Collection entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_collection",
        "description": "Fetch one Collection by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_collection",
        "description": "Create a new Collection. Body shape is the full Collection schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "CollectionCrossEncoder": {
                  "description": "User-facing pointer to a :class:`CrossEncoderProvider`.\n\nMirrors :class:`primer.model.collection.CollectionEmbedder`: the\n``provider_id`` references an entry in the application's\n:class:`CrossEncoderProvider` registry, and ``model`` names one\nof that provider's permitted models. Both are validated against\nthe configured providers at runtime, not here.",
                  "properties": {
                    "provider_id": {
                      "description": "Identifier of the CrossEncoderProvider configured for this collection. Must match a CrossEncoderProvider.id in the application's provider registry.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model": {
                      "description": "Provider-side cross-encoder model name (e.g. ``BAAI/bge-reranker-v2-m3``). Must be one of the models permitted on the referenced provider.",
                      "minLength": 1,
                      "title": "Model",
                      "type": "string"
                    },
                    "top_n": {
                      "default": 100,
                      "description": "How many vector-search candidates the cross-encoder scores. Quality plateaus past ~100 in published benchmarks; latency grows roughly linearly past that point. The searcher overfetches from the vector store to fill this pool.",
                      "exclusiveMinimum": 0,
                      "title": "Top N",
                      "type": "integer"
                    },
                    "batch_size": {
                      "default": 32,
                      "description": "Batch size handed to the underlying cross-encoder predictor. ``32`` is the sentence-transformers default and works well on CPU; on GPU, 64\u2013128 is typical.",
                      "exclusiveMinimum": 0,
                      "title": "Batch Size",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model"
                  ],
                  "title": "CollectionCrossEncoder",
                  "type": "object"
                },
                "CollectionEmbedder": {
                  "description": "Which embedding provider + model a collection uses to vectorise documents.\n\nThe ``provider_id`` references an :class:`primer.model.provider.EmbeddingProvider`\nby its user-chosen id; the ``model`` names one of that provider's\npermitted embedding models. Both are validated against the application's\nconfigured providers at runtime, not here -- this model just carries\nthe reference.",
                  "properties": {
                    "provider_id": {
                      "description": "Identifier of the EmbeddingProvider configured for this collection. Must match an EmbeddingProvider.id in the application's provider registry.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model": {
                      "description": "Provider-side embedding model name to use for this collection (e.g. 'text-embedding-3-small'). Must be one of the models permitted on the referenced provider.",
                      "minLength": 1,
                      "title": "Model",
                      "type": "string"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model"
                  ],
                  "title": "CollectionEmbedder",
                  "type": "object"
                },
                "CollectionSearch": {
                  "description": "Per-collection search-augmentation toggles.\n\nBoth fields are independently optional. The searcher runs them\nin the canonical order ``vector \u2192 cross-encoder rerank \u2192 MMR``\nwhen both are set:\n\n* the cross-encoder needs a relevance-rich pool to re-score,\n* MMR diversifies a small already-relevant pool,\n\nso reversing the order would have the reranker waste compute on\ndiverse-but-irrelevant items.",
                  "properties": {
                    "mmr": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/MmrConfig"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Diversification config. ``None`` = MMR disabled (default vector ranking is preserved)."
                    },
                    "cer": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/CollectionCrossEncoder"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Cross-encoder reranker config. ``None`` = reranking disabled (vector-store score is preserved)."
                    }
                  },
                  "title": "CollectionSearch",
                  "type": "object"
                },
                "MmrConfig": {
                  "description": "Maximal Marginal Relevance tuning.\n\nCheap (pure linear algebra over the candidate pool) and adds\nessentially zero latency for ``fetch_k < 200``. The diversity\ndecision uses the candidate vectors already returned by the\nvector store -- no re-embedding cost.",
                  "properties": {
                    "lambda_mult": {
                      "default": 0.5,
                      "description": "Diversity\u2194relevance trade-off. ``1.0`` = pure relevance (equivalent to vanilla similarity); ``0.0`` = pure diversity. The conventional default of ``0.5`` matches LangChain / Haystack / OpenSearch.",
                      "maximum": 1.0,
                      "minimum": 0.0,
                      "title": "Lambda Mult",
                      "type": "number"
                    },
                    "fetch_k": {
                      "anyOf": [
                        {
                          "exclusiveMinimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Candidates pulled from the vector store before MMR runs. ``None`` defers the decision to call time; the searcher uses ``max(50, 10 * k)`` as the default overfetch (matches LangChain ``fetch_k`` and OpenSearch ``candidates`` defaults).",
                      "title": "Fetch K"
                    }
                  },
                  "title": "MmrConfig",
                  "type": "object"
                }
              },
              "description": "A set of documents searchable by similarity / semantic / hybrid search.\n\nInherits ``id`` and ``description`` from :class:`Describeable`. The\n``embedder`` field selects how the collection's documents are\nvectorised; all documents in a collection share the same embedding\nspace.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "embedder": {
                  "$ref": "#/$defs/CollectionEmbedder",
                  "description": "Embedding provider and model used to vectorise this collection's documents."
                },
                "search_provider_id": {
                  "description": "Id of the SemanticSearchProvider backing this collection's vector index. Bound at create; immutable thereafter (see Collection PUT validator \u2014 wired in Task 5).",
                  "minLength": 1,
                  "title": "Search Provider Id",
                  "type": "string"
                },
                "search": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/CollectionSearch"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional retrieval-augmentation toggles applied on top of the base vector search. ``None`` (default) means vanilla vector ranking; setting ``mmr`` adds Maximal Marginal Relevance diversification; setting ``cer`` adds cross-encoder reranking; setting both runs ``vector \u2192 cross-encoder rerank \u2192 MMR``. See :class:`primer.model.search.CollectionSearch`."
                },
                "system": {
                  "default": false,
                  "description": "Marks the collection as system-managed (created and owned by an internal subsystem like the SemanticCatalog). Future Collection-CRUD APIs MUST refuse delete and update on system collections. Defaults to False; legacy rows without this field deserialise as user collections.",
                  "title": "System",
                  "type": "boolean"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "description",
                "embedder",
                "search_provider_id"
              ],
              "title": "Collection",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_collection",
        "description": "Replace an existing Collection. Pass ``id`` (the row to update) and ``entity`` (the full new Collection body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "CollectionCrossEncoder": {
                  "description": "User-facing pointer to a :class:`CrossEncoderProvider`.\n\nMirrors :class:`primer.model.collection.CollectionEmbedder`: the\n``provider_id`` references an entry in the application's\n:class:`CrossEncoderProvider` registry, and ``model`` names one\nof that provider's permitted models. Both are validated against\nthe configured providers at runtime, not here.",
                  "properties": {
                    "provider_id": {
                      "description": "Identifier of the CrossEncoderProvider configured for this collection. Must match a CrossEncoderProvider.id in the application's provider registry.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model": {
                      "description": "Provider-side cross-encoder model name (e.g. ``BAAI/bge-reranker-v2-m3``). Must be one of the models permitted on the referenced provider.",
                      "minLength": 1,
                      "title": "Model",
                      "type": "string"
                    },
                    "top_n": {
                      "default": 100,
                      "description": "How many vector-search candidates the cross-encoder scores. Quality plateaus past ~100 in published benchmarks; latency grows roughly linearly past that point. The searcher overfetches from the vector store to fill this pool.",
                      "exclusiveMinimum": 0,
                      "title": "Top N",
                      "type": "integer"
                    },
                    "batch_size": {
                      "default": 32,
                      "description": "Batch size handed to the underlying cross-encoder predictor. ``32`` is the sentence-transformers default and works well on CPU; on GPU, 64\u2013128 is typical.",
                      "exclusiveMinimum": 0,
                      "title": "Batch Size",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model"
                  ],
                  "title": "CollectionCrossEncoder",
                  "type": "object"
                },
                "CollectionEmbedder": {
                  "description": "Which embedding provider + model a collection uses to vectorise documents.\n\nThe ``provider_id`` references an :class:`primer.model.provider.EmbeddingProvider`\nby its user-chosen id; the ``model`` names one of that provider's\npermitted embedding models. Both are validated against the application's\nconfigured providers at runtime, not here -- this model just carries\nthe reference.",
                  "properties": {
                    "provider_id": {
                      "description": "Identifier of the EmbeddingProvider configured for this collection. Must match an EmbeddingProvider.id in the application's provider registry.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model": {
                      "description": "Provider-side embedding model name to use for this collection (e.g. 'text-embedding-3-small'). Must be one of the models permitted on the referenced provider.",
                      "minLength": 1,
                      "title": "Model",
                      "type": "string"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model"
                  ],
                  "title": "CollectionEmbedder",
                  "type": "object"
                },
                "CollectionSearch": {
                  "description": "Per-collection search-augmentation toggles.\n\nBoth fields are independently optional. The searcher runs them\nin the canonical order ``vector \u2192 cross-encoder rerank \u2192 MMR``\nwhen both are set:\n\n* the cross-encoder needs a relevance-rich pool to re-score,\n* MMR diversifies a small already-relevant pool,\n\nso reversing the order would have the reranker waste compute on\ndiverse-but-irrelevant items.",
                  "properties": {
                    "mmr": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/MmrConfig"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Diversification config. ``None`` = MMR disabled (default vector ranking is preserved)."
                    },
                    "cer": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/CollectionCrossEncoder"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Cross-encoder reranker config. ``None`` = reranking disabled (vector-store score is preserved)."
                    }
                  },
                  "title": "CollectionSearch",
                  "type": "object"
                },
                "MmrConfig": {
                  "description": "Maximal Marginal Relevance tuning.\n\nCheap (pure linear algebra over the candidate pool) and adds\nessentially zero latency for ``fetch_k < 200``. The diversity\ndecision uses the candidate vectors already returned by the\nvector store -- no re-embedding cost.",
                  "properties": {
                    "lambda_mult": {
                      "default": 0.5,
                      "description": "Diversity\u2194relevance trade-off. ``1.0`` = pure relevance (equivalent to vanilla similarity); ``0.0`` = pure diversity. The conventional default of ``0.5`` matches LangChain / Haystack / OpenSearch.",
                      "maximum": 1.0,
                      "minimum": 0.0,
                      "title": "Lambda Mult",
                      "type": "number"
                    },
                    "fetch_k": {
                      "anyOf": [
                        {
                          "exclusiveMinimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Candidates pulled from the vector store before MMR runs. ``None`` defers the decision to call time; the searcher uses ``max(50, 10 * k)`` as the default overfetch (matches LangChain ``fetch_k`` and OpenSearch ``candidates`` defaults).",
                      "title": "Fetch K"
                    }
                  },
                  "title": "MmrConfig",
                  "type": "object"
                }
              },
              "description": "A set of documents searchable by similarity / semantic / hybrid search.\n\nInherits ``id`` and ``description`` from :class:`Describeable`. The\n``embedder`` field selects how the collection's documents are\nvectorised; all documents in a collection share the same embedding\nspace.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "embedder": {
                  "$ref": "#/$defs/CollectionEmbedder",
                  "description": "Embedding provider and model used to vectorise this collection's documents."
                },
                "search_provider_id": {
                  "description": "Id of the SemanticSearchProvider backing this collection's vector index. Bound at create; immutable thereafter (see Collection PUT validator \u2014 wired in Task 5).",
                  "minLength": 1,
                  "title": "Search Provider Id",
                  "type": "string"
                },
                "search": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/CollectionSearch"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional retrieval-augmentation toggles applied on top of the base vector search. ``None`` (default) means vanilla vector ranking; setting ``mmr`` adds Maximal Marginal Relevance diversification; setting ``cer`` adds cross-encoder reranking; setting both runs ``vector \u2192 cross-encoder rerank \u2192 MMR``. See :class:`primer.model.search.CollectionSearch`."
                },
                "system": {
                  "default": false,
                  "description": "Marks the collection as system-managed (created and owned by an internal subsystem like the SemanticCatalog). Future Collection-CRUD APIs MUST refuse delete and update on system collections. Defaults to False; legacy rows without this field deserialise as user collections.",
                  "title": "System",
                  "type": "boolean"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "description",
                "embedder",
                "search_provider_id"
              ],
              "title": "Collection",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_collection",
        "description": "Delete a Collection by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_collections",
        "description": "Find Collection entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_documents",
        "description": "List Document entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_document",
        "description": "Fetch one Document by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_document",
        "description": "Create a new Document. Body shape is the full Document schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "description": "A single document stored in a :class:`Collection`.\n\nInherits ``id`` from :class:`Identifiable`. ``collection_id`` is the\nid of the parent :class:`Collection`; ``name`` is a human-readable\nlabel (distinct from ``id``, which is the wire identifier);\n``meta`` is a free-form bag the application can use for filtering,\nrouting, or display.\n\nThe document's payload (the actual text being indexed) is not on\nthis model -- payload storage is the storage backend's concern, and\ndifferent backends model it differently (raw bytes, pre-chunked\nspans, external URI, etc.).",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "collection_id": {
                  "description": "Identifier of the Collection this document belongs to.",
                  "minLength": 1,
                  "title": "Collection Id",
                  "type": "string"
                },
                "name": {
                  "description": "Human-readable name of the document.",
                  "minLength": 1,
                  "title": "Name",
                  "type": "string"
                },
                "meta": {
                  "additionalProperties": true,
                  "description": "Free-form metadata. Useful for filtering search results, tagging, audit trails, etc. Schema is application-defined.",
                  "title": "Meta",
                  "type": "object"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "collection_id",
                "name"
              ],
              "title": "Document",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_document",
        "description": "Replace an existing Document. Pass ``id`` (the row to update) and ``entity`` (the full new Document body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "description": "A single document stored in a :class:`Collection`.\n\nInherits ``id`` from :class:`Identifiable`. ``collection_id`` is the\nid of the parent :class:`Collection`; ``name`` is a human-readable\nlabel (distinct from ``id``, which is the wire identifier);\n``meta`` is a free-form bag the application can use for filtering,\nrouting, or display.\n\nThe document's payload (the actual text being indexed) is not on\nthis model -- payload storage is the storage backend's concern, and\ndifferent backends model it differently (raw bytes, pre-chunked\nspans, external URI, etc.).",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "collection_id": {
                  "description": "Identifier of the Collection this document belongs to.",
                  "minLength": 1,
                  "title": "Collection Id",
                  "type": "string"
                },
                "name": {
                  "description": "Human-readable name of the document.",
                  "minLength": 1,
                  "title": "Name",
                  "type": "string"
                },
                "meta": {
                  "additionalProperties": true,
                  "description": "Free-form metadata. Useful for filtering search results, tagging, audit trails, etc. Schema is application-defined.",
                  "title": "Meta",
                  "type": "object"
                },
                "harness_id": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "When set, this row is managed by the named harness. Mutation through the public CRUD endpoints returns 409 \u2014 use the harness's sync/uninstall flow instead.",
                  "title": "Harness Id"
                }
              },
              "required": [
                "id",
                "collection_id",
                "name"
              ],
              "title": "Document",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_document",
        "description": "Delete a Document by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_documents",
        "description": "Find Document entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_agent_threads",
        "description": "List Thread entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_agent_thread",
        "description": "Fetch one Thread by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_agent_thread",
        "description": "Create a new Thread. Body shape is the full Thread schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "description": "One persistent chat thread between a user and an agent.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "agent_id": {
                  "description": "Identifier of the :class:`primer.model.agent.Agent` this thread runs against. Snapshot -- if the agent definition changes later, existing threads keep their original agent_id but pick up the new agent definition's system_prompt / tools / model on subsequent turns.",
                  "minLength": 1,
                  "title": "Agent Id",
                  "type": "string"
                },
                "title": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional human-readable thread title. The executor does NOT auto-generate one; UIs that want titles supply their own at open time or update later via :meth:`primer.int.Storage.update`.",
                  "title": "Title"
                },
                "created_at": {
                  "description": "UTC instant the thread was opened.",
                  "format": "date-time",
                  "title": "Created At",
                  "type": "string"
                },
                "last_activity_at": {
                  "description": "UTC instant of the most recent message append. Updated by the executor at the end of every turn.",
                  "format": "date-time",
                  "title": "Last Activity At",
                  "type": "string"
                }
              },
              "required": [
                "id",
                "agent_id",
                "created_at",
                "last_activity_at"
              ],
              "title": "Thread",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_agent_thread",
        "description": "Replace an existing Thread. Pass ``id`` (the row to update) and ``entity`` (the full new Thread body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "description": "One persistent chat thread between a user and an agent.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "agent_id": {
                  "description": "Identifier of the :class:`primer.model.agent.Agent` this thread runs against. Snapshot -- if the agent definition changes later, existing threads keep their original agent_id but pick up the new agent definition's system_prompt / tools / model on subsequent turns.",
                  "minLength": 1,
                  "title": "Agent Id",
                  "type": "string"
                },
                "title": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional human-readable thread title. The executor does NOT auto-generate one; UIs that want titles supply their own at open time or update later via :meth:`primer.int.Storage.update`.",
                  "title": "Title"
                },
                "created_at": {
                  "description": "UTC instant the thread was opened.",
                  "format": "date-time",
                  "title": "Created At",
                  "type": "string"
                },
                "last_activity_at": {
                  "description": "UTC instant of the most recent message append. Updated by the executor at the end of every turn.",
                  "format": "date-time",
                  "title": "Last Activity At",
                  "type": "string"
                }
              },
              "required": [
                "id",
                "agent_id",
                "created_at",
                "last_activity_at"
              ],
              "title": "Thread",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_agent_thread",
        "description": "Delete a Thread by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_agent_threads",
        "description": "Find Thread entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_graph_threads",
        "description": "List GraphThread entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_graph_thread",
        "description": "Fetch one GraphThread by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_graph_thread",
        "description": "Create a new GraphThread. Body shape is the full GraphThread schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "NodeRuntimeState": {
                  "description": "Per-node runtime state within a graph execution.",
                  "properties": {
                    "status": {
                      "$ref": "#/$defs/NodeRuntimeStatus",
                      "default": "pending",
                      "description": "Current per-node status."
                    },
                    "last_run_iteration": {
                      "anyOf": [
                        {
                          "minimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Most recent graph iteration that ran this node, if any.",
                      "title": "Last Run Iteration"
                    },
                    "last_run_at": {
                      "anyOf": [
                        {
                          "format": "date-time",
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "UTC instant of the most recent run.",
                      "title": "Last Run At"
                    },
                    "error": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Error message when ``status == FAILED``.",
                      "title": "Error"
                    }
                  },
                  "title": "NodeRuntimeState",
                  "type": "object"
                },
                "NodeRuntimeStatus": {
                  "description": "Per-node lifecycle within a graph execution.\n\nDistinct from :class:`primer.model.session.SessionStatus` because\ngraphs add the ``pending`` (not yet reached) and ``failed``\n(errored out) states that don't apply to standalone agent\nsessions. The graph's :class:`SessionStatus` is aggregated FROM\nthese per-node values.",
                  "enum": [
                    "pending",
                    "running",
                    "waiting",
                    "ended",
                    "failed"
                  ],
                  "title": "NodeRuntimeStatus",
                  "type": "string"
                },
                "SessionStatus": {
                  "description": "Lifecycle state of an :class:`AgentSession`.\n\n:attr:`CREATED` is the pre-execution state \u2014 the session row exists\nbut no worker has been told to run it yet. ``POST .../resume`` (or\n``auto_start=True`` on session create) signals the scheduler to\ntransition into :attr:`RUNNING`.\n\nTransitions out of :attr:`CREATED` / between non-terminal states\nare driven by the agent runtime (which sets :attr:`RUNNING` while a\nturn is in flight, :attr:`WAITING` when blocked on either user\ninput or an approval, :attr:`ENDED` on terminal) and by the user\n(who can request :attr:`PAUSED` and resume back to :attr:`RUNNING`).\n\n:attr:`WAITING` is intentionally one state regardless of what the\nsession is blocked on -- the distinction is recorded in\n``.state/sessions/<id>/waiting.json`` via the :class:`WaitingState`\ndiscriminated union, which the user inspects to figure out what\nresponse is needed.\n\nTerminal: :attr:`ENDED`. Non-terminal: everything else.",
                  "enum": [
                    "created",
                    "running",
                    "waiting",
                    "paused",
                    "ended"
                  ],
                  "title": "SessionStatus",
                  "type": "string"
                }
              },
              "description": "One execution of one graph (standalone, storage-backed).\n\nPersisted via :class:`primer.int.Storage` with model class\n``GraphThread``. The G2 sub-project introduces this as the\nparent row for per-node :class:`GraphNodeMessage` rows; the\ntype lives here so the model layer is self-contained.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "graph_id": {
                  "minLength": 1,
                  "title": "Graph Id",
                  "type": "string"
                },
                "title": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Title"
                },
                "created_at": {
                  "format": "date-time",
                  "title": "Created At",
                  "type": "string"
                },
                "last_activity_at": {
                  "format": "date-time",
                  "title": "Last Activity At",
                  "type": "string"
                },
                "iteration": {
                  "default": 0,
                  "minimum": 0,
                  "title": "Iteration",
                  "type": "integer"
                },
                "node_states": {
                  "additionalProperties": {
                    "$ref": "#/$defs/NodeRuntimeState"
                  },
                  "title": "Node States",
                  "type": "object"
                },
                "status": {
                  "$ref": "#/$defs/SessionStatus",
                  "default": "running",
                  "description": "Aggregated graph status; derived from per-node statuses."
                },
                "ended_reason": {
                  "anyOf": [
                    {
                      "enum": [
                        "completed",
                        "failed",
                        "cancelled",
                        "max_iterations_exceeded"
                      ],
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Set when ``status == ENDED``.",
                  "title": "Ended Reason"
                },
                "ended_detail": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Spec \u00a75.4 failure code (e.g. ``end_output_invalid``, ``template_error``, ``routing_failed``, ``max_iterations_exceeded``) carried alongside ``ended_reason='failed'``. ``None`` for successful completions and for ``cancelled``.",
                  "title": "Ended Detail"
                }
              },
              "required": [
                "id",
                "graph_id",
                "created_at",
                "last_activity_at"
              ],
              "title": "GraphThread",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_graph_thread",
        "description": "Replace an existing GraphThread. Pass ``id`` (the row to update) and ``entity`` (the full new GraphThread body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "NodeRuntimeState": {
                  "description": "Per-node runtime state within a graph execution.",
                  "properties": {
                    "status": {
                      "$ref": "#/$defs/NodeRuntimeStatus",
                      "default": "pending",
                      "description": "Current per-node status."
                    },
                    "last_run_iteration": {
                      "anyOf": [
                        {
                          "minimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Most recent graph iteration that ran this node, if any.",
                      "title": "Last Run Iteration"
                    },
                    "last_run_at": {
                      "anyOf": [
                        {
                          "format": "date-time",
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "UTC instant of the most recent run.",
                      "title": "Last Run At"
                    },
                    "error": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Error message when ``status == FAILED``.",
                      "title": "Error"
                    }
                  },
                  "title": "NodeRuntimeState",
                  "type": "object"
                },
                "NodeRuntimeStatus": {
                  "description": "Per-node lifecycle within a graph execution.\n\nDistinct from :class:`primer.model.session.SessionStatus` because\ngraphs add the ``pending`` (not yet reached) and ``failed``\n(errored out) states that don't apply to standalone agent\nsessions. The graph's :class:`SessionStatus` is aggregated FROM\nthese per-node values.",
                  "enum": [
                    "pending",
                    "running",
                    "waiting",
                    "ended",
                    "failed"
                  ],
                  "title": "NodeRuntimeStatus",
                  "type": "string"
                },
                "SessionStatus": {
                  "description": "Lifecycle state of an :class:`AgentSession`.\n\n:attr:`CREATED` is the pre-execution state \u2014 the session row exists\nbut no worker has been told to run it yet. ``POST .../resume`` (or\n``auto_start=True`` on session create) signals the scheduler to\ntransition into :attr:`RUNNING`.\n\nTransitions out of :attr:`CREATED` / between non-terminal states\nare driven by the agent runtime (which sets :attr:`RUNNING` while a\nturn is in flight, :attr:`WAITING` when blocked on either user\ninput or an approval, :attr:`ENDED` on terminal) and by the user\n(who can request :attr:`PAUSED` and resume back to :attr:`RUNNING`).\n\n:attr:`WAITING` is intentionally one state regardless of what the\nsession is blocked on -- the distinction is recorded in\n``.state/sessions/<id>/waiting.json`` via the :class:`WaitingState`\ndiscriminated union, which the user inspects to figure out what\nresponse is needed.\n\nTerminal: :attr:`ENDED`. Non-terminal: everything else.",
                  "enum": [
                    "created",
                    "running",
                    "waiting",
                    "paused",
                    "ended"
                  ],
                  "title": "SessionStatus",
                  "type": "string"
                }
              },
              "description": "One execution of one graph (standalone, storage-backed).\n\nPersisted via :class:`primer.int.Storage` with model class\n``GraphThread``. The G2 sub-project introduces this as the\nparent row for per-node :class:`GraphNodeMessage` rows; the\ntype lives here so the model layer is self-contained.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "graph_id": {
                  "minLength": 1,
                  "title": "Graph Id",
                  "type": "string"
                },
                "title": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Title"
                },
                "created_at": {
                  "format": "date-time",
                  "title": "Created At",
                  "type": "string"
                },
                "last_activity_at": {
                  "format": "date-time",
                  "title": "Last Activity At",
                  "type": "string"
                },
                "iteration": {
                  "default": 0,
                  "minimum": 0,
                  "title": "Iteration",
                  "type": "integer"
                },
                "node_states": {
                  "additionalProperties": {
                    "$ref": "#/$defs/NodeRuntimeState"
                  },
                  "title": "Node States",
                  "type": "object"
                },
                "status": {
                  "$ref": "#/$defs/SessionStatus",
                  "default": "running",
                  "description": "Aggregated graph status; derived from per-node statuses."
                },
                "ended_reason": {
                  "anyOf": [
                    {
                      "enum": [
                        "completed",
                        "failed",
                        "cancelled",
                        "max_iterations_exceeded"
                      ],
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Set when ``status == ENDED``.",
                  "title": "Ended Reason"
                },
                "ended_detail": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Spec \u00a75.4 failure code (e.g. ``end_output_invalid``, ``template_error``, ``routing_failed``, ``max_iterations_exceeded``) carried alongside ``ended_reason='failed'``. ``None`` for successful completions and for ``cancelled``.",
                  "title": "Ended Detail"
                }
              },
              "required": [
                "id",
                "graph_id",
                "created_at",
                "last_activity_at"
              ],
              "title": "GraphThread",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_graph_thread",
        "description": "Delete a GraphThread by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_graph_threads",
        "description": "Find GraphThread entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_semantic_search_providers",
        "description": "List SemanticSearchProvider entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_semantic_search_provider",
        "description": "Fetch one SemanticSearchProvider by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_semantic_search_provider",
        "description": "Create a new SemanticSearchProvider. Body shape is the full SemanticSearchProvider schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "LanceConfig": {
                  "description": "LanceDB embedded-mode SemanticSearchProvider configuration.\n\nPersists every collection's vector table as a Lance dataset under\n``path``. The directory is created with mode 0o700 on first use.\nMultiple LanceDB SSPs can coexist as long as they use different\npaths. Single-process write-safe; multi-process primer-api +\nprimer-worker against the same path is out of scope (spec \u00a79).",
                  "properties": {
                    "path": {
                      "description": "Filesystem directory holding the LanceDB datasets. Created on initialise if missing. Must be writable by the primer process. Use an absolute path.",
                      "format": "path",
                      "title": "Path",
                      "type": "string"
                    },
                    "hnsw_m": {
                      "default": 16,
                      "description": "HNSW graph degree. Mirrors PgVectorConfig.hnsw_m so the create modal can share one knobs section across backends.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw M",
                      "type": "integer"
                    },
                    "hnsw_ef_construction": {
                      "default": 64,
                      "description": "HNSW 'ef_construction' -- candidate list size during index build. Higher = better recall, slower build. Mirrors PgVectorConfig.hnsw_ef_construction's default.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Construction",
                      "type": "integer"
                    },
                    "hnsw_ef_search": {
                      "default": 40,
                      "description": "HNSW query-time candidate list size. Higher = better recall, slower queries. Mirrors PgVectorConfig.hnsw_ef_search (the pgvector variant exposes this via the hnsw.ef_search GUC).",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Search",
                      "type": "integer"
                    },
                    "index_min_rows": {
                      "default": 1000,
                      "description": "Skip ANN-index construction until a collection has at least this many rows. Below the threshold, search runs brute-force.",
                      "exclusiveMinimum": 0,
                      "title": "Index Min Rows",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "path"
                  ],
                  "title": "LanceConfig",
                  "type": "object"
                },
                "PgVectorConfig": {
                  "description": "Connection settings for the pgvector VectorStore provider.\n\nRequires the ``vector`` extension to be installable on the target\ndatabase (the provider runs ``CREATE EXTENSION IF NOT EXISTS vector``\non initialise).",
                  "properties": {
                    "hostname": {
                      "description": "Postgres host (e.g. 'db.internal' or '127.0.0.1').",
                      "minLength": 1,
                      "title": "Hostname",
                      "type": "string"
                    },
                    "port": {
                      "default": 5432,
                      "description": "Postgres TCP port.",
                      "maximum": 65535,
                      "minimum": 1,
                      "title": "Port",
                      "type": "integer"
                    },
                    "username": {
                      "description": "Postgres role to authenticate as.",
                      "minLength": 1,
                      "title": "Username",
                      "type": "string"
                    },
                    "password": {
                      "description": "Password for the role.",
                      "format": "password",
                      "title": "Password",
                      "type": "string",
                      "writeOnly": true
                    },
                    "database": {
                      "description": "Database name to connect to.",
                      "minLength": 1,
                      "title": "Database",
                      "type": "string"
                    },
                    "db_schema": {
                      "default": "public",
                      "description": "Postgres schema where tables and indexes are created. Renamed from 'schema' to avoid shadowing Pydantic's BaseModel.schema().",
                      "minLength": 1,
                      "title": "Db Schema",
                      "type": "string"
                    },
                    "pool": {
                      "$ref": "#/$defs/PoolConfig",
                      "description": "Connection pool settings."
                    },
                    "distance_metric": {
                      "default": "cosine",
                      "description": "Distance metric for the vector index. 'cosine' for normalised embeddings (most common), 'l2' for Euclidean, 'ip' for inner product.",
                      "enum": [
                        "cosine",
                        "l2",
                        "ip"
                      ],
                      "title": "Distance Metric",
                      "type": "string"
                    },
                    "hnsw_m": {
                      "default": 16,
                      "description": "HNSW 'm' parameter -- max connections per node. Higher = better recall, larger index, slower build. pgvector default is 16.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw M",
                      "type": "integer"
                    },
                    "hnsw_ef_construction": {
                      "default": 64,
                      "description": "HNSW 'ef_construction' -- candidate list size during build. Higher = better recall, slower build. pgvector default is 64.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Construction",
                      "type": "integer"
                    },
                    "hnsw_ef_search": {
                      "default": 40,
                      "description": "Query-time 'hnsw.ef_search' GUC -- candidate list size during queries. Higher = better recall, slower queries. pgvector default is 40.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Search",
                      "type": "integer"
                    },
                    "reindex_cron": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Crontab expression scheduling periodic HNSW maintenance via :meth:`primer.int.VectorStoreProvider.maintain_indexes`. None disables scheduling (caller drives maintenance manually).",
                      "title": "Reindex Cron"
                    }
                  },
                  "required": [
                    "hostname",
                    "username",
                    "password",
                    "database"
                  ],
                  "title": "PgVectorConfig",
                  "type": "object"
                },
                "PgVectorScaleConfig": {
                  "description": "Connection settings for the pgvectorscale VectorStore provider.\n\nRequires the ``vector`` AND ``vectorscale`` extensions. pgvectorscale\nlayers on top of pgvector and adds the StreamingDiskANN index, SBQ\nquantization, and tuned HNSW behaviour. When ``enable_diskann`` is\nTrue the per-collection vector tables get a DiskANN index instead\nof HNSW; the ``diskann_*`` fields below tune that index. When\n``enable_diskann`` is False the provider behaves exactly like\n:class:`PgVectorConfig` plus the ``vectorscale`` extension being\ninstalled for opportunistic use.",
                  "properties": {
                    "hostname": {
                      "description": "Postgres host (e.g. 'db.internal' or '127.0.0.1').",
                      "minLength": 1,
                      "title": "Hostname",
                      "type": "string"
                    },
                    "port": {
                      "default": 5432,
                      "description": "Postgres TCP port.",
                      "maximum": 65535,
                      "minimum": 1,
                      "title": "Port",
                      "type": "integer"
                    },
                    "username": {
                      "description": "Postgres role to authenticate as.",
                      "minLength": 1,
                      "title": "Username",
                      "type": "string"
                    },
                    "password": {
                      "description": "Password for the role.",
                      "format": "password",
                      "title": "Password",
                      "type": "string",
                      "writeOnly": true
                    },
                    "database": {
                      "description": "Database name to connect to.",
                      "minLength": 1,
                      "title": "Database",
                      "type": "string"
                    },
                    "db_schema": {
                      "default": "public",
                      "description": "Postgres schema where tables and indexes are created. Renamed from 'schema' to avoid shadowing Pydantic's BaseModel.schema().",
                      "minLength": 1,
                      "title": "Db Schema",
                      "type": "string"
                    },
                    "pool": {
                      "$ref": "#/$defs/PoolConfig",
                      "description": "Connection pool settings."
                    },
                    "distance_metric": {
                      "default": "cosine",
                      "description": "Distance metric for the vector index. 'cosine' for normalised embeddings (most common), 'l2' for Euclidean, 'ip' for inner product.",
                      "enum": [
                        "cosine",
                        "l2",
                        "ip"
                      ],
                      "title": "Distance Metric",
                      "type": "string"
                    },
                    "hnsw_m": {
                      "default": 16,
                      "description": "HNSW 'm' parameter -- max connections per node. Higher = better recall, larger index, slower build. pgvector default is 16.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw M",
                      "type": "integer"
                    },
                    "hnsw_ef_construction": {
                      "default": 64,
                      "description": "HNSW 'ef_construction' -- candidate list size during build. Higher = better recall, slower build. pgvector default is 64.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Construction",
                      "type": "integer"
                    },
                    "hnsw_ef_search": {
                      "default": 40,
                      "description": "Query-time 'hnsw.ef_search' GUC -- candidate list size during queries. Higher = better recall, slower queries. pgvector default is 40.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Search",
                      "type": "integer"
                    },
                    "reindex_cron": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Crontab expression scheduling periodic HNSW maintenance via :meth:`primer.int.VectorStoreProvider.maintain_indexes`. None disables scheduling (caller drives maintenance manually).",
                      "title": "Reindex Cron"
                    },
                    "enable_diskann": {
                      "default": false,
                      "description": "When True, create StreamingDiskANN indexes (from pgvectorscale) instead of pgvector's HNSW. DiskANN is the right choice for very large collections (10M+ vectors) where HNSW's memory cost becomes prohibitive.",
                      "title": "Enable Diskann",
                      "type": "boolean"
                    },
                    "diskann_storage_layout": {
                      "default": "memory_optimized",
                      "description": "DiskANN storage layout. ``memory_optimized`` enables Statistical Binary Quantization (SBQ) -- the default and the recommended choice; ``plain`` keeps full-precision vectors in the index.",
                      "enum": [
                        "memory_optimized",
                        "plain"
                      ],
                      "title": "Diskann Storage Layout",
                      "type": "string"
                    },
                    "diskann_num_neighbors": {
                      "default": 50,
                      "description": "DiskANN graph degree -- number of neighbours stored per node. Higher = better recall, larger index. Default 50.",
                      "exclusiveMinimum": 0,
                      "title": "Diskann Num Neighbors",
                      "type": "integer"
                    },
                    "diskann_search_list_size": {
                      "default": 100,
                      "description": "DiskANN ``search_list_size`` build parameter AND the default query-time ``diskann.query_search_list_size`` GUC. Higher = better recall, slower queries / build.",
                      "exclusiveMinimum": 0,
                      "title": "Diskann Search List Size",
                      "type": "integer"
                    },
                    "diskann_max_alpha": {
                      "default": 1.2,
                      "description": "DiskANN graph density. Higher (up to ~1.4) increases recall at the cost of build time. Default 1.2.",
                      "exclusiveMinimum": 1.0,
                      "title": "Diskann Max Alpha",
                      "type": "number"
                    },
                    "diskann_num_bits_per_dimension": {
                      "anyOf": [
                        {
                          "exclusiveMinimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Bits per dimension used by SBQ when storage_layout is ``memory_optimized``. None lets pgvectorscale pick the default (typically 2). Ignored when storage_layout is ``plain``.",
                      "title": "Diskann Num Bits Per Dimension"
                    }
                  },
                  "required": [
                    "hostname",
                    "username",
                    "password",
                    "database"
                  ],
                  "title": "PgVectorScaleConfig",
                  "type": "object"
                },
                "PoolConfig": {
                  "description": "Connection pool settings shared by Postgres-backed providers.\n\nMaps directly onto asyncpg's :func:`asyncpg.create_pool` parameters.\nDefaults are tuned for a small-to-medium application; large\ndeployments should raise ``max_size`` to match expected concurrency.",
                  "properties": {
                    "min_size": {
                      "default": 1,
                      "description": "Minimum number of connections kept open in the pool.",
                      "exclusiveMinimum": 0,
                      "title": "Min Size",
                      "type": "integer"
                    },
                    "max_size": {
                      "default": 25,
                      "description": "Maximum number of connections the pool will open. asyncpg opens connections lazily up to this ceiling and closes idle ones, so a high ceiling costs nothing for a quiet process. The default accounts for a worker/coordinator process, which pins several long-lived LISTEN connections from this same pool (scheduler session_ready + session_cancel, claim engine claim_ready, and one per event-bus subscriber: yield listener, session/chat tick forwarders, mcp-task bridge, watchers) on top of per-turn storage and rate-limiter acquires at worker concurrency. With the old default of 10 those persistent listeners starved per-turn acquires and a turn could block on pool.acquire. Large deployments should raise this further to match expected concurrency.",
                      "exclusiveMinimum": 0,
                      "title": "Max Size",
                      "type": "integer"
                    },
                    "acquire_timeout": {
                      "default": 30.0,
                      "description": "Seconds a caller will wait to acquire a connection before raising.",
                      "exclusiveMinimum": 0,
                      "title": "Acquire Timeout",
                      "type": "number"
                    },
                    "max_idle": {
                      "default": 300.0,
                      "description": "Seconds an idle connection may stay in the pool before being closed.",
                      "exclusiveMinimum": 0,
                      "title": "Max Idle",
                      "type": "number"
                    },
                    "max_lifetime": {
                      "default": 3600.0,
                      "description": "Seconds a connection may live before being recycled (defends against leaks).",
                      "exclusiveMinimum": 0,
                      "title": "Max Lifetime",
                      "type": "number"
                    }
                  },
                  "title": "PoolConfig",
                  "type": "object"
                },
                "SemanticSearchProviderType": {
                  "description": "Supported semantic-search backends.\n\nMirrors VectorStoreProviderType (which will be removed once all\ncallsites migrate to SemanticSearchProvider).",
                  "enum": [
                    "pgvector",
                    "pgvectorscale",
                    "lance"
                  ],
                  "title": "SemanticSearchProviderType",
                  "type": "string"
                }
              },
              "description": "Operator-managed semantic-search backend backing collections\nand the internal collections subsystem.\n\nStored as a CRUD-able row alongside LLMProvider, EmbeddingProvider,\netc. The discriminated ``config`` carries backend-specific\nconnection + index settings; the parent ``provider`` discriminator\nchooses which config shape is valid.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/SemanticSearchProviderType",
                  "description": "Which semantic-search backend to use."
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/PgVectorConfig"
                    },
                    {
                      "$ref": "#/$defs/PgVectorScaleConfig"
                    },
                    {
                      "$ref": "#/$defs/LanceConfig"
                    }
                  ],
                  "description": "Backend-specific connection settings; must match ``provider``.",
                  "title": "Config"
                }
              },
              "required": [
                "id",
                "provider",
                "config"
              ],
              "title": "SemanticSearchProvider",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_semantic_search_provider",
        "description": "Replace an existing SemanticSearchProvider. Pass ``id`` (the row to update) and ``entity`` (the full new SemanticSearchProvider body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "LanceConfig": {
                  "description": "LanceDB embedded-mode SemanticSearchProvider configuration.\n\nPersists every collection's vector table as a Lance dataset under\n``path``. The directory is created with mode 0o700 on first use.\nMultiple LanceDB SSPs can coexist as long as they use different\npaths. Single-process write-safe; multi-process primer-api +\nprimer-worker against the same path is out of scope (spec \u00a79).",
                  "properties": {
                    "path": {
                      "description": "Filesystem directory holding the LanceDB datasets. Created on initialise if missing. Must be writable by the primer process. Use an absolute path.",
                      "format": "path",
                      "title": "Path",
                      "type": "string"
                    },
                    "hnsw_m": {
                      "default": 16,
                      "description": "HNSW graph degree. Mirrors PgVectorConfig.hnsw_m so the create modal can share one knobs section across backends.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw M",
                      "type": "integer"
                    },
                    "hnsw_ef_construction": {
                      "default": 64,
                      "description": "HNSW 'ef_construction' -- candidate list size during index build. Higher = better recall, slower build. Mirrors PgVectorConfig.hnsw_ef_construction's default.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Construction",
                      "type": "integer"
                    },
                    "hnsw_ef_search": {
                      "default": 40,
                      "description": "HNSW query-time candidate list size. Higher = better recall, slower queries. Mirrors PgVectorConfig.hnsw_ef_search (the pgvector variant exposes this via the hnsw.ef_search GUC).",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Search",
                      "type": "integer"
                    },
                    "index_min_rows": {
                      "default": 1000,
                      "description": "Skip ANN-index construction until a collection has at least this many rows. Below the threshold, search runs brute-force.",
                      "exclusiveMinimum": 0,
                      "title": "Index Min Rows",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "path"
                  ],
                  "title": "LanceConfig",
                  "type": "object"
                },
                "PgVectorConfig": {
                  "description": "Connection settings for the pgvector VectorStore provider.\n\nRequires the ``vector`` extension to be installable on the target\ndatabase (the provider runs ``CREATE EXTENSION IF NOT EXISTS vector``\non initialise).",
                  "properties": {
                    "hostname": {
                      "description": "Postgres host (e.g. 'db.internal' or '127.0.0.1').",
                      "minLength": 1,
                      "title": "Hostname",
                      "type": "string"
                    },
                    "port": {
                      "default": 5432,
                      "description": "Postgres TCP port.",
                      "maximum": 65535,
                      "minimum": 1,
                      "title": "Port",
                      "type": "integer"
                    },
                    "username": {
                      "description": "Postgres role to authenticate as.",
                      "minLength": 1,
                      "title": "Username",
                      "type": "string"
                    },
                    "password": {
                      "description": "Password for the role.",
                      "format": "password",
                      "title": "Password",
                      "type": "string",
                      "writeOnly": true
                    },
                    "database": {
                      "description": "Database name to connect to.",
                      "minLength": 1,
                      "title": "Database",
                      "type": "string"
                    },
                    "db_schema": {
                      "default": "public",
                      "description": "Postgres schema where tables and indexes are created. Renamed from 'schema' to avoid shadowing Pydantic's BaseModel.schema().",
                      "minLength": 1,
                      "title": "Db Schema",
                      "type": "string"
                    },
                    "pool": {
                      "$ref": "#/$defs/PoolConfig",
                      "description": "Connection pool settings."
                    },
                    "distance_metric": {
                      "default": "cosine",
                      "description": "Distance metric for the vector index. 'cosine' for normalised embeddings (most common), 'l2' for Euclidean, 'ip' for inner product.",
                      "enum": [
                        "cosine",
                        "l2",
                        "ip"
                      ],
                      "title": "Distance Metric",
                      "type": "string"
                    },
                    "hnsw_m": {
                      "default": 16,
                      "description": "HNSW 'm' parameter -- max connections per node. Higher = better recall, larger index, slower build. pgvector default is 16.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw M",
                      "type": "integer"
                    },
                    "hnsw_ef_construction": {
                      "default": 64,
                      "description": "HNSW 'ef_construction' -- candidate list size during build. Higher = better recall, slower build. pgvector default is 64.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Construction",
                      "type": "integer"
                    },
                    "hnsw_ef_search": {
                      "default": 40,
                      "description": "Query-time 'hnsw.ef_search' GUC -- candidate list size during queries. Higher = better recall, slower queries. pgvector default is 40.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Search",
                      "type": "integer"
                    },
                    "reindex_cron": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Crontab expression scheduling periodic HNSW maintenance via :meth:`primer.int.VectorStoreProvider.maintain_indexes`. None disables scheduling (caller drives maintenance manually).",
                      "title": "Reindex Cron"
                    }
                  },
                  "required": [
                    "hostname",
                    "username",
                    "password",
                    "database"
                  ],
                  "title": "PgVectorConfig",
                  "type": "object"
                },
                "PgVectorScaleConfig": {
                  "description": "Connection settings for the pgvectorscale VectorStore provider.\n\nRequires the ``vector`` AND ``vectorscale`` extensions. pgvectorscale\nlayers on top of pgvector and adds the StreamingDiskANN index, SBQ\nquantization, and tuned HNSW behaviour. When ``enable_diskann`` is\nTrue the per-collection vector tables get a DiskANN index instead\nof HNSW; the ``diskann_*`` fields below tune that index. When\n``enable_diskann`` is False the provider behaves exactly like\n:class:`PgVectorConfig` plus the ``vectorscale`` extension being\ninstalled for opportunistic use.",
                  "properties": {
                    "hostname": {
                      "description": "Postgres host (e.g. 'db.internal' or '127.0.0.1').",
                      "minLength": 1,
                      "title": "Hostname",
                      "type": "string"
                    },
                    "port": {
                      "default": 5432,
                      "description": "Postgres TCP port.",
                      "maximum": 65535,
                      "minimum": 1,
                      "title": "Port",
                      "type": "integer"
                    },
                    "username": {
                      "description": "Postgres role to authenticate as.",
                      "minLength": 1,
                      "title": "Username",
                      "type": "string"
                    },
                    "password": {
                      "description": "Password for the role.",
                      "format": "password",
                      "title": "Password",
                      "type": "string",
                      "writeOnly": true
                    },
                    "database": {
                      "description": "Database name to connect to.",
                      "minLength": 1,
                      "title": "Database",
                      "type": "string"
                    },
                    "db_schema": {
                      "default": "public",
                      "description": "Postgres schema where tables and indexes are created. Renamed from 'schema' to avoid shadowing Pydantic's BaseModel.schema().",
                      "minLength": 1,
                      "title": "Db Schema",
                      "type": "string"
                    },
                    "pool": {
                      "$ref": "#/$defs/PoolConfig",
                      "description": "Connection pool settings."
                    },
                    "distance_metric": {
                      "default": "cosine",
                      "description": "Distance metric for the vector index. 'cosine' for normalised embeddings (most common), 'l2' for Euclidean, 'ip' for inner product.",
                      "enum": [
                        "cosine",
                        "l2",
                        "ip"
                      ],
                      "title": "Distance Metric",
                      "type": "string"
                    },
                    "hnsw_m": {
                      "default": 16,
                      "description": "HNSW 'm' parameter -- max connections per node. Higher = better recall, larger index, slower build. pgvector default is 16.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw M",
                      "type": "integer"
                    },
                    "hnsw_ef_construction": {
                      "default": 64,
                      "description": "HNSW 'ef_construction' -- candidate list size during build. Higher = better recall, slower build. pgvector default is 64.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Construction",
                      "type": "integer"
                    },
                    "hnsw_ef_search": {
                      "default": 40,
                      "description": "Query-time 'hnsw.ef_search' GUC -- candidate list size during queries. Higher = better recall, slower queries. pgvector default is 40.",
                      "exclusiveMinimum": 0,
                      "title": "Hnsw Ef Search",
                      "type": "integer"
                    },
                    "reindex_cron": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Crontab expression scheduling periodic HNSW maintenance via :meth:`primer.int.VectorStoreProvider.maintain_indexes`. None disables scheduling (caller drives maintenance manually).",
                      "title": "Reindex Cron"
                    },
                    "enable_diskann": {
                      "default": false,
                      "description": "When True, create StreamingDiskANN indexes (from pgvectorscale) instead of pgvector's HNSW. DiskANN is the right choice for very large collections (10M+ vectors) where HNSW's memory cost becomes prohibitive.",
                      "title": "Enable Diskann",
                      "type": "boolean"
                    },
                    "diskann_storage_layout": {
                      "default": "memory_optimized",
                      "description": "DiskANN storage layout. ``memory_optimized`` enables Statistical Binary Quantization (SBQ) -- the default and the recommended choice; ``plain`` keeps full-precision vectors in the index.",
                      "enum": [
                        "memory_optimized",
                        "plain"
                      ],
                      "title": "Diskann Storage Layout",
                      "type": "string"
                    },
                    "diskann_num_neighbors": {
                      "default": 50,
                      "description": "DiskANN graph degree -- number of neighbours stored per node. Higher = better recall, larger index. Default 50.",
                      "exclusiveMinimum": 0,
                      "title": "Diskann Num Neighbors",
                      "type": "integer"
                    },
                    "diskann_search_list_size": {
                      "default": 100,
                      "description": "DiskANN ``search_list_size`` build parameter AND the default query-time ``diskann.query_search_list_size`` GUC. Higher = better recall, slower queries / build.",
                      "exclusiveMinimum": 0,
                      "title": "Diskann Search List Size",
                      "type": "integer"
                    },
                    "diskann_max_alpha": {
                      "default": 1.2,
                      "description": "DiskANN graph density. Higher (up to ~1.4) increases recall at the cost of build time. Default 1.2.",
                      "exclusiveMinimum": 1.0,
                      "title": "Diskann Max Alpha",
                      "type": "number"
                    },
                    "diskann_num_bits_per_dimension": {
                      "anyOf": [
                        {
                          "exclusiveMinimum": 0,
                          "type": "integer"
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Bits per dimension used by SBQ when storage_layout is ``memory_optimized``. None lets pgvectorscale pick the default (typically 2). Ignored when storage_layout is ``plain``.",
                      "title": "Diskann Num Bits Per Dimension"
                    }
                  },
                  "required": [
                    "hostname",
                    "username",
                    "password",
                    "database"
                  ],
                  "title": "PgVectorScaleConfig",
                  "type": "object"
                },
                "PoolConfig": {
                  "description": "Connection pool settings shared by Postgres-backed providers.\n\nMaps directly onto asyncpg's :func:`asyncpg.create_pool` parameters.\nDefaults are tuned for a small-to-medium application; large\ndeployments should raise ``max_size`` to match expected concurrency.",
                  "properties": {
                    "min_size": {
                      "default": 1,
                      "description": "Minimum number of connections kept open in the pool.",
                      "exclusiveMinimum": 0,
                      "title": "Min Size",
                      "type": "integer"
                    },
                    "max_size": {
                      "default": 25,
                      "description": "Maximum number of connections the pool will open. asyncpg opens connections lazily up to this ceiling and closes idle ones, so a high ceiling costs nothing for a quiet process. The default accounts for a worker/coordinator process, which pins several long-lived LISTEN connections from this same pool (scheduler session_ready + session_cancel, claim engine claim_ready, and one per event-bus subscriber: yield listener, session/chat tick forwarders, mcp-task bridge, watchers) on top of per-turn storage and rate-limiter acquires at worker concurrency. With the old default of 10 those persistent listeners starved per-turn acquires and a turn could block on pool.acquire. Large deployments should raise this further to match expected concurrency.",
                      "exclusiveMinimum": 0,
                      "title": "Max Size",
                      "type": "integer"
                    },
                    "acquire_timeout": {
                      "default": 30.0,
                      "description": "Seconds a caller will wait to acquire a connection before raising.",
                      "exclusiveMinimum": 0,
                      "title": "Acquire Timeout",
                      "type": "number"
                    },
                    "max_idle": {
                      "default": 300.0,
                      "description": "Seconds an idle connection may stay in the pool before being closed.",
                      "exclusiveMinimum": 0,
                      "title": "Max Idle",
                      "type": "number"
                    },
                    "max_lifetime": {
                      "default": 3600.0,
                      "description": "Seconds a connection may live before being recycled (defends against leaks).",
                      "exclusiveMinimum": 0,
                      "title": "Max Lifetime",
                      "type": "number"
                    }
                  },
                  "title": "PoolConfig",
                  "type": "object"
                },
                "SemanticSearchProviderType": {
                  "description": "Supported semantic-search backends.\n\nMirrors VectorStoreProviderType (which will be removed once all\ncallsites migrate to SemanticSearchProvider).",
                  "enum": [
                    "pgvector",
                    "pgvectorscale",
                    "lance"
                  ],
                  "title": "SemanticSearchProviderType",
                  "type": "string"
                }
              },
              "description": "Operator-managed semantic-search backend backing collections\nand the internal collections subsystem.\n\nStored as a CRUD-able row alongside LLMProvider, EmbeddingProvider,\netc. The discriminated ``config`` carries backend-specific\nconnection + index settings; the parent ``provider`` discriminator\nchooses which config shape is valid.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/SemanticSearchProviderType",
                  "description": "Which semantic-search backend to use."
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/PgVectorConfig"
                    },
                    {
                      "$ref": "#/$defs/PgVectorScaleConfig"
                    },
                    {
                      "$ref": "#/$defs/LanceConfig"
                    }
                  ],
                  "description": "Backend-specific connection settings; must match ``provider``.",
                  "title": "Config"
                }
              },
              "required": [
                "id",
                "provider",
                "config"
              ],
              "title": "SemanticSearchProvider",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_semantic_search_provider",
        "description": "Delete a SemanticSearchProvider by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_semantic_search_providers",
        "description": "Find SemanticSearchProvider entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_tool_approval_policies",
        "description": "List ToolApprovalPolicy entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_tool_approval_policy",
        "description": "Fetch one ToolApprovalPolicy by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_tool_approval_policy",
        "description": "Create a new ToolApprovalPolicy. Body shape is the full ToolApprovalPolicy schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "LlmApprovalConfig": {
                  "description": "``type=llm`` \u2014 ask an LLM judge for the approval verdict.",
                  "properties": {
                    "type": {
                      "const": "llm",
                      "default": "llm",
                      "title": "Type",
                      "type": "string"
                    },
                    "provider_id": {
                      "description": "Id of an existing ``LLMProvider`` row whose adapter answers the judge call.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model": {
                      "description": "Model name as published by the provider's ``models`` list. Validated at policy create/update time.",
                      "minLength": 1,
                      "title": "Model",
                      "type": "string"
                    },
                    "prompt": {
                      "description": "System prompt the judge receives. The call context is appended as the user message.",
                      "maxLength": 16000,
                      "minLength": 1,
                      "title": "Prompt",
                      "type": "string"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model",
                    "prompt"
                  ],
                  "title": "LlmApprovalConfig",
                  "type": "object"
                },
                "PolicyApprovalConfig": {
                  "description": "``type=policy`` \u2014 evaluate a Rego policy against the call context.",
                  "properties": {
                    "type": {
                      "const": "policy",
                      "default": "policy",
                      "title": "Type",
                      "type": "string"
                    },
                    "policy": {
                      "description": "Rego policy source. Must evaluate to a result document with a boolean ``required`` key and an optional string ``reason`` key.",
                      "minLength": 1,
                      "title": "Policy",
                      "type": "string"
                    }
                  },
                  "required": [
                    "policy"
                  ],
                  "title": "PolicyApprovalConfig",
                  "type": "object"
                },
                "RequiredApprovalConfig": {
                  "description": "``type=required`` \u2014 gate trips unconditionally.",
                  "properties": {
                    "type": {
                      "const": "required",
                      "default": "required",
                      "title": "Type",
                      "type": "string"
                    }
                  },
                  "title": "RequiredApprovalConfig",
                  "type": "object"
                }
              },
              "description": "Operator-configured approval gate for one ``(toolset_id, tool_name)``.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "toolset_id": {
                  "description": "Toolset id the policy applies to. May be a reserved internal toolset (``_system``, ``_workspaces``, ``_misc``, ``_search``, ``web``) or a user-created Toolset row's id.",
                  "minLength": 1,
                  "title": "Toolset Id",
                  "type": "string"
                },
                "tool_name": {
                  "description": "Bare tool name as registered in the provider catalogue.",
                  "minLength": 1,
                  "title": "Tool Name",
                  "type": "string"
                },
                "enabled": {
                  "default": true,
                  "description": "When false the policy is stored but skipped at evaluation time.",
                  "title": "Enabled",
                  "type": "boolean"
                },
                "approval": {
                  "description": "Approval strategy and its config.",
                  "discriminator": {
                    "mapping": {
                      "llm": "#/$defs/LlmApprovalConfig",
                      "policy": "#/$defs/PolicyApprovalConfig",
                      "required": "#/$defs/RequiredApprovalConfig"
                    },
                    "propertyName": "type"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/RequiredApprovalConfig"
                    },
                    {
                      "$ref": "#/$defs/PolicyApprovalConfig"
                    },
                    {
                      "$ref": "#/$defs/LlmApprovalConfig"
                    }
                  ],
                  "title": "Approval"
                },
                "timeout_seconds": {
                  "anyOf": [
                    {
                      "exclusiveMinimum": 0.0,
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional per-policy timeout. None falls back to the global yield cap.",
                  "title": "Timeout Seconds"
                }
              },
              "required": [
                "id",
                "toolset_id",
                "tool_name",
                "approval"
              ],
              "title": "ToolApprovalPolicy",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_tool_approval_policy",
        "description": "Replace an existing ToolApprovalPolicy. Pass ``id`` (the row to update) and ``entity`` (the full new ToolApprovalPolicy body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "LlmApprovalConfig": {
                  "description": "``type=llm`` \u2014 ask an LLM judge for the approval verdict.",
                  "properties": {
                    "type": {
                      "const": "llm",
                      "default": "llm",
                      "title": "Type",
                      "type": "string"
                    },
                    "provider_id": {
                      "description": "Id of an existing ``LLMProvider`` row whose adapter answers the judge call.",
                      "minLength": 1,
                      "title": "Provider Id",
                      "type": "string"
                    },
                    "model": {
                      "description": "Model name as published by the provider's ``models`` list. Validated at policy create/update time.",
                      "minLength": 1,
                      "title": "Model",
                      "type": "string"
                    },
                    "prompt": {
                      "description": "System prompt the judge receives. The call context is appended as the user message.",
                      "maxLength": 16000,
                      "minLength": 1,
                      "title": "Prompt",
                      "type": "string"
                    }
                  },
                  "required": [
                    "provider_id",
                    "model",
                    "prompt"
                  ],
                  "title": "LlmApprovalConfig",
                  "type": "object"
                },
                "PolicyApprovalConfig": {
                  "description": "``type=policy`` \u2014 evaluate a Rego policy against the call context.",
                  "properties": {
                    "type": {
                      "const": "policy",
                      "default": "policy",
                      "title": "Type",
                      "type": "string"
                    },
                    "policy": {
                      "description": "Rego policy source. Must evaluate to a result document with a boolean ``required`` key and an optional string ``reason`` key.",
                      "minLength": 1,
                      "title": "Policy",
                      "type": "string"
                    }
                  },
                  "required": [
                    "policy"
                  ],
                  "title": "PolicyApprovalConfig",
                  "type": "object"
                },
                "RequiredApprovalConfig": {
                  "description": "``type=required`` \u2014 gate trips unconditionally.",
                  "properties": {
                    "type": {
                      "const": "required",
                      "default": "required",
                      "title": "Type",
                      "type": "string"
                    }
                  },
                  "title": "RequiredApprovalConfig",
                  "type": "object"
                }
              },
              "description": "Operator-configured approval gate for one ``(toolset_id, tool_name)``.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "toolset_id": {
                  "description": "Toolset id the policy applies to. May be a reserved internal toolset (``_system``, ``_workspaces``, ``_misc``, ``_search``, ``web``) or a user-created Toolset row's id.",
                  "minLength": 1,
                  "title": "Toolset Id",
                  "type": "string"
                },
                "tool_name": {
                  "description": "Bare tool name as registered in the provider catalogue.",
                  "minLength": 1,
                  "title": "Tool Name",
                  "type": "string"
                },
                "enabled": {
                  "default": true,
                  "description": "When false the policy is stored but skipped at evaluation time.",
                  "title": "Enabled",
                  "type": "boolean"
                },
                "approval": {
                  "description": "Approval strategy and its config.",
                  "discriminator": {
                    "mapping": {
                      "llm": "#/$defs/LlmApprovalConfig",
                      "policy": "#/$defs/PolicyApprovalConfig",
                      "required": "#/$defs/RequiredApprovalConfig"
                    },
                    "propertyName": "type"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/RequiredApprovalConfig"
                    },
                    {
                      "$ref": "#/$defs/PolicyApprovalConfig"
                    },
                    {
                      "$ref": "#/$defs/LlmApprovalConfig"
                    }
                  ],
                  "title": "Approval"
                },
                "timeout_seconds": {
                  "anyOf": [
                    {
                      "exclusiveMinimum": 0.0,
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional per-policy timeout. None falls back to the global yield cap.",
                  "title": "Timeout Seconds"
                }
              },
              "required": [
                "id",
                "toolset_id",
                "tool_name",
                "approval"
              ],
              "title": "ToolApprovalPolicy",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_tool_approval_policy",
        "description": "Delete a ToolApprovalPolicy by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_tool_approval_policies",
        "description": "Find ToolApprovalPolicy entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_channel_providers",
        "description": "List ChannelProvider entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_channel_provider",
        "description": "Fetch one ChannelProvider by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_channel_provider",
        "description": "Create a new ChannelProvider. Body shape is the full ChannelProvider schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "$defs": {
                "ChannelProviderType": {
                  "enum": [
                    "slack",
                    "telegram",
                    "discord"
                  ],
                  "title": "ChannelProviderType",
                  "type": "string"
                },
                "DiscordChannelProviderConfig": {
                  "description": "One Discord bot token + intent toggle.",
                  "properties": {
                    "bot_token": {
                      "description": "Discord bot token from the Developer Portal. Do NOT include the 'Bot ' prefix; the adapter adds it.",
                      "format": "password",
                      "title": "Bot Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "enable_dms": {
                      "default": true,
                      "description": "Request dm_messages intent so the bot can read DM channels. Turn off for guild-only deployments.",
                      "title": "Enable Dms",
                      "type": "boolean"
                    }
                  },
                  "required": [
                    "bot_token"
                  ],
                  "title": "DiscordChannelProviderConfig",
                  "type": "object"
                },
                "SlackChannelProviderConfig": {
                  "description": "Credentials for one Slack workspace (Slack-team).",
                  "properties": {
                    "app_token": {
                      "description": "App-level token (xapp-\u2026) required for Socket Mode. Generated in the Slack app's Basic Information panel.",
                      "format": "password",
                      "title": "App Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "bot_token": {
                      "description": "Bot OAuth token (xoxb-\u2026) used for chat.postMessage, conversations.history, etc.",
                      "format": "password",
                      "title": "Bot Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "signing_secret": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional. Unused in Socket Mode (signing only matters for HTTP event delivery).",
                      "title": "Signing Secret"
                    }
                  },
                  "required": [
                    "app_token",
                    "bot_token"
                  ],
                  "title": "SlackChannelProviderConfig",
                  "type": "object"
                },
                "TelegramChannelProviderConfig": {
                  "description": "One Telegram bot token + polling knob.",
                  "properties": {
                    "bot_token": {
                      "description": "Telegram bot token in '123456:ABCDEF\u2026' shape (generated by @BotFather).",
                      "format": "password",
                      "title": "Bot Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "poll_timeout_seconds": {
                      "default": 25,
                      "description": "Long-poll timeout per getUpdates call. Telegram caps at 50; 25 balances latency vs connection churn.",
                      "maximum": 60,
                      "minimum": 1,
                      "title": "Poll Timeout Seconds",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "bot_token"
                  ],
                  "title": "TelegramChannelProviderConfig",
                  "type": "object"
                }
              },
              "description": "A configured messaging-platform credential set.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/ChannelProviderType"
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SlackChannelProviderConfig"
                    },
                    {
                      "$ref": "#/$defs/TelegramChannelProviderConfig"
                    },
                    {
                      "$ref": "#/$defs/DiscordChannelProviderConfig"
                    }
                  ],
                  "description": "Platform-specific config; must match parent provider.",
                  "title": "Config"
                }
              },
              "required": [
                "id",
                "provider",
                "config"
              ],
              "title": "ChannelProvider",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_channel_provider",
        "description": "Replace an existing ChannelProvider. Pass ``id`` (the row to update) and ``entity`` (the full new ChannelProvider body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "$defs": {
                "ChannelProviderType": {
                  "enum": [
                    "slack",
                    "telegram",
                    "discord"
                  ],
                  "title": "ChannelProviderType",
                  "type": "string"
                },
                "DiscordChannelProviderConfig": {
                  "description": "One Discord bot token + intent toggle.",
                  "properties": {
                    "bot_token": {
                      "description": "Discord bot token from the Developer Portal. Do NOT include the 'Bot ' prefix; the adapter adds it.",
                      "format": "password",
                      "title": "Bot Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "enable_dms": {
                      "default": true,
                      "description": "Request dm_messages intent so the bot can read DM channels. Turn off for guild-only deployments.",
                      "title": "Enable Dms",
                      "type": "boolean"
                    }
                  },
                  "required": [
                    "bot_token"
                  ],
                  "title": "DiscordChannelProviderConfig",
                  "type": "object"
                },
                "SlackChannelProviderConfig": {
                  "description": "Credentials for one Slack workspace (Slack-team).",
                  "properties": {
                    "app_token": {
                      "description": "App-level token (xapp-\u2026) required for Socket Mode. Generated in the Slack app's Basic Information panel.",
                      "format": "password",
                      "title": "App Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "bot_token": {
                      "description": "Bot OAuth token (xoxb-\u2026) used for chat.postMessage, conversations.history, etc.",
                      "format": "password",
                      "title": "Bot Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "signing_secret": {
                      "anyOf": [
                        {
                          "format": "password",
                          "type": "string",
                          "writeOnly": true
                        },
                        {
                          "type": "null"
                        }
                      ],
                      "default": null,
                      "description": "Optional. Unused in Socket Mode (signing only matters for HTTP event delivery).",
                      "title": "Signing Secret"
                    }
                  },
                  "required": [
                    "app_token",
                    "bot_token"
                  ],
                  "title": "SlackChannelProviderConfig",
                  "type": "object"
                },
                "TelegramChannelProviderConfig": {
                  "description": "One Telegram bot token + polling knob.",
                  "properties": {
                    "bot_token": {
                      "description": "Telegram bot token in '123456:ABCDEF\u2026' shape (generated by @BotFather).",
                      "format": "password",
                      "title": "Bot Token",
                      "type": "string",
                      "writeOnly": true
                    },
                    "poll_timeout_seconds": {
                      "default": 25,
                      "description": "Long-poll timeout per getUpdates call. Telegram caps at 50; 25 balances latency vs connection churn.",
                      "maximum": 60,
                      "minimum": 1,
                      "title": "Poll Timeout Seconds",
                      "type": "integer"
                    }
                  },
                  "required": [
                    "bot_token"
                  ],
                  "title": "TelegramChannelProviderConfig",
                  "type": "object"
                }
              },
              "description": "A configured messaging-platform credential set.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/ChannelProviderType"
                },
                "config": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/SlackChannelProviderConfig"
                    },
                    {
                      "$ref": "#/$defs/TelegramChannelProviderConfig"
                    },
                    {
                      "$ref": "#/$defs/DiscordChannelProviderConfig"
                    }
                  ],
                  "description": "Platform-specific config; must match parent provider.",
                  "title": "Config"
                }
              },
              "required": [
                "id",
                "provider",
                "config"
              ],
              "title": "ChannelProvider",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_channel_provider",
        "description": "Delete a ChannelProvider by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_channel_providers",
        "description": "Find ChannelProvider entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_channels",
        "description": "List Channel entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_channel",
        "description": "Fetch one Channel by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_channel",
        "description": "Create a new Channel. Body shape is the full Channel schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "description": "One conversational target within a ChannelProvider.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider_id": {
                  "minLength": 1,
                  "title": "Provider Id",
                  "type": "string"
                },
                "external_id": {
                  "minLength": 1,
                  "title": "External Id",
                  "type": "string"
                },
                "label": {
                  "default": "",
                  "maxLength": 200,
                  "title": "Label",
                  "type": "string"
                }
              },
              "required": [
                "id",
                "provider_id",
                "external_id"
              ],
              "title": "Channel",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_channel",
        "description": "Replace an existing Channel. Pass ``id`` (the row to update) and ``entity`` (the full new Channel body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "description": "One conversational target within a ChannelProvider.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider_id": {
                  "minLength": 1,
                  "title": "Provider Id",
                  "type": "string"
                },
                "external_id": {
                  "minLength": 1,
                  "title": "External Id",
                  "type": "string"
                },
                "label": {
                  "default": "",
                  "maxLength": 200,
                  "title": "Label",
                  "type": "string"
                }
              },
              "required": [
                "id",
                "provider_id",
                "external_id"
              ],
              "title": "Channel",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_channel",
        "description": "Delete a Channel by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_channels",
        "description": "Find Channel entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_workspace_channel_associations",
        "description": "List WorkspaceChannelAssociation entities with pagination. Supply ``offset`` (default 0) OR ``cursor`` (mutually exclusive). ``limit`` defaults to 20, max 200. Optional ``order_by`` sort spec like ``['id:asc']``. Returns a page object with ``items`` (the entities), ``length`` (count this page), ``total`` (full set count, offset mode only), and ``next_cursor`` (cursor mode only). On invalid arguments returns ``is_error=true``.",
        "input_schema": {
          "description": "Page selector \u2014 supply EITHER ``offset`` OR ``cursor``, never both.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_workspace_channel_association",
        "description": "Fetch one WorkspaceChannelAssociation by its ``id``. Returns the full entity object on success. Returns ``is_error=true`` with ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Look up an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_GetByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__create_workspace_channel_association",
        "description": "Create a new WorkspaceChannelAssociation. Body shape is the full WorkspaceChannelAssociation schema (see ``entity`` parameter). The entity's ``id`` field is the wire-level identifier \u2014 must be unique across this entity type. Returns the created entity (echoes back the body). On duplicate id returns ``is_error=true`` with ``type=conflict``; on schema violation returns ``type=validation-error``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "entity": {
              "description": "Many-to-many link between a workspace and a channel.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "workspace_id": {
                  "minLength": 1,
                  "title": "Workspace Id",
                  "type": "string"
                },
                "channel_id": {
                  "minLength": 1,
                  "title": "Channel Id",
                  "type": "string"
                },
                "enabled": {
                  "default": true,
                  "title": "Enabled",
                  "type": "boolean"
                },
                "forward_ask_user": {
                  "default": true,
                  "title": "Forward Ask User",
                  "type": "boolean"
                },
                "forward_tool_approval": {
                  "default": true,
                  "title": "Forward Tool Approval",
                  "type": "boolean"
                }
              },
              "required": [
                "id",
                "workspace_id",
                "channel_id"
              ],
              "title": "WorkspaceChannelAssociation",
              "type": "object"
            }
          },
          "required": [
            "entity"
          ]
        }
      },
      {
        "id": "system__update_workspace_channel_association",
        "description": "Replace an existing WorkspaceChannelAssociation. Pass ``id`` (the row to update) and ``entity`` (the full new WorkspaceChannelAssociation body \u2014 replaces, does not patch). The body's ``id`` must equal the path ``id``. Returns the updated entity. Mutating provider/toolset/vector-store rows cascades to invalidate the matching cached adapter. On unknown id returns ``type=not-found``; on id mismatch returns ``type=conflict``.",
        "input_schema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Id of the row to replace."
            },
            "entity": {
              "description": "Many-to-many link between a workspace and a channel.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "workspace_id": {
                  "minLength": 1,
                  "title": "Workspace Id",
                  "type": "string"
                },
                "channel_id": {
                  "minLength": 1,
                  "title": "Channel Id",
                  "type": "string"
                },
                "enabled": {
                  "default": true,
                  "title": "Enabled",
                  "type": "boolean"
                },
                "forward_ask_user": {
                  "default": true,
                  "title": "Forward Ask User",
                  "type": "boolean"
                },
                "forward_tool_approval": {
                  "default": true,
                  "title": "Forward Tool Approval",
                  "type": "boolean"
                }
              },
              "required": [
                "id",
                "workspace_id",
                "channel_id"
              ],
              "title": "WorkspaceChannelAssociation",
              "type": "object"
            }
          },
          "required": [
            "id",
            "entity"
          ]
        }
      },
      {
        "id": "system__delete_workspace_channel_association",
        "description": "Delete a WorkspaceChannelAssociation by ``id``. Cascades to invalidate the cached adapter for provider/toolset/vector-store rows. Returns ``{'deleted': true, 'id': '...'}`` on success. Returns ``type=not-found`` if no row has that id.",
        "input_schema": {
          "description": "Delete an entity by its id.",
          "properties": {
            "id": {
              "description": "Entity id (case-sensitive).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_DeleteByIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_workspace_channel_associations",
        "description": "Find WorkspaceChannelAssociation entities matching a predicate tree. Same pagination + ordering as ``list``. Pass ``predicate=null`` to match everything (equivalent to list). The predicate is a binary tree \u2014 see the ``predicate`` parameter description for the full operator and operand shape.",
        "input_schema": {
          "description": "Predicate-based search arguments.",
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "predicate": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Predicate tree (see :class:`primer.model.storage.Predicate`). Binary tree of comparison/logical ops. Each node is ``{kind:'predicate', left:..., op:..., right:...}``; leaf field references are ``{kind:'field', name:'...'}`` and literal values are ``{kind:'value', value:...}``. Operators: =, !=, ~=, >, <, >=, <=, in, and, or. Pass ``null`` to find all rows (equivalent to list).",
              "title": "Predicate"
            }
          },
          "title": "_FindArgs",
          "type": "object"
        }
      },
      {
        "id": "system__invalidate_semantic_search_provider",
        "description": "Expire the cached VectorStoreProvider adapter for a SemanticSearchProvider row. Call this after updating the provider row so the next search request rebuilds the adapter from the new config. Safe to call even if no cached instance exists (no-op). Returns ``{'invalidated': true, 'id': '...'}``.",
        "input_schema": {
          "description": "Force-expire the cached VectorStoreProvider for one SSP row.",
          "properties": {
            "id": {
              "description": "Id of the SemanticSearchProvider row whose cached VectorStoreProvider instance should be evicted. The next call that needs the backend will re-resolve the row from storage and reconstruct the adapter.",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_InvalidateSSPArgs",
          "type": "object"
        }
      },
      {
        "id": "system__fetch_llm_provider_models",
        "description": "Live model list reported by the LLM provider. Differs from the configured ``models`` field on the provider row, which is what the application is *permitted* to send. Use this to discover model identifiers the provider currently exposes.",
        "input_schema": {
          "description": "Reference to a provider row by id.",
          "properties": {
            "provider_id": {
              "description": "Id of the provider row to query.",
              "minLength": 1,
              "title": "Provider Id",
              "type": "string"
            }
          },
          "required": [
            "provider_id"
          ],
          "title": "_ProviderIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__fetch_embedding_provider_models",
        "description": "Live model list reported by the embedding provider. Differs from the configured ``models`` field on the provider row, which is what the application is *permitted* to send. Use this to discover model identifiers the provider currently exposes.",
        "input_schema": {
          "description": "Reference to a provider row by id.",
          "properties": {
            "provider_id": {
              "description": "Id of the provider row to query.",
              "minLength": 1,
              "title": "Provider Id",
              "type": "string"
            }
          },
          "required": [
            "provider_id"
          ],
          "title": "_ProviderIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__fetch_cross_encoder_provider_models",
        "description": "Live model list reported by the cross-encoder provider. Differs from the configured ``models`` field on the provider row, which is what the application is *permitted* to send. Use this to discover model identifiers the provider currently exposes.",
        "input_schema": {
          "description": "Reference to a provider row by id.",
          "properties": {
            "provider_id": {
              "description": "Id of the provider row to query.",
              "minLength": 1,
              "title": "Provider Id",
              "type": "string"
            }
          },
          "required": [
            "provider_id"
          ],
          "title": "_ProviderIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_toolset_tools",
        "description": "Enumerate tools currently exposed by a toolset. Calls the live provider so OAuth-protected MCP toolsets may raise an auth-required error (returned as ``is_error=true`` ``type=auth-required``). Returns ``{'tools': [Tool, ...]}`` \u2014 each Tool carries its id, description, schema, and originating toolset_id.",
        "input_schema": {
          "properties": {
            "toolset_id": {
              "description": "Id of the toolset to query.",
              "minLength": 1,
              "title": "Toolset Id",
              "type": "string"
            },
            "principal": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Optional end-user identity passed through to the toolset. Required for OAuth-protected MCP toolsets if the cached token is bound to a specific user.",
              "title": "Principal"
            }
          },
          "required": [
            "toolset_id"
          ],
          "title": "_ToolsetIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__call_tool",
        "description": "Meta-dispatch: invoke any tool from any toolset by id. Useful when you have discovered a tool via ``list_toolset_tools`` and want to execute it without going through the dedicated agent toolset wiring. The dispatched tool's own ``output`` and ``is_error`` are passed through unchanged so the model can act on them.",
        "input_schema": {
          "properties": {
            "toolset_id": {
              "description": "Toolset id.",
              "minLength": 1,
              "title": "Toolset Id",
              "type": "string"
            },
            "tool_name": {
              "description": "Tool wire id.",
              "minLength": 1,
              "title": "Tool Name",
              "type": "string"
            },
            "arguments": {
              "additionalProperties": true,
              "description": "Argument object for the call.",
              "title": "Arguments",
              "type": "object"
            },
            "principal": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Optional end-user identity. Required for OAuth-protected MCP toolsets if per-user token caching is in effect.",
              "title": "Principal"
            }
          },
          "required": [
            "toolset_id",
            "tool_name"
          ],
          "title": "_CallToolArgs",
          "type": "object"
        }
      },
      {
        "id": "system__list_collection_documents",
        "description": "List documents belonging to a collection. Server-side filter on ``Document.collection_id == collection_id``. Same pagination contract as the entity ``list`` tools. Returns ``type=not-found`` if the collection itself does not exist.",
        "input_schema": {
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "collection_id": {
              "description": "Collection id.",
              "minLength": 1,
              "title": "Collection Id",
              "type": "string"
            }
          },
          "required": [
            "collection_id"
          ],
          "title": "_CollectionDocumentsListArgs",
          "type": "object"
        }
      },
      {
        "id": "system__find_collection_documents_by_meta",
        "description": "Find documents in a collection by metadata equality. ``meta_filter`` is a flat dict of ``{key: equality_value, ...}`` translated server-side to ``meta.key == value`` predicates joined with AND. For richer predicates (range, OR, IN) use the generic ``find_documents`` tool with a hand-built Predicate.",
        "input_schema": {
          "properties": {
            "limit": {
              "default": 20,
              "description": "Maximum number of items returned (1-200, default 20).",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Offset-based: number of items to skip. Mutually exclusive with ``cursor``. If both are omitted, defaults to offset 0.",
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Cursor-based: opaque cursor returned as ``next_cursor`` by a prior list call. Mutually exclusive with ``offset``.",
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Sort spec, e.g. ``['id:asc', 'name:desc']``. Each entry is ``field:direction`` where direction is ``asc`` or ``desc``. Direction defaults to ``asc`` if omitted.",
              "title": "Order By"
            },
            "collection_id": {
              "description": "Collection id.",
              "minLength": 1,
              "title": "Collection Id",
              "type": "string"
            },
            "meta_filter": {
              "additionalProperties": true,
              "description": "Key/value pairs to match against ``Document.meta``. Equality only \u2014 for richer predicates use ``find_documents``.",
              "title": "Meta Filter",
              "type": "object"
            }
          },
          "required": [
            "collection_id"
          ],
          "title": "_CollectionFindByMetaArgs",
          "type": "object"
        }
      },
      {
        "id": "system__search_collection",
        "description": "Semantic / hybrid search over a collection. STUB: returns ``is_error=true`` ``type=not-implemented`` until the SearchService pipeline lands. Use ``find_collection_documents_by_meta`` for metadata filtering in the meantime.",
        "input_schema": {
          "properties": {
            "collection_id": {
              "description": "Collection id.",
              "minLength": 1,
              "title": "Collection Id",
              "type": "string"
            },
            "query": {
              "description": "Free-text query string.",
              "minLength": 1,
              "title": "Query",
              "type": "string"
            },
            "top_k": {
              "default": 10,
              "description": "Maximum number of hits to return.",
              "maximum": 100,
              "minimum": 1,
              "title": "Top K",
              "type": "integer"
            }
          },
          "required": [
            "collection_id",
            "query"
          ],
          "title": "_CollectionSearchArgs",
          "type": "object"
        }
      },
      {
        "id": "system__refresh_collection",
        "description": "Re-embed every document in the collection (e.g. after swapping the embedder). STUB: returns ``is_error=true`` ``type=not-implemented`` until the ingestion pipeline lands.",
        "input_schema": {
          "properties": {
            "collection_id": {
              "description": "Collection id.",
              "minLength": 1,
              "title": "Collection Id",
              "type": "string"
            }
          },
          "required": [
            "collection_id"
          ],
          "title": "_CollectionIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__get_document_content",
        "description": "Fetch a document's text content. Looks up ``Document.meta['content']`` (where ``put_document`` stores the raw text). Returns ``{id, collection_id, name, content}``. Empty string content is normal for documents that were created without going through ``put_document``.",
        "input_schema": {
          "properties": {
            "document_id": {
              "description": "Document id.",
              "minLength": 1,
              "title": "Document Id",
              "type": "string"
            }
          },
          "required": [
            "document_id"
          ],
          "title": "_DocumentIdArgs",
          "type": "object"
        }
      },
      {
        "id": "system__put_document",
        "description": "Ingest a document into a collection (upsert). Synthesises a ``Document`` row, stores ``content`` under ``meta['content']``, and creates the row if new or updates it if the id already exists. Until the chunking + embedding pipeline lands, this does NOT vectorise the document \u2014 search will not see it. Use ``create_document`` / ``update_document`` for the raw row-level CRUD without content semantics.",
        "input_schema": {
          "description": "Ingest a document with content into a collection.\n\nUntil the docling/embedding pipeline lands, ``content`` is stored\nverbatim under ``Document.meta['content']`` so it can be retrieved\nby ``get_document_content``. Once ingestion lands, content will\nflow through chunking + embedding before vector storage.",
          "properties": {
            "id": {
              "description": "Document id (unique within the collection).",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            },
            "collection_id": {
              "description": "Parent collection id.",
              "minLength": 1,
              "title": "Collection Id",
              "type": "string"
            },
            "name": {
              "description": "Human-readable document name.",
              "minLength": 1,
              "title": "Name",
              "type": "string"
            },
            "content": {
              "description": "Raw text content of the document. Stored under ``meta['content']`` until the chunking pipeline lands.",
              "minLength": 1,
              "title": "Content",
              "type": "string"
            },
            "meta": {
              "additionalProperties": true,
              "description": "Application-defined metadata. Merged with the synthesised ``content`` key (your value wins on conflict).",
              "title": "Meta",
              "type": "object"
            }
          },
          "required": [
            "id",
            "collection_id",
            "name",
            "content"
          ],
          "title": "_PutDocumentArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__list_workspace_providers",
        "description": "List configured WorkspaceProviders with pagination. Same limit/offset/cursor/order_by contract as the rest of the system toolset's list_* tools. Returns ``items``, ``length``, ``total`` (offset mode), ``next_cursor`` (cursor mode).",
        "input_schema": {
          "properties": {
            "limit": {
              "default": 20,
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__get_workspace_provider",
        "description": "Fetch one WorkspaceProvider by id. Returns the provider row with its discriminated config. ``type=not-found`` when missing.",
        "input_schema": {
          "properties": {
            "id": {
              "description": "Entity id.",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_IdArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__create_workspace_provider",
        "description": "Create a new WorkspaceProvider. Body shape is the full WorkspaceProvider schema with ``provider`` discriminator and matching ``config``. The Update operation is intentionally absent \u2014 to change a provider's config, delete and recreate.",
        "input_schema": {
          "$defs": {
            "ContainerConnectionRemote": {
              "properties": {
                "kind": {
                  "const": "remote",
                  "default": "remote",
                  "title": "Kind",
                  "type": "string"
                },
                "url": {
                  "description": "Remote runtime endpoint, e.g. tcp://docker:2375.",
                  "title": "Url",
                  "type": "string"
                },
                "tls_ca": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "PEM CA cert for mTLS.",
                  "title": "Tls Ca"
                },
                "tls_cert": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "PEM client cert for mTLS.",
                  "title": "Tls Cert"
                },
                "tls_key": {
                  "anyOf": [
                    {
                      "format": "password",
                      "type": "string",
                      "writeOnly": true
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "PEM client key for mTLS.",
                  "title": "Tls Key"
                }
              },
              "required": [
                "url"
              ],
              "title": "ContainerConnectionRemote",
              "type": "object"
            },
            "ContainerConnectionSocket": {
              "properties": {
                "kind": {
                  "const": "socket",
                  "default": "socket",
                  "title": "Kind",
                  "type": "string"
                },
                "socket_path": {
                  "description": "Unix socket to the container runtime.",
                  "title": "Socket Path",
                  "type": "string"
                }
              },
              "required": [
                "socket_path"
              ],
              "title": "ContainerConnectionSocket",
              "type": "object"
            },
            "ContainerReachabilityBridge": {
              "properties": {
                "kind": {
                  "const": "bridge_network",
                  "default": "bridge_network",
                  "title": "Kind",
                  "type": "string"
                },
                "network_name": {
                  "description": "Docker network shared by the platform container and workspace containers. Workspaces are reached via the container hostname; no port mapping is needed.",
                  "title": "Network Name",
                  "type": "string"
                }
              },
              "required": [
                "network_name"
              ],
              "title": "ContainerReachabilityBridge",
              "type": "object"
            },
            "ContainerReachabilityHostPort": {
              "properties": {
                "kind": {
                  "const": "host_port",
                  "default": "host_port",
                  "title": "Kind",
                  "type": "string"
                },
                "bind_host": {
                  "default": "127.0.0.1",
                  "description": "Host interface the container port maps to. 127.0.0.1 keeps it loopback-only.",
                  "title": "Bind Host",
                  "type": "string"
                }
              },
              "title": "ContainerReachabilityHostPort",
              "type": "object"
            },
            "ContainerWorkspaceConfig": {
              "additionalProperties": false,
              "description": "Container provider \u2014 connection to the runtime + reachability mode only.\n\nImage, entrypoint, mounts, resource limits, etc. moved to the template\nin this redesign (see ContainerTemplateConfig).",
              "properties": {
                "kind": {
                  "const": "container",
                  "default": "container",
                  "title": "Kind",
                  "type": "string"
                },
                "runtime": {
                  "description": "Container runtime to use.",
                  "enum": [
                    "docker",
                    "podman",
                    "containerd"
                  ],
                  "title": "Runtime",
                  "type": "string"
                },
                "connection": {
                  "description": "How the platform reaches the runtime API.",
                  "discriminator": {
                    "mapping": {
                      "remote": "#/$defs/ContainerConnectionRemote",
                      "socket": "#/$defs/ContainerConnectionSocket"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/ContainerConnectionSocket"
                    },
                    {
                      "$ref": "#/$defs/ContainerConnectionRemote"
                    }
                  ],
                  "title": "Connection"
                },
                "reachability": {
                  "description": "How the platform reaches primer-runtime inside each workspace.",
                  "discriminator": {
                    "mapping": {
                      "bridge_network": "#/$defs/ContainerReachabilityBridge",
                      "host_port": "#/$defs/ContainerReachabilityHostPort"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/ContainerReachabilityHostPort"
                    },
                    {
                      "$ref": "#/$defs/ContainerReachabilityBridge"
                    }
                  ],
                  "title": "Reachability"
                },
                "image_pull_secrets": {
                  "description": "Optional list of registry-auth secret refs.",
                  "items": {
                    "type": "string"
                  },
                  "title": "Image Pull Secrets",
                  "type": "array"
                }
              },
              "required": [
                "runtime",
                "connection",
                "reachability"
              ],
              "title": "ContainerWorkspaceConfig",
              "type": "object"
            },
            "K8sConnectionInCluster": {
              "description": "In-cluster service-account auth \u2014 no extra fields needed.",
              "properties": {
                "kind": {
                  "const": "in_cluster",
                  "default": "in_cluster",
                  "description": "Discriminator tag identifying this as in-cluster auth.",
                  "title": "Kind",
                  "type": "string"
                }
              },
              "title": "K8sConnectionInCluster",
              "type": "object"
            },
            "K8sConnectionKubeconfig": {
              "description": "Kubeconfig-based auth \u2014 path + optional context.",
              "properties": {
                "kind": {
                  "const": "kubeconfig",
                  "default": "kubeconfig",
                  "description": "Discriminator tag identifying this as kubeconfig auth.",
                  "title": "Kind",
                  "type": "string"
                },
                "path": {
                  "description": "Path to the kubeconfig file (tilde-expanded).",
                  "title": "Path",
                  "type": "string"
                },
                "context": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Named context to select; null uses the file's current-context.",
                  "title": "Context"
                }
              },
              "required": [
                "path"
              ],
              "title": "K8sConnectionKubeconfig",
              "type": "object"
            },
            "K8sConnectionServiceAccountToken": {
              "description": "Direct service-account-token auth for out-of-cluster setups.",
              "properties": {
                "kind": {
                  "const": "service_account_token",
                  "default": "service_account_token",
                  "description": "Discriminator tag identifying this as service-account-token auth.",
                  "title": "Kind",
                  "type": "string"
                },
                "apiserver_url": {
                  "description": "https://<host>:<port> apiserver URL.",
                  "title": "Apiserver Url",
                  "type": "string"
                },
                "ca_data": {
                  "description": "PEM cluster CA cert (multi-line string).",
                  "title": "Ca Data",
                  "type": "string"
                },
                "token": {
                  "description": "Bearer token for the service account.",
                  "format": "password",
                  "title": "Token",
                  "type": "string",
                  "writeOnly": true
                },
                "namespace": {
                  "default": "default",
                  "description": "Namespace claimed by the token; informational.",
                  "title": "Namespace",
                  "type": "string"
                }
              },
              "required": [
                "apiserver_url",
                "ca_data",
                "token"
              ],
              "title": "K8sConnectionServiceAccountToken",
              "type": "object"
            },
            "K8sGatewayParentRef": {
              "additionalProperties": false,
              "description": "Reference to a pre-created Gateway the per-workspace HTTPRoute attaches to.",
              "properties": {
                "name": {
                  "description": "Gateway resource name.",
                  "title": "Name",
                  "type": "string"
                },
                "namespace": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Gateway namespace; null means the workspace namespace.",
                  "title": "Namespace"
                },
                "section_name": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional listener (sectionName) on the Gateway to bind to.",
                  "title": "Section Name"
                }
              },
              "required": [
                "name"
              ],
              "title": "K8sGatewayParentRef",
              "type": "object"
            },
            "K8sGatewayRoutingHostname": {
              "additionalProperties": false,
              "description": "Route by per-workspace hostname (Host header). Needs wildcard DNS.",
              "properties": {
                "kind": {
                  "const": "hostname",
                  "default": "hostname",
                  "description": "Discriminator: per-workspace hostname routing.",
                  "title": "Kind",
                  "type": "string"
                },
                "hostname_template": {
                  "description": "Hostname with {workspace_id}, e.g. '{workspace_id}.ws.local'.",
                  "title": "Hostname Template",
                  "type": "string"
                }
              },
              "required": [
                "hostname_template"
              ],
              "title": "K8sGatewayRoutingHostname",
              "type": "object"
            },
            "K8sGatewayRoutingPath": {
              "additionalProperties": false,
              "description": "Route by shared hostname + path prefix, rewritten to '/'.",
              "properties": {
                "kind": {
                  "const": "path_prefix",
                  "default": "path_prefix",
                  "description": "Discriminator: shared-host path-prefix routing.",
                  "title": "Kind",
                  "type": "string"
                },
                "hostname": {
                  "description": "Shared host, e.g. 'ws.local'.",
                  "title": "Hostname",
                  "type": "string"
                },
                "path_template": {
                  "default": "/ws/{workspace_id}",
                  "description": "Path prefix with {workspace_id}; stripped to '/' via URLRewrite.",
                  "title": "Path Template",
                  "type": "string"
                }
              },
              "required": [
                "hostname"
              ],
              "title": "K8sGatewayRoutingPath",
              "type": "object"
            },
            "K8sReachabilityGateway": {
              "additionalProperties": false,
              "description": "Platform OUT of cluster -- backend creates a Gateway API HTTPRoute per\nworkspace pod. The operator pre-creates the Gateway (and GatewayClass);\nthis config references it.",
              "properties": {
                "kind": {
                  "const": "gateway_httproute",
                  "default": "gateway_httproute",
                  "description": "Discriminator tag identifying this as gateway-httproute reachability.",
                  "title": "Kind",
                  "type": "string"
                },
                "gateway": {
                  "$ref": "#/$defs/K8sGatewayParentRef",
                  "description": "The Gateway the per-workspace HTTPRoute attaches to."
                },
                "routing": {
                  "description": "Routing strategy: hostname or path_prefix.",
                  "discriminator": {
                    "mapping": {
                      "hostname": "#/$defs/K8sGatewayRoutingHostname",
                      "path_prefix": "#/$defs/K8sGatewayRoutingPath"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/K8sGatewayRoutingHostname"
                    },
                    {
                      "$ref": "#/$defs/K8sGatewayRoutingPath"
                    }
                  ],
                  "title": "Routing"
                },
                "scheme": {
                  "default": "ws",
                  "description": "WebSocket scheme the platform dials.",
                  "enum": [
                    "ws",
                    "wss"
                  ],
                  "title": "Scheme",
                  "type": "string"
                },
                "external_port": {
                  "description": "Port the platform dials (Gateway/entrypoint reachable port).",
                  "maximum": 65535,
                  "minimum": 1,
                  "title": "External Port",
                  "type": "integer"
                },
                "backend_port": {
                  "default": 5959,
                  "description": "Workspace Service port the HTTPRoute targets.",
                  "maximum": 65535,
                  "minimum": 1,
                  "title": "Backend Port",
                  "type": "integer"
                }
              },
              "required": [
                "gateway",
                "routing",
                "external_port"
              ],
              "title": "K8sReachabilityGateway",
              "type": "object"
            },
            "K8sReachabilityInCluster": {
              "description": "Platform IS in the same cluster \u2014 use headless-service DNS.",
              "properties": {
                "kind": {
                  "const": "in_cluster",
                  "default": "in_cluster",
                  "description": "Discriminator tag identifying this as in-cluster reachability.",
                  "title": "Kind",
                  "type": "string"
                }
              },
              "title": "K8sReachabilityInCluster",
              "type": "object"
            },
            "K8sReachabilityIngress": {
              "description": "Platform OUT of cluster \u2014 operator-supplied ingress URL pattern.",
              "properties": {
                "kind": {
                  "const": "ingress",
                  "default": "ingress",
                  "description": "Discriminator tag identifying this as ingress reachability.",
                  "title": "Kind",
                  "type": "string"
                },
                "url_template": {
                  "description": "wss:// URL template with {workspace_id} placeholder. The platform substitutes the id at attach-time.",
                  "title": "Url Template",
                  "type": "string"
                }
              },
              "required": [
                "url_template"
              ],
              "title": "K8sReachabilityIngress",
              "type": "object"
            },
            "KubernetesWorkspaceConfig": {
              "additionalProperties": false,
              "description": "Kubernetes provider \u2014 connection + reachability + variant slot.\n\nThe ``variant`` field reserves space for an ``agent_sandbox`` variant that\nmaterialises workspaces as Sandbox CRDs instead of StatefulSets.\nOnly ``system`` (StatefulSet+PVC+Headless Service+Secret) is implemented\nin v1; ``agent_sandbox`` is accepted on provider create but workspace\ncreate returns 501.\n\nImage, resources, storage class, security context, network policies,\npod overrides etc. moved to the template (see :class:`KubernetesTemplateConfig`).",
              "properties": {
                "kind": {
                  "const": "kubernetes",
                  "default": "kubernetes",
                  "description": "Substrate discriminator (matches WorkspaceProviderConfig union).",
                  "title": "Kind",
                  "type": "string"
                },
                "variant": {
                  "default": "system",
                  "description": "Which k8s materialisation strategy to use. `system` creates a StatefulSet+PVC; `agent_sandbox` (reserved) will create a Sandbox CRD.",
                  "enum": [
                    "system",
                    "agent_sandbox"
                  ],
                  "title": "Variant",
                  "type": "string"
                },
                "connection": {
                  "description": "How the platform reaches the kube apiserver.",
                  "discriminator": {
                    "mapping": {
                      "in_cluster": "#/$defs/K8sConnectionInCluster",
                      "kubeconfig": "#/$defs/K8sConnectionKubeconfig",
                      "service_account_token": "#/$defs/K8sConnectionServiceAccountToken"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/K8sConnectionInCluster"
                    },
                    {
                      "$ref": "#/$defs/K8sConnectionKubeconfig"
                    },
                    {
                      "$ref": "#/$defs/K8sConnectionServiceAccountToken"
                    }
                  ],
                  "title": "Connection"
                },
                "namespace": {
                  "description": "Namespace where workspaces are created.",
                  "title": "Namespace",
                  "type": "string"
                },
                "reachability": {
                  "description": "How the platform reaches primer-runtime inside workspace pods.",
                  "discriminator": {
                    "mapping": {
                      "gateway_httproute": "#/$defs/K8sReachabilityGateway",
                      "in_cluster": "#/$defs/K8sReachabilityInCluster",
                      "ingress": "#/$defs/K8sReachabilityIngress"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/K8sReachabilityInCluster"
                    },
                    {
                      "$ref": "#/$defs/K8sReachabilityIngress"
                    },
                    {
                      "$ref": "#/$defs/K8sReachabilityGateway"
                    }
                  ],
                  "title": "Reachability"
                },
                "image_pull_secrets": {
                  "description": "Names of pre-created imagePullSecrets in the namespace.",
                  "items": {
                    "type": "string"
                  },
                  "title": "Image Pull Secrets",
                  "type": "array"
                }
              },
              "required": [
                "connection",
                "namespace",
                "reachability"
              ],
              "title": "KubernetesWorkspaceConfig",
              "type": "object"
            },
            "LocalWorkspaceConfig": {
              "additionalProperties": false,
              "description": "Local provider \u2014 host-filesystem workspaces under a single root.\n\nWorkdir / exec timeouts / package management etc. moved off this\nconfig; local templates own materialisation. The only thing the\nprovider needs to know is where to put workspace directories.",
              "properties": {
                "kind": {
                  "const": "local",
                  "default": "local",
                  "description": "Discriminator tag identifying this as local-workspace config.",
                  "title": "Kind",
                  "type": "string"
                },
                "root_path": {
                  "default": "~/.primer/workspaces",
                  "description": "Root directory under which each workspace gets its own subdirectory. Tilde-expanded.",
                  "title": "Root Path",
                  "type": "string"
                }
              },
              "title": "LocalWorkspaceConfig",
              "type": "object"
            },
            "WorkspaceProvider": {
              "description": "A configured workspace backend.\n\nMirrors the ``LLMProvider`` / ``StorageProviderConfig`` /\n``VectorStoreProviderConfig`` pattern: an identifiable handle plus\na discriminated provider enum and a backend-specific ``config``\nsub-model.\n\nThe id is a user-chosen handle; :class:`WorkspaceTemplate` carries\na ``provider_id`` referencing this entry by id.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "provider": {
                  "$ref": "#/$defs/WorkspaceProviderType",
                  "description": "Which workspace backend this entry targets."
                },
                "config": {
                  "description": "Backend-specific connection settings; must match ``provider``.",
                  "discriminator": {
                    "mapping": {
                      "container": "#/$defs/ContainerWorkspaceConfig",
                      "kubernetes": "#/$defs/KubernetesWorkspaceConfig",
                      "local": "#/$defs/LocalWorkspaceConfig"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/LocalWorkspaceConfig"
                    },
                    {
                      "$ref": "#/$defs/ContainerWorkspaceConfig"
                    },
                    {
                      "$ref": "#/$defs/KubernetesWorkspaceConfig"
                    }
                  ],
                  "title": "Config"
                }
              },
              "required": [
                "id",
                "provider",
                "config"
              ],
              "title": "WorkspaceProvider",
              "type": "object"
            },
            "WorkspaceProviderType": {
              "description": "Supported workspace backends.\n\nThe string value is what gets serialised in configuration so it\nmust remain stable across releases.",
              "enum": [
                "local",
                "container",
                "kubernetes"
              ],
              "title": "WorkspaceProviderType",
              "type": "string"
            }
          },
          "properties": {
            "entity": {
              "$ref": "#/$defs/WorkspaceProvider",
              "description": "WorkspaceProvider body."
            }
          },
          "required": [
            "entity"
          ],
          "title": "_CreateProviderArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__delete_workspace_provider",
        "description": "Delete a WorkspaceProvider by id. Cascades to drop the cached backend instance from the WorkspaceRegistry.",
        "input_schema": {
          "properties": {
            "id": {
              "description": "Entity id.",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_IdArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__list_workspace_templates",
        "description": "List WorkspaceTemplates with pagination.",
        "input_schema": {
          "properties": {
            "limit": {
              "default": 20,
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__get_workspace_template",
        "description": "Fetch one WorkspaceTemplate by id; ``type=not-found`` if missing.",
        "input_schema": {
          "properties": {
            "id": {
              "description": "Entity id.",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_IdArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__create_workspace_template",
        "description": "Create a new WorkspaceTemplate. Body must reference an existing ``provider_id`` and include the materialisation recipe (packages, files, env, init_commands, resources).",
        "input_schema": {
          "$defs": {
            "ContainerMount": {
              "additionalProperties": false,
              "description": "Host \u2192 container mount declaration (template-owned).",
              "properties": {
                "host": {
                  "description": "Path on the host (provider's runtime).",
                  "title": "Host",
                  "type": "string"
                },
                "container": {
                  "description": "Mount point inside the container.",
                  "title": "Container",
                  "type": "string"
                },
                "readonly": {
                  "default": false,
                  "description": "Mount read-only.",
                  "title": "Readonly",
                  "type": "boolean"
                }
              },
              "required": [
                "host",
                "container"
              ],
              "title": "ContainerMount",
              "type": "object"
            },
            "ContainerNetworkConfig": {
              "additionalProperties": false,
              "description": "Container network policy.\n\n``egress`` toggles whether the workspace can reach outside the provider's\nnetwork. Best-effort: docker/podman support ``--internal`` networks\n(deny_all); containerd is CNI-dependent.",
              "properties": {
                "egress": {
                  "anyOf": [
                    {
                      "enum": [
                        "allow_all",
                        "deny_all"
                      ],
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "allow_all \u2192 default bridge (internet reachable); deny_all \u2192 docker --internal network (workspace-internal only); null \u2192 runtime default.",
                  "title": "Egress"
                }
              },
              "title": "ContainerNetworkConfig",
              "type": "object"
            },
            "ContainerTemplateConfig": {
              "additionalProperties": false,
              "description": "Container template variant \u2014 owns image, cpu/mem, mounts, network.\n\nImage is expected to derive from a primer-runtime base; the platform\nreaches the runtime via WSSandbox + RuntimeClient (Phase 5/6 work).",
              "properties": {
                "kind": {
                  "const": "container",
                  "default": "container",
                  "description": "Discriminator tag identifying this as container-template config.",
                  "title": "Kind",
                  "type": "string"
                },
                "image": {
                  "description": "OCI image; expected to derive from a primer-runtime base.",
                  "title": "Image",
                  "type": "string"
                },
                "entrypoint": {
                  "anyOf": [
                    {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Override the image's ENTRYPOINT; null = use image default.",
                  "title": "Entrypoint"
                },
                "user": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "uid:gid override; null = image default.",
                  "title": "User"
                },
                "workdir": {
                  "default": "/workspace",
                  "description": "Container workdir.",
                  "title": "Workdir",
                  "type": "string"
                },
                "cpu_cores": {
                  "anyOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "docker --cpus value; null = unlimited.",
                  "title": "Cpu Cores"
                },
                "memory_bytes": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "docker --memory value in bytes; null = unlimited.",
                  "title": "Memory Bytes"
                },
                "extra_mounts": {
                  "description": "Optional additional host mounts (operator opt-in).",
                  "items": {
                    "$ref": "#/$defs/ContainerMount"
                  },
                  "title": "Extra Mounts",
                  "type": "array"
                },
                "network": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/ContainerNetworkConfig"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Network policy; null = runtime default."
                }
              },
              "required": [
                "image"
              ],
              "title": "ContainerTemplateConfig",
              "type": "object"
            },
            "FileMount": {
              "description": "One file to seed into the workspace at materialisation.",
              "properties": {
                "path": {
                  "description": "Destination path inside the workspace (relative to the workspace root).",
                  "minLength": 1,
                  "title": "Path",
                  "type": "string"
                },
                "source": {
                  "description": "Where the file content comes from.",
                  "discriminator": {
                    "mapping": {
                      "document": "#/$defs/_DocumentSource",
                      "inline": "#/$defs/_InlineSource",
                      "secret": "#/$defs/_SecretSource",
                      "url": "#/$defs/_UrlSource"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/_InlineSource"
                    },
                    {
                      "$ref": "#/$defs/_UrlSource"
                    },
                    {
                      "$ref": "#/$defs/_DocumentSource"
                    },
                    {
                      "$ref": "#/$defs/_SecretSource"
                    }
                  ],
                  "title": "Source"
                },
                "mode": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Octal mode string (e.g. '0755'). When None, backends use 0644.",
                  "title": "Mode"
                }
              },
              "required": [
                "path",
                "source"
              ],
              "title": "FileMount",
              "type": "object"
            },
            "K8sVolume": {
              "additionalProperties": true,
              "description": "K8s Volume spec; passthrough to k8s. Forbidden 'name' must be present;\nother fields (configMap/secret/emptyDir/etc.) are validated by k8s itself.",
              "properties": {
                "name": {
                  "title": "Name",
                  "type": "string"
                }
              },
              "required": [
                "name"
              ],
              "title": "K8sVolume",
              "type": "object"
            },
            "K8sVolumeMount": {
              "additionalProperties": true,
              "description": "K8s VolumeMount spec; passthrough. `name` + `mountPath` required.",
              "properties": {
                "name": {
                  "title": "Name",
                  "type": "string"
                },
                "mountPath": {
                  "title": "Mountpath",
                  "type": "string"
                },
                "readOnly": {
                  "anyOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Readonly"
                }
              },
              "required": [
                "name",
                "mountPath"
              ],
              "title": "K8sVolumeMount",
              "type": "object"
            },
            "KubernetesTemplateConfig": {
              "additionalProperties": false,
              "description": "K8s template variant \u2014 system variant. agent_sandbox variant is reserved.\n\nImage is expected to derive from a primer-runtime base; runtime\nreachability is configured on the workspace *provider*, not the\ntemplate (see KubernetesWorkspaceConfig.reachability).",
              "properties": {
                "kind": {
                  "const": "kubernetes",
                  "default": "kubernetes",
                  "description": "Discriminator tag identifying this as k8s-template config.",
                  "title": "Kind",
                  "type": "string"
                },
                "image": {
                  "description": "OCI image; primer-runtime derivative.",
                  "title": "Image",
                  "type": "string"
                },
                "entrypoint": {
                  "anyOf": [
                    {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Entrypoint"
                },
                "args": {
                  "anyOf": [
                    {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Args"
                },
                "workdir": {
                  "default": "/workspace",
                  "title": "Workdir",
                  "type": "string"
                },
                "cpu_request": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "K8s resource request, e.g. '500m'.",
                  "title": "Cpu Request"
                },
                "cpu_limit": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "K8s resource limit, e.g. '2'.",
                  "title": "Cpu Limit"
                },
                "memory_request": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "e.g. '1Gi'.",
                  "title": "Memory Request"
                },
                "memory_limit": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "e.g. '4Gi'.",
                  "title": "Memory Limit"
                },
                "pvc_size": {
                  "default": "10Gi",
                  "description": "PVC size declared on the StatefulSet's volumeClaimTemplates.",
                  "title": "Pvc Size",
                  "type": "string"
                },
                "pvc_access_modes": {
                  "items": {
                    "enum": [
                      "ReadWriteOnce",
                      "ReadWriteMany"
                    ],
                    "type": "string"
                  },
                  "title": "Pvc Access Modes",
                  "type": "array"
                },
                "storage_class": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "StorageClass name; null = cluster default.",
                  "title": "Storage Class"
                },
                "extra_volumes": {
                  "items": {
                    "$ref": "#/$defs/K8sVolume"
                  },
                  "title": "Extra Volumes",
                  "type": "array"
                },
                "extra_volume_mounts": {
                  "items": {
                    "$ref": "#/$defs/K8sVolumeMount"
                  },
                  "title": "Extra Volume Mounts",
                  "type": "array"
                },
                "network_policy_name": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Name of a NetworkPolicy operators pre-create in the namespace.",
                  "title": "Network Policy Name"
                },
                "pod_overrides": {
                  "anyOf": [
                    {
                      "additionalProperties": true,
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Deep-merged into PodSpec at manifest build time.",
                  "title": "Pod Overrides"
                },
                "container_security_context_overrides": {
                  "anyOf": [
                    {
                      "additionalProperties": true,
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Container Security Context Overrides"
                }
              },
              "required": [
                "image"
              ],
              "title": "KubernetesTemplateConfig",
              "type": "object"
            },
            "LocalTemplateConfig": {
              "description": "Local-backend template config (no backend-specific fields today).",
              "properties": {
                "kind": {
                  "const": "local",
                  "default": "local",
                  "title": "Kind",
                  "type": "string"
                }
              },
              "title": "LocalTemplateConfig",
              "type": "object"
            },
            "ResourceLimits": {
              "description": "Backend-enforced bounds on a workspace.\n\nNone means \"no limit\" for that dimension; the backend SHOULD\nenforce limits where it can (cgroups / Docker resource flags /\netc.). Backends that cannot enforce a particular bound MAY emit a\nstartup warning but MUST NOT fail to materialise a workspace\npurely because they cannot enforce one.",
              "properties": {
                "cpu_cores": {
                  "anyOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Maximum CPU cores. None = no limit.",
                  "title": "Cpu Cores"
                },
                "memory_bytes": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Maximum resident memory in bytes. None = no limit.",
                  "title": "Memory Bytes"
                },
                "network": {
                  "default": "egress",
                  "description": "Network access mode. 'none' = no network, 'egress' = outbound only, 'full' = full bidirectional.",
                  "enum": [
                    "none",
                    "egress",
                    "full"
                  ],
                  "title": "Network",
                  "type": "string"
                },
                "disk_bytes": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Maximum disk usage in bytes. None = no limit.",
                  "title": "Disk Bytes"
                }
              },
              "title": "ResourceLimits",
              "type": "object"
            },
            "WorkspaceTemplate": {
              "description": "Declarative recipe for materialising a workspace.\n\nStored in a ``workspace_templates`` :class:`Collection`. One\ntemplate can provision many workspaces; each instantiation MAY\noverride individual fields (env additions, extra files) at create\ntime via :class:`WorkspaceTemplateOverrides`.\n\nTemplates do NOT carry backend-specific connection details (image\nreference, root path, etc.). Instead they reference a configured\n:class:`WorkspaceProvider` by id; the provider supplies the\nbackend-specific configuration, the template supplies the\ndeclarative materialisation recipe.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "provider_id": {
                  "description": "Identifier of the WorkspaceProvider that should materialise this template. Resolved at workspace-creation time; not validated here -- the runtime is responsible for matching the template against a configured provider.",
                  "minLength": 1,
                  "title": "Provider Id",
                  "type": "string"
                },
                "backend": {
                  "description": "Per-backend recipe fields. Must match the provider type the template targets.",
                  "discriminator": {
                    "mapping": {
                      "container": "#/$defs/ContainerTemplateConfig",
                      "kubernetes": "#/$defs/KubernetesTemplateConfig",
                      "local": "#/$defs/LocalTemplateConfig"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/LocalTemplateConfig"
                    },
                    {
                      "$ref": "#/$defs/ContainerTemplateConfig"
                    },
                    {
                      "$ref": "#/$defs/KubernetesTemplateConfig"
                    }
                  ],
                  "title": "Backend"
                },
                "files": {
                  "description": "Files to seed into the workspace at materialisation time.",
                  "items": {
                    "$ref": "#/$defs/FileMount"
                  },
                  "title": "Files",
                  "type": "array"
                },
                "env": {
                  "additionalProperties": {
                    "format": "password",
                    "type": "string",
                    "writeOnly": true
                  },
                  "description": "Environment variables injected for every shell session.",
                  "title": "Env",
                  "type": "object"
                },
                "init_commands": {
                  "description": "Shell commands run once after files land. Failure of any command is fatal to workspace materialisation.",
                  "items": {
                    "type": "string"
                  },
                  "title": "Init Commands",
                  "type": "array"
                },
                "state_path": {
                  "default": ".state",
                  "description": "Path inside the workspace root where the state repo lives.",
                  "minLength": 1,
                  "title": "State Path",
                  "type": "string"
                },
                "tmp_path": {
                  "default": ".tmp",
                  "description": "Path inside the workspace root where the truncation cache lives.",
                  "minLength": 1,
                  "title": "Tmp Path",
                  "type": "string"
                },
                "resources": {
                  "$ref": "#/$defs/ResourceLimits",
                  "description": "CPU / memory / network bounds the backend SHOULD enforce."
                }
              },
              "required": [
                "id",
                "description",
                "provider_id"
              ],
              "title": "WorkspaceTemplate",
              "type": "object"
            },
            "_DocumentSource": {
              "description": "File content sourced from a Document in a Collection.",
              "properties": {
                "kind": {
                  "const": "document",
                  "default": "document",
                  "description": "Discriminator tag identifying this as a Document source.",
                  "title": "Kind",
                  "type": "string"
                },
                "collection_id": {
                  "description": "Collection holding the source Document.",
                  "minLength": 1,
                  "title": "Collection Id",
                  "type": "string"
                },
                "document_id": {
                  "description": "Document id within the collection.",
                  "minLength": 1,
                  "title": "Document Id",
                  "type": "string"
                }
              },
              "required": [
                "collection_id",
                "document_id"
              ],
              "title": "_DocumentSource",
              "type": "object"
            },
            "_InlineSource": {
              "description": "File content supplied inline as a string.",
              "properties": {
                "kind": {
                  "const": "inline",
                  "default": "inline",
                  "description": "Discriminator tag identifying this as an inline source.",
                  "title": "Kind",
                  "type": "string"
                },
                "content": {
                  "description": "Verbatim file content.",
                  "title": "Content",
                  "type": "string"
                }
              },
              "required": [
                "content"
              ],
              "title": "_InlineSource",
              "type": "object"
            },
            "_SecretSource": {
              "description": "File content sourced from the host secret store.",
              "properties": {
                "kind": {
                  "const": "secret",
                  "default": "secret",
                  "description": "Discriminator tag identifying this as a secret-store reference.",
                  "title": "Kind",
                  "type": "string"
                },
                "name": {
                  "description": "Reference into the host secret store.",
                  "minLength": 1,
                  "title": "Name",
                  "type": "string"
                }
              },
              "required": [
                "name"
              ],
              "title": "_SecretSource",
              "type": "object"
            },
            "_UrlSource": {
              "description": "File content fetched from an HTTP(S) URL at materialisation.",
              "properties": {
                "kind": {
                  "const": "url",
                  "default": "url",
                  "description": "Discriminator tag identifying this as a URL source.",
                  "title": "Kind",
                  "type": "string"
                },
                "url": {
                  "description": "HTTP(S) URL to fetch the content from.",
                  "format": "uri",
                  "maxLength": 2083,
                  "minLength": 1,
                  "title": "Url",
                  "type": "string"
                },
                "sha256": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional SHA-256 of the expected content for integrity verification.",
                  "title": "Sha256"
                }
              },
              "required": [
                "url"
              ],
              "title": "_UrlSource",
              "type": "object"
            }
          },
          "properties": {
            "entity": {
              "$ref": "#/$defs/WorkspaceTemplate",
              "description": "WorkspaceTemplate body."
            }
          },
          "required": [
            "entity"
          ],
          "title": "_CreateTemplateArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__update_workspace_template",
        "description": "Replace an existing WorkspaceTemplate. The body's ``id`` must equal the path ``id``. Existing materialised Workspaces are NOT re-materialised; only future creates see the new recipe.",
        "input_schema": {
          "$defs": {
            "ContainerMount": {
              "additionalProperties": false,
              "description": "Host \u2192 container mount declaration (template-owned).",
              "properties": {
                "host": {
                  "description": "Path on the host (provider's runtime).",
                  "title": "Host",
                  "type": "string"
                },
                "container": {
                  "description": "Mount point inside the container.",
                  "title": "Container",
                  "type": "string"
                },
                "readonly": {
                  "default": false,
                  "description": "Mount read-only.",
                  "title": "Readonly",
                  "type": "boolean"
                }
              },
              "required": [
                "host",
                "container"
              ],
              "title": "ContainerMount",
              "type": "object"
            },
            "ContainerNetworkConfig": {
              "additionalProperties": false,
              "description": "Container network policy.\n\n``egress`` toggles whether the workspace can reach outside the provider's\nnetwork. Best-effort: docker/podman support ``--internal`` networks\n(deny_all); containerd is CNI-dependent.",
              "properties": {
                "egress": {
                  "anyOf": [
                    {
                      "enum": [
                        "allow_all",
                        "deny_all"
                      ],
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "allow_all \u2192 default bridge (internet reachable); deny_all \u2192 docker --internal network (workspace-internal only); null \u2192 runtime default.",
                  "title": "Egress"
                }
              },
              "title": "ContainerNetworkConfig",
              "type": "object"
            },
            "ContainerTemplateConfig": {
              "additionalProperties": false,
              "description": "Container template variant \u2014 owns image, cpu/mem, mounts, network.\n\nImage is expected to derive from a primer-runtime base; the platform\nreaches the runtime via WSSandbox + RuntimeClient (Phase 5/6 work).",
              "properties": {
                "kind": {
                  "const": "container",
                  "default": "container",
                  "description": "Discriminator tag identifying this as container-template config.",
                  "title": "Kind",
                  "type": "string"
                },
                "image": {
                  "description": "OCI image; expected to derive from a primer-runtime base.",
                  "title": "Image",
                  "type": "string"
                },
                "entrypoint": {
                  "anyOf": [
                    {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Override the image's ENTRYPOINT; null = use image default.",
                  "title": "Entrypoint"
                },
                "user": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "uid:gid override; null = image default.",
                  "title": "User"
                },
                "workdir": {
                  "default": "/workspace",
                  "description": "Container workdir.",
                  "title": "Workdir",
                  "type": "string"
                },
                "cpu_cores": {
                  "anyOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "docker --cpus value; null = unlimited.",
                  "title": "Cpu Cores"
                },
                "memory_bytes": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "docker --memory value in bytes; null = unlimited.",
                  "title": "Memory Bytes"
                },
                "extra_mounts": {
                  "description": "Optional additional host mounts (operator opt-in).",
                  "items": {
                    "$ref": "#/$defs/ContainerMount"
                  },
                  "title": "Extra Mounts",
                  "type": "array"
                },
                "network": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/ContainerNetworkConfig"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Network policy; null = runtime default."
                }
              },
              "required": [
                "image"
              ],
              "title": "ContainerTemplateConfig",
              "type": "object"
            },
            "FileMount": {
              "description": "One file to seed into the workspace at materialisation.",
              "properties": {
                "path": {
                  "description": "Destination path inside the workspace (relative to the workspace root).",
                  "minLength": 1,
                  "title": "Path",
                  "type": "string"
                },
                "source": {
                  "description": "Where the file content comes from.",
                  "discriminator": {
                    "mapping": {
                      "document": "#/$defs/_DocumentSource",
                      "inline": "#/$defs/_InlineSource",
                      "secret": "#/$defs/_SecretSource",
                      "url": "#/$defs/_UrlSource"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/_InlineSource"
                    },
                    {
                      "$ref": "#/$defs/_UrlSource"
                    },
                    {
                      "$ref": "#/$defs/_DocumentSource"
                    },
                    {
                      "$ref": "#/$defs/_SecretSource"
                    }
                  ],
                  "title": "Source"
                },
                "mode": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Octal mode string (e.g. '0755'). When None, backends use 0644.",
                  "title": "Mode"
                }
              },
              "required": [
                "path",
                "source"
              ],
              "title": "FileMount",
              "type": "object"
            },
            "K8sVolume": {
              "additionalProperties": true,
              "description": "K8s Volume spec; passthrough to k8s. Forbidden 'name' must be present;\nother fields (configMap/secret/emptyDir/etc.) are validated by k8s itself.",
              "properties": {
                "name": {
                  "title": "Name",
                  "type": "string"
                }
              },
              "required": [
                "name"
              ],
              "title": "K8sVolume",
              "type": "object"
            },
            "K8sVolumeMount": {
              "additionalProperties": true,
              "description": "K8s VolumeMount spec; passthrough. `name` + `mountPath` required.",
              "properties": {
                "name": {
                  "title": "Name",
                  "type": "string"
                },
                "mountPath": {
                  "title": "Mountpath",
                  "type": "string"
                },
                "readOnly": {
                  "anyOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Readonly"
                }
              },
              "required": [
                "name",
                "mountPath"
              ],
              "title": "K8sVolumeMount",
              "type": "object"
            },
            "KubernetesTemplateConfig": {
              "additionalProperties": false,
              "description": "K8s template variant \u2014 system variant. agent_sandbox variant is reserved.\n\nImage is expected to derive from a primer-runtime base; runtime\nreachability is configured on the workspace *provider*, not the\ntemplate (see KubernetesWorkspaceConfig.reachability).",
              "properties": {
                "kind": {
                  "const": "kubernetes",
                  "default": "kubernetes",
                  "description": "Discriminator tag identifying this as k8s-template config.",
                  "title": "Kind",
                  "type": "string"
                },
                "image": {
                  "description": "OCI image; primer-runtime derivative.",
                  "title": "Image",
                  "type": "string"
                },
                "entrypoint": {
                  "anyOf": [
                    {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Entrypoint"
                },
                "args": {
                  "anyOf": [
                    {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Args"
                },
                "workdir": {
                  "default": "/workspace",
                  "title": "Workdir",
                  "type": "string"
                },
                "cpu_request": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "K8s resource request, e.g. '500m'.",
                  "title": "Cpu Request"
                },
                "cpu_limit": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "K8s resource limit, e.g. '2'.",
                  "title": "Cpu Limit"
                },
                "memory_request": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "e.g. '1Gi'.",
                  "title": "Memory Request"
                },
                "memory_limit": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "e.g. '4Gi'.",
                  "title": "Memory Limit"
                },
                "pvc_size": {
                  "default": "10Gi",
                  "description": "PVC size declared on the StatefulSet's volumeClaimTemplates.",
                  "title": "Pvc Size",
                  "type": "string"
                },
                "pvc_access_modes": {
                  "items": {
                    "enum": [
                      "ReadWriteOnce",
                      "ReadWriteMany"
                    ],
                    "type": "string"
                  },
                  "title": "Pvc Access Modes",
                  "type": "array"
                },
                "storage_class": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "StorageClass name; null = cluster default.",
                  "title": "Storage Class"
                },
                "extra_volumes": {
                  "items": {
                    "$ref": "#/$defs/K8sVolume"
                  },
                  "title": "Extra Volumes",
                  "type": "array"
                },
                "extra_volume_mounts": {
                  "items": {
                    "$ref": "#/$defs/K8sVolumeMount"
                  },
                  "title": "Extra Volume Mounts",
                  "type": "array"
                },
                "network_policy_name": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Name of a NetworkPolicy operators pre-create in the namespace.",
                  "title": "Network Policy Name"
                },
                "pod_overrides": {
                  "anyOf": [
                    {
                      "additionalProperties": true,
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Deep-merged into PodSpec at manifest build time.",
                  "title": "Pod Overrides"
                },
                "container_security_context_overrides": {
                  "anyOf": [
                    {
                      "additionalProperties": true,
                      "type": "object"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Container Security Context Overrides"
                }
              },
              "required": [
                "image"
              ],
              "title": "KubernetesTemplateConfig",
              "type": "object"
            },
            "LocalTemplateConfig": {
              "description": "Local-backend template config (no backend-specific fields today).",
              "properties": {
                "kind": {
                  "const": "local",
                  "default": "local",
                  "title": "Kind",
                  "type": "string"
                }
              },
              "title": "LocalTemplateConfig",
              "type": "object"
            },
            "ResourceLimits": {
              "description": "Backend-enforced bounds on a workspace.\n\nNone means \"no limit\" for that dimension; the backend SHOULD\nenforce limits where it can (cgroups / Docker resource flags /\netc.). Backends that cannot enforce a particular bound MAY emit a\nstartup warning but MUST NOT fail to materialise a workspace\npurely because they cannot enforce one.",
              "properties": {
                "cpu_cores": {
                  "anyOf": [
                    {
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Maximum CPU cores. None = no limit.",
                  "title": "Cpu Cores"
                },
                "memory_bytes": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Maximum resident memory in bytes. None = no limit.",
                  "title": "Memory Bytes"
                },
                "network": {
                  "default": "egress",
                  "description": "Network access mode. 'none' = no network, 'egress' = outbound only, 'full' = full bidirectional.",
                  "enum": [
                    "none",
                    "egress",
                    "full"
                  ],
                  "title": "Network",
                  "type": "string"
                },
                "disk_bytes": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Maximum disk usage in bytes. None = no limit.",
                  "title": "Disk Bytes"
                }
              },
              "title": "ResourceLimits",
              "type": "object"
            },
            "WorkspaceTemplate": {
              "description": "Declarative recipe for materialising a workspace.\n\nStored in a ``workspace_templates`` :class:`Collection`. One\ntemplate can provision many workspaces; each instantiation MAY\noverride individual fields (env additions, extra files) at create\ntime via :class:`WorkspaceTemplateOverrides`.\n\nTemplates do NOT carry backend-specific connection details (image\nreference, root path, etc.). Instead they reference a configured\n:class:`WorkspaceProvider` by id; the provider supplies the\nbackend-specific configuration, the template supplies the\ndeclarative materialisation recipe.",
              "properties": {
                "id": {
                  "description": "User-defined identifier referenced by the application.",
                  "minLength": 1,
                  "title": "Id",
                  "type": "string"
                },
                "description": {
                  "description": "Free-form human-readable description of the entry.",
                  "title": "Description",
                  "type": "string"
                },
                "provider_id": {
                  "description": "Identifier of the WorkspaceProvider that should materialise this template. Resolved at workspace-creation time; not validated here -- the runtime is responsible for matching the template against a configured provider.",
                  "minLength": 1,
                  "title": "Provider Id",
                  "type": "string"
                },
                "backend": {
                  "description": "Per-backend recipe fields. Must match the provider type the template targets.",
                  "discriminator": {
                    "mapping": {
                      "container": "#/$defs/ContainerTemplateConfig",
                      "kubernetes": "#/$defs/KubernetesTemplateConfig",
                      "local": "#/$defs/LocalTemplateConfig"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/LocalTemplateConfig"
                    },
                    {
                      "$ref": "#/$defs/ContainerTemplateConfig"
                    },
                    {
                      "$ref": "#/$defs/KubernetesTemplateConfig"
                    }
                  ],
                  "title": "Backend"
                },
                "files": {
                  "description": "Files to seed into the workspace at materialisation time.",
                  "items": {
                    "$ref": "#/$defs/FileMount"
                  },
                  "title": "Files",
                  "type": "array"
                },
                "env": {
                  "additionalProperties": {
                    "format": "password",
                    "type": "string",
                    "writeOnly": true
                  },
                  "description": "Environment variables injected for every shell session.",
                  "title": "Env",
                  "type": "object"
                },
                "init_commands": {
                  "description": "Shell commands run once after files land. Failure of any command is fatal to workspace materialisation.",
                  "items": {
                    "type": "string"
                  },
                  "title": "Init Commands",
                  "type": "array"
                },
                "state_path": {
                  "default": ".state",
                  "description": "Path inside the workspace root where the state repo lives.",
                  "minLength": 1,
                  "title": "State Path",
                  "type": "string"
                },
                "tmp_path": {
                  "default": ".tmp",
                  "description": "Path inside the workspace root where the truncation cache lives.",
                  "minLength": 1,
                  "title": "Tmp Path",
                  "type": "string"
                },
                "resources": {
                  "$ref": "#/$defs/ResourceLimits",
                  "description": "CPU / memory / network bounds the backend SHOULD enforce."
                }
              },
              "required": [
                "id",
                "description",
                "provider_id"
              ],
              "title": "WorkspaceTemplate",
              "type": "object"
            },
            "_DocumentSource": {
              "description": "File content sourced from a Document in a Collection.",
              "properties": {
                "kind": {
                  "const": "document",
                  "default": "document",
                  "description": "Discriminator tag identifying this as a Document source.",
                  "title": "Kind",
                  "type": "string"
                },
                "collection_id": {
                  "description": "Collection holding the source Document.",
                  "minLength": 1,
                  "title": "Collection Id",
                  "type": "string"
                },
                "document_id": {
                  "description": "Document id within the collection.",
                  "minLength": 1,
                  "title": "Document Id",
                  "type": "string"
                }
              },
              "required": [
                "collection_id",
                "document_id"
              ],
              "title": "_DocumentSource",
              "type": "object"
            },
            "_InlineSource": {
              "description": "File content supplied inline as a string.",
              "properties": {
                "kind": {
                  "const": "inline",
                  "default": "inline",
                  "description": "Discriminator tag identifying this as an inline source.",
                  "title": "Kind",
                  "type": "string"
                },
                "content": {
                  "description": "Verbatim file content.",
                  "title": "Content",
                  "type": "string"
                }
              },
              "required": [
                "content"
              ],
              "title": "_InlineSource",
              "type": "object"
            },
            "_SecretSource": {
              "description": "File content sourced from the host secret store.",
              "properties": {
                "kind": {
                  "const": "secret",
                  "default": "secret",
                  "description": "Discriminator tag identifying this as a secret-store reference.",
                  "title": "Kind",
                  "type": "string"
                },
                "name": {
                  "description": "Reference into the host secret store.",
                  "minLength": 1,
                  "title": "Name",
                  "type": "string"
                }
              },
              "required": [
                "name"
              ],
              "title": "_SecretSource",
              "type": "object"
            },
            "_UrlSource": {
              "description": "File content fetched from an HTTP(S) URL at materialisation.",
              "properties": {
                "kind": {
                  "const": "url",
                  "default": "url",
                  "description": "Discriminator tag identifying this as a URL source.",
                  "title": "Kind",
                  "type": "string"
                },
                "url": {
                  "description": "HTTP(S) URL to fetch the content from.",
                  "format": "uri",
                  "maxLength": 2083,
                  "minLength": 1,
                  "title": "Url",
                  "type": "string"
                },
                "sha256": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional SHA-256 of the expected content for integrity verification.",
                  "title": "Sha256"
                }
              },
              "required": [
                "url"
              ],
              "title": "_UrlSource",
              "type": "object"
            }
          },
          "properties": {
            "id": {
              "minLength": 1,
              "title": "Id",
              "type": "string"
            },
            "entity": {
              "$ref": "#/$defs/WorkspaceTemplate"
            }
          },
          "required": [
            "id",
            "entity"
          ],
          "title": "_UpdateTemplateArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__delete_workspace_template",
        "description": "Delete a WorkspaceTemplate. Existing Workspaces that referenced it keep their snapshot ``template_id`` but the row no longer resolves.",
        "input_schema": {
          "properties": {
            "id": {
              "description": "Entity id.",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_IdArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__list_workspaces",
        "description": "List persisted Workspace rows with pagination.",
        "input_schema": {
          "properties": {
            "limit": {
              "default": 20,
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "anyOf": [
                {
                  "minimum": 0,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Offset"
            },
            "cursor": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Cursor"
            },
            "order_by": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Order By"
            }
          },
          "title": "_PaginationArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__get_workspace",
        "description": "Fetch one Workspace row by id; ``type=not-found`` if missing.",
        "input_schema": {
          "properties": {
            "id": {
              "description": "Entity id.",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_IdArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__create_workspace",
        "description": "Materialise a new Workspace from a template. Looks up the template, asks the matching backend to create the live instance, then persists a Workspace row with the assigned id. Optional ``overrides`` layer per-instantiation env vars / files / init commands on top of the template.",
        "input_schema": {
          "$defs": {
            "FileMount": {
              "description": "One file to seed into the workspace at materialisation.",
              "properties": {
                "path": {
                  "description": "Destination path inside the workspace (relative to the workspace root).",
                  "minLength": 1,
                  "title": "Path",
                  "type": "string"
                },
                "source": {
                  "description": "Where the file content comes from.",
                  "discriminator": {
                    "mapping": {
                      "document": "#/$defs/_DocumentSource",
                      "inline": "#/$defs/_InlineSource",
                      "secret": "#/$defs/_SecretSource",
                      "url": "#/$defs/_UrlSource"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/_InlineSource"
                    },
                    {
                      "$ref": "#/$defs/_UrlSource"
                    },
                    {
                      "$ref": "#/$defs/_DocumentSource"
                    },
                    {
                      "$ref": "#/$defs/_SecretSource"
                    }
                  ],
                  "title": "Source"
                },
                "mode": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Octal mode string (e.g. '0755'). When None, backends use 0644.",
                  "title": "Mode"
                }
              },
              "required": [
                "path",
                "source"
              ],
              "title": "FileMount",
              "type": "object"
            },
            "WorkspaceTemplateOverrides": {
              "description": "Per-instantiation tweaks layered on top of a :class:`WorkspaceTemplate`.\n\nOverride semantics are merge-then-extend:\n\n* ``env`` -- caller's keys overlay the template's keys (caller wins\n  on conflict).\n* ``files`` -- caller's list extends the template's list (both\n  apply; later mounts win on path conflict).\n* ``init_commands`` -- caller's list extends the template's list\n  (template's commands run first, then caller's).",
              "properties": {
                "env": {
                  "additionalProperties": {
                    "format": "password",
                    "type": "string",
                    "writeOnly": true
                  },
                  "title": "Env",
                  "type": "object"
                },
                "files": {
                  "items": {
                    "$ref": "#/$defs/FileMount"
                  },
                  "title": "Files",
                  "type": "array"
                },
                "init_commands": {
                  "items": {
                    "type": "string"
                  },
                  "title": "Init Commands",
                  "type": "array"
                }
              },
              "title": "WorkspaceTemplateOverrides",
              "type": "object"
            },
            "_DocumentSource": {
              "description": "File content sourced from a Document in a Collection.",
              "properties": {
                "kind": {
                  "const": "document",
                  "default": "document",
                  "description": "Discriminator tag identifying this as a Document source.",
                  "title": "Kind",
                  "type": "string"
                },
                "collection_id": {
                  "description": "Collection holding the source Document.",
                  "minLength": 1,
                  "title": "Collection Id",
                  "type": "string"
                },
                "document_id": {
                  "description": "Document id within the collection.",
                  "minLength": 1,
                  "title": "Document Id",
                  "type": "string"
                }
              },
              "required": [
                "collection_id",
                "document_id"
              ],
              "title": "_DocumentSource",
              "type": "object"
            },
            "_InlineSource": {
              "description": "File content supplied inline as a string.",
              "properties": {
                "kind": {
                  "const": "inline",
                  "default": "inline",
                  "description": "Discriminator tag identifying this as an inline source.",
                  "title": "Kind",
                  "type": "string"
                },
                "content": {
                  "description": "Verbatim file content.",
                  "title": "Content",
                  "type": "string"
                }
              },
              "required": [
                "content"
              ],
              "title": "_InlineSource",
              "type": "object"
            },
            "_SecretSource": {
              "description": "File content sourced from the host secret store.",
              "properties": {
                "kind": {
                  "const": "secret",
                  "default": "secret",
                  "description": "Discriminator tag identifying this as a secret-store reference.",
                  "title": "Kind",
                  "type": "string"
                },
                "name": {
                  "description": "Reference into the host secret store.",
                  "minLength": 1,
                  "title": "Name",
                  "type": "string"
                }
              },
              "required": [
                "name"
              ],
              "title": "_SecretSource",
              "type": "object"
            },
            "_UrlSource": {
              "description": "File content fetched from an HTTP(S) URL at materialisation.",
              "properties": {
                "kind": {
                  "const": "url",
                  "default": "url",
                  "description": "Discriminator tag identifying this as a URL source.",
                  "title": "Kind",
                  "type": "string"
                },
                "url": {
                  "description": "HTTP(S) URL to fetch the content from.",
                  "format": "uri",
                  "maxLength": 2083,
                  "minLength": 1,
                  "title": "Url",
                  "type": "string"
                },
                "sha256": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "description": "Optional SHA-256 of the expected content for integrity verification.",
                  "title": "Sha256"
                }
              },
              "required": [
                "url"
              ],
              "title": "_UrlSource",
              "type": "object"
            }
          },
          "properties": {
            "id": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "title": "Id"
            },
            "template_id": {
              "minLength": 1,
              "title": "Template Id",
              "type": "string"
            },
            "overrides": {
              "anyOf": [
                {
                  "$ref": "#/$defs/WorkspaceTemplateOverrides"
                },
                {
                  "type": "null"
                }
              ],
              "default": null
            }
          },
          "required": [
            "template_id"
          ],
          "title": "_CreateWorkspaceArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__delete_workspace",
        "description": "Destroy a Workspace \u2014 backend resources AND the persisted row. ``type=not-found`` when the id is unknown.",
        "input_schema": {
          "properties": {
            "id": {
              "description": "Entity id.",
              "minLength": 1,
              "title": "Id",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "title": "_IdArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__list_workspace_sessions",
        "description": "List sessions on a workspace, paginated. ``items`` is a list of SessionInfo objects; ``total`` is the full count.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "limit": {
              "default": 20,
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "default": 0,
              "minimum": 0,
              "title": "Offset",
              "type": "integer"
            }
          },
          "required": [
            "workspace_id"
          ],
          "title": "_WorkspaceListSessionsArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__get_workspace_session",
        "description": "Get session state \u2014 returns ``{info, status}`` where ``info`` is the SessionInfo and ``status`` is the current lifecycle state (running / waiting / paused / ended).",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "session_id": {
              "minLength": 1,
              "title": "Session Id",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "session_id"
          ],
          "title": "_WorkspaceSessionArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__pause_workspace_session",
        "description": "Request that a session pause at the next safe point. ``type=conflict`` when the session is in an incompatible lifecycle state (already ended, etc.).",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "session_id": {
              "minLength": 1,
              "title": "Session Id",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "session_id"
          ],
          "title": "_WorkspaceSessionArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__resume_workspace_session",
        "description": "Request that a paused session resume. ``type=conflict`` when the session is not currently paused.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "session_id": {
              "minLength": 1,
              "title": "Session Id",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "session_id"
          ],
          "title": "_WorkspaceSessionArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__steer_workspace_session",
        "description": "Append a steering user instruction to a running session. The agent will see it on its next turn. Returns the appended Instruction object on success.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "session_id": {
              "minLength": 1,
              "title": "Session Id",
              "type": "string"
            },
            "instruction": {
              "minLength": 1,
              "title": "Instruction",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "session_id",
            "instruction"
          ],
          "title": "_SteerArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__list_workspace_files",
        "description": "List files in a workspace at ``path`` (default: root) with pagination. ``recursive=true`` walks the whole tree. Each item is a FileEntry (path, kind, size_bytes, modified_at).",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "path": {
              "default": ".",
              "title": "Path",
              "type": "string"
            },
            "recursive": {
              "default": false,
              "title": "Recursive",
              "type": "boolean"
            },
            "limit": {
              "default": 200,
              "maximum": 2000,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            },
            "offset": {
              "default": 0,
              "minimum": 0,
              "title": "Offset",
              "type": "integer"
            }
          },
          "required": [
            "workspace_id"
          ],
          "title": "_ListFilesArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__get_workspace_file_info",
        "description": "Fetch the FileEntry for a single path (file / dir / symlink). ``type=not-found`` when missing; ``type=bad-request`` on path-escape attempts.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "path": {
              "minLength": 1,
              "title": "Path",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "path"
          ],
          "title": "_WorkspacePathArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__read_workspace_file",
        "description": "Read a workspace file. ``encoding=text`` returns UTF-8 decoded content; ``encoding=base64`` returns the raw bytes as base64 (use this for binaries). Returns ``{path, encoding, content, size_bytes}``.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "path": {
              "minLength": 1,
              "title": "Path",
              "type": "string"
            },
            "encoding": {
              "default": "text",
              "description": "``text`` (UTF-8 decode) or ``base64`` (binary as base64).",
              "title": "Encoding",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "path"
          ],
          "title": "_ReadFileArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__delete_workspace_file",
        "description": "Delete a file or empty directory. Refuses to delete the workspace root or paths inside ``.state`` / ``.tmp`` with ``type=bad-request``.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "path": {
              "minLength": 1,
              "title": "Path",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "path"
          ],
          "title": "_WorkspacePathArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__write_workspace_file",
        "description": "Replace (or create) a workspace file. ``encoding=text`` UTF-8 encodes the content; ``encoding=base64`` decodes the content first. Creates parent directories as needed. Refuses to overwrite a directory or write inside reserved ``.state`` / ``.tmp`` trees.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "path": {
              "minLength": 1,
              "title": "Path",
              "type": "string"
            },
            "content": {
              "description": "File content; encoding selects interpretation.",
              "title": "Content",
              "type": "string"
            },
            "encoding": {
              "default": "text",
              "description": "``text`` (UTF-8 encoded as-is) or ``base64`` (decoded to raw bytes).",
              "title": "Encoding",
              "type": "string"
            }
          },
          "required": [
            "workspace_id",
            "path",
            "content"
          ],
          "title": "_WriteFileArgs",
          "type": "object"
        }
      },
      {
        "id": "workspace_ext__watch_files",
        "description": "Watch one or more workspace-relative paths and pause this agent turn until something changes. YIELDING TOOL \u2014 execution is suspended; the worker is released to handle other sessions while the watcher polls. Files and directories are both accepted; directories report child-file changes. Optional ``timeout_seconds`` (falls back to the global yield cap). Optional ``batch_window_ms`` (default 250) coalesces bursts of changes into one wake. Returns ``{timed_out: false, changes: [...]}`` on change, ``{timed_out: true, changes: []}`` on timeout, or ``{cancelled: true, ...}`` if the operator skipped the yield. Each change carries ``{path, event_type (created|modified|deleted), mtime_after}``.",
        "input_schema": {
          "description": "Workspace-relative paths to watch + polling/coalescing controls.",
          "properties": {
            "paths": {
              "description": "Workspace-relative paths to watch. Files and directories are both accepted; directories are watched recursively (at one level \u2014 child file mtime changes count). Paths must NOT be absolute and must NOT contain ``..`` segments (no traversal). Required, at least one path.",
              "items": {
                "type": "string"
              },
              "maxItems": 200,
              "minItems": 1,
              "title": "Paths",
              "type": "array"
            },
            "timeout_seconds": {
              "anyOf": [
                {
                  "exclusiveMinimum": 0.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Optional per-call timeout. When omitted, falls back to the global yield cap. If no change fires before the deadline, the resume hook returns ``{timed_out: true, changes: [], elapsed_seconds: ...}``.",
              "title": "Timeout Seconds"
            },
            "batch_window_ms": {
              "default": 250,
              "description": "After the first change is detected, the watcher waits this many milliseconds for more changes before publishing one coalesced burst. Defaults to 250ms \u2014 tune up for noisy file systems (long compile, large directory tree).",
              "maximum": 5000,
              "minimum": 0,
              "title": "Batch Window Ms",
              "type": "integer"
            }
          },
          "required": [
            "paths"
          ],
          "title": "_WatchFilesArgs",
          "type": "object"
        }
      },
      {
        "id": "workspaces__get_workspace_log",
        "description": "Fetch up to ``limit`` recent commits from the workspace's ``.state`` git repo. Newest first. Each commit carries the parsed ``X-Primer-*`` trailers (workspace, session, agent, op, tool, call) for structured rendering.",
        "input_schema": {
          "properties": {
            "workspace_id": {
              "minLength": 1,
              "title": "Workspace Id",
              "type": "string"
            },
            "limit": {
              "default": 50,
              "maximum": 500,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          "required": [
            "workspace_id"
          ],
          "title": "_LogArgs",
          "type": "object"
        }
      },
      {
        "id": "misc__get_datetime",
        "description": "Current date and time as ISO 8601 plus Unix epoch seconds. Optional ``timezone`` (IANA name, e.g. 'America/New_York') \u2014 defaults to UTC. Returns ``{datetime, timezone, unix}``. Use this whenever you need the wall-clock time; do not estimate.",
        "input_schema": {
          "description": "Optional timezone selector for the returned datetime.",
          "properties": {
            "timezone": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "IANA timezone name (e.g. 'UTC', 'America/Los_Angeles', 'Asia/Tokyo'). Defaults to UTC. Invalid names return ``is_error=true`` ``type=bad-request``.",
              "title": "Timezone"
            }
          },
          "title": "_GetDatetimeArgs",
          "type": "object"
        }
      },
      {
        "id": "system__ask_user",
        "description": "Ask the human operator a question and pause the agent's turn until they respond. YIELDING TOOL \u2014 the agent's execution is suspended while the operator types; the worker is released to handle other sessions in the meantime. Use this when you genuinely need human input (clarification, approval, a choice the agent can't make autonomously) \u2014 do not use it for status updates or progress reports. Optional ``timeout_seconds`` (falls back to the global yield cap). Optional ``response_schema`` (JSON Schema validated server-side). Returns ``{response: <any>}`` on operator reply, ``{timed_out: true, elapsed_seconds}`` on timeout, or ``{cancelled: true, reason, elapsed_seconds}`` if the operator skipped the prompt.",
        "input_schema": {
          "description": "Prompt the operator sees and shape of the expected reply.",
          "properties": {
            "prompt": {
              "description": "Question or instruction shown to the operator. Newlines are preserved by the UI panel. Required.",
              "maxLength": 8000,
              "minLength": 1,
              "title": "Prompt",
              "type": "string"
            },
            "response_schema": {
              "anyOf": [
                {
                  "additionalProperties": true,
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Optional JSON Schema the operator's response must satisfy. Validated server-side at POST time; a violation is surfaced inline in the UI without resuming the agent. Omit for free-text responses.",
              "title": "Response Schema"
            },
            "timeout_seconds": {
              "anyOf": [
                {
                  "exclusiveMinimum": 0.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Optional per-call timeout. When omitted, falls back to the global yield cap (default 60 minutes). If the operator doesn't respond in time the resume hook returns ``{timed_out: true, elapsed_seconds: ...}`` so the agent can decide whether to retry or proceed.",
              "title": "Timeout Seconds"
            }
          },
          "required": [
            "prompt"
          ],
          "title": "_AskUserArgs",
          "type": "object"
        }
      },
      {
        "id": "workspace_ext__sleep",
        "description": "Pause this agent turn for ``seconds`` seconds (fractional allowed). YIELDING TOOL \u2014 the agent's execution is suspended for the requested duration; the worker is released to handle other sessions in the meantime. Useful for polling external state with backoff or for deliberate pacing. Returns ``{requested_seconds, elapsed_seconds}``. If the operator cancels the yield mid-sleep, the result additionally includes ``cancelled: true``.",
        "input_schema": {
          "description": "How long to pause the calling agent's turn for.",
          "properties": {
            "seconds": {
              "description": "Number of seconds to sleep. Fractional values are honoured. Bounded by the global yield-timeout cap (default 60 minutes). Longer waits are documented as a future Yielding-Tools follow-up rather than today's tool surface.",
              "minimum": 0.0,
              "title": "Seconds",
              "type": "number"
            }
          },
          "required": [
            "seconds"
          ],
          "title": "_SleepArgs",
          "type": "object"
        }
      },
      {
        "id": "misc__uuid_v4",
        "description": "Generate one or more cryptographically random UUIDv4 strings. Use this whenever you need a fresh stable identifier (entity id, conversation tag, dedup key) \u2014 do not invent one yourself, LLM-generated 'random' strings are low entropy. Returns ``{uuids: [...]}``.",
        "input_schema": {
          "description": "How many UUIDs to generate.",
          "properties": {
            "count": {
              "default": 1,
              "description": "Number of UUIDv4 strings to generate (1-100, default 1). Each is a fresh random uuid produced by the OS CSPRNG.",
              "maximum": 100,
              "minimum": 1,
              "title": "Count",
              "type": "integer"
            }
          },
          "title": "_UuidV4Args",
          "type": "object"
        }
      },
      {
        "id": "misc__hash",
        "description": "Compute a hex digest of an input string. Default algorithm is sha256 (suitable for content addressing); sha1 and md5 are available for interop only \u2014 neither is cryptographically safe. Returns ``{algorithm, hex_digest}``.",
        "input_schema": {
          "description": "Compute a hex digest of an input string.",
          "properties": {
            "input": {
              "description": "String to hash. Encoded as UTF-8 before digesting. Empty string is allowed.",
              "title": "Input",
              "type": "string"
            },
            "algorithm": {
              "default": "sha256",
              "description": "Digest algorithm. ``sha256`` (default) for content addressing; ``sha1`` and ``md5`` available for interop with legacy systems \u2014 neither is cryptographically safe.",
              "enum": [
                "sha256",
                "sha1",
                "md5"
              ],
              "title": "Algorithm",
              "type": "string"
            }
          },
          "required": [
            "input"
          ],
          "title": "_HashArgs",
          "type": "object"
        }
      },
      {
        "id": "misc__calculate",
        "description": "Evaluate an arithmetic expression safely. Supports +, -, *, /, //, %, **, parentheses, unary +/-, an allowlist of math functions (abs, round, min, max, pow, sqrt, log, log2, log10, exp, sin, cos, tan, asin, acos, atan, floor, ceil), and the constants pi, e, tau. NOT a Python eval \u2014 no attribute access, no comprehensions, no string ops. Use this whenever you need correct arithmetic; do not compute by hand. Returns ``{expression, result}``.",
        "input_schema": {
          "description": "Arithmetic expression to evaluate.",
          "properties": {
            "expression": {
              "description": "Arithmetic expression. Supports +, -, *, /, //, %, **, parentheses, unary +/-. Allowed functions: abs, round, min, max, pow, sqrt, log, log2, log10, exp, sin, cos, tan, asin, acos, atan, floor, ceil. Allowed constants: pi, e, tau. NO Python attribute access, subscripts, comprehensions, or string operations. Examples: '2 + 2', 'sqrt(16) * pi', '(100 - 32) * 5 / 9'.",
              "maxLength": 1024,
              "minLength": 1,
              "title": "Expression",
              "type": "string"
            }
          },
          "required": [
            "expression"
          ],
          "title": "_CalculateArgs",
          "type": "object"
        }
      },
      {
        "id": "web__web-search",
        "description": "Perform a web search and return up to ``count`` title/url/snippet results. Use for fact lookup, current events, or finding canonical documentation pages.",
        "input_schema": {
          "description": "Arguments for the ``web-search`` tool.",
          "properties": {
            "query": {
              "description": "Free-text search query.",
              "minLength": 1,
              "title": "Query",
              "type": "string"
            },
            "count": {
              "default": 5,
              "description": "Maximum number of results to return. Backends may return fewer.",
              "maximum": 25,
              "minimum": 1,
              "title": "Count",
              "type": "integer"
            },
            "safe_search": {
              "default": "moderate",
              "description": "SafeSearch level passed to the backend. ``moderate`` is the default; ``strict`` filters explicit content where the engine supports it; ``off`` disables filtering.",
              "enum": [
                "off",
                "moderate",
                "strict"
              ],
              "title": "Safe Search",
              "type": "string"
            }
          },
          "required": [
            "query"
          ],
          "title": "WebSearchArgs",
          "type": "object"
        }
      },
      {
        "id": "web__http-request",
        "description": "Perform an HTTP request against ``url`` with the given ``method`` and optional ``headers`` / ``body``. Returns JSON with the response status, headers, and body. The body is truncated past the configured byte cap.",
        "input_schema": {
          "description": "Arguments for the ``http-request`` tool.",
          "properties": {
            "url": {
              "description": "Absolute URL to fetch (http or https).",
              "format": "uri",
              "maxLength": 2083,
              "minLength": 1,
              "title": "Url",
              "type": "string"
            },
            "method": {
              "default": "GET",
              "description": "HTTP method.",
              "enum": [
                "GET",
                "POST",
                "PUT",
                "PATCH",
                "DELETE",
                "HEAD",
                "OPTIONS"
              ],
              "title": "Method",
              "type": "string"
            },
            "headers": {
              "anyOf": [
                {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "type": "object"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Optional request headers. Keys and values must both be strings.",
              "title": "Headers"
            },
            "body": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "default": null,
              "description": "Optional request body as a string. Callers are responsible for serialising structured payloads (JSON, form encoding, etc.) before invocation.",
              "title": "Body"
            },
            "timeout_seconds": {
              "default": 30.0,
              "description": "Per-request timeout in seconds.",
              "exclusiveMinimum": 0,
              "maximum": 300,
              "title": "Timeout Seconds",
              "type": "number"
            }
          },
          "required": [
            "url"
          ],
          "title": "HttpRequestArgs",
          "type": "object"
        }
      },
      {
        "id": "harness__harness__list",
        "description": "List harnesses with optional filters. Returns a paginated response with items, total, offset, and length.",
        "input_schema": {
          "type": "object",
          "properties": {
            "slug": {
              "type": "string",
              "description": "Filter by exact slug."
            },
            "status": {
              "type": "string",
              "enum": [
                "draft",
                "ready",
                "installed",
                "outdated",
                "error"
              ],
              "description": "Filter by harness status."
            },
            "offset": {
              "type": "integer",
              "minimum": 0,
              "default": 0
            },
            "length": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100,
              "default": 50
            }
          }
        }
      },
      {
        "id": "harness__harness__get",
        "description": "Get a single harness by id. Returns the full row or is_error=true type=not-found.",
        "input_schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string",
              "description": "The harness id (e.g. hns_abc123)."
            }
          }
        }
      },
      {
        "id": "harness__harness__register",
        "description": "Register a new harness (git-backed bundle). Status: DRAFT. Call harness__fetch next to load schema.",
        "input_schema": {
          "type": "object",
          "required": [
            "name",
            "slug",
            "git_url"
          ],
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1,
              "maxLength": 200
            },
            "slug": {
              "type": "string",
              "pattern": "^[a-z][a-z0-9-]{1,63}$",
              "description": "Unique kebab-case identifier."
            },
            "git_url": {
              "type": "string",
              "minLength": 1
            },
            "ref": {
              "type": "string",
              "minLength": 1,
              "description": "Git ref. Defaults to 'main'."
            },
            "subpath": {
              "type": "string",
              "description": "Subdirectory within the repo."
            },
            "git_token": {
              "type": "string",
              "description": "Personal access token (stored encrypted)."
            },
            "description": {
              "type": "string",
              "maxLength": 2000
            }
          }
        }
      },
      {
        "id": "harness__harness__update",
        "description": "Update mutable metadata on a harness (name, description, ref, subpath, git_token).",
        "input_schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string"
            },
            "name": {
              "type": "string",
              "minLength": 1,
              "maxLength": 200
            },
            "description": {
              "type": "string",
              "maxLength": 2000
            },
            "ref": {
              "type": "string",
              "minLength": 1
            },
            "subpath": {
              "type": "string"
            },
            "git_token": {
              "type": "string"
            }
          }
        }
      },
      {
        "id": "harness__harness__update_overrides",
        "description": "Validate overrides against the cached schema and store them. Returns is_error=true type=overrides-invalid if validation fails. Returns is_error=true type=overrides-schema-missing if no schema is cached.",
        "input_schema": {
          "type": "object",
          "required": [
            "id",
            "overrides"
          ],
          "properties": {
            "id": {
              "type": "string"
            },
            "overrides": {
              "type": "object",
              "description": "Override values dict."
            }
          }
        }
      },
      {
        "id": "harness__harness__fetch",
        "description": "Enqueue a FETCH operation for the harness. Returns 202-style ack with pending_operation=fetch. Returns is_error=true type=conflict if already pending.",
        "input_schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string"
            }
          }
        }
      },
      {
        "id": "harness__harness__install",
        "description": "Enqueue an INSTALL operation. Requires status in [draft, ready, outdated] and an overrides schema cached. Returns is_error=true on conflict or missing schema.",
        "input_schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string"
            }
          }
        }
      },
      {
        "id": "harness__harness__sync",
        "description": "Enqueue a SYNC operation. Requires status in [installed, outdated] and a fetched bundle. Returns is_error=true on conflict or missing bundle.",
        "input_schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string"
            }
          }
        }
      },
      {
        "id": "harness__harness__uninstall",
        "description": "Enqueue an UNINSTALL operation. Returns is_error=true type=conflict if a pending operation is already set.",
        "input_schema": {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "type": "string"
            }
          }
        }
      }
    ]
  }
}